# TODO

Open work only. This file was restructured on 2026-08-16: the record of
completed campaigns — closed rows, narratives, measurements, wrong turns and
their corrections, all previously in this file — is frozen verbatim in
[`history/todo_ledger_archive.md`](history/todo_ledger_archive.md), and the
recurring lessons distilled from that record live in
[`experiences.md`](experiences.md). Code and test comments citing a "todo row"
by U-number resolve against the archive. Standing
deliberately-not-being-done decisions also live in `experiences.md`.

When an item below closes: move its row (with the closing measurements) to the
archive under a dated note, and if it taught something reusable, add the
lesson to `experiences.md`.

## Open, most actionable first

| item | state | notes |
|---|---|---|
| Twiss and dispersion analysis | **theory complete incl. mode degeneracy; architecture decided 2026-09-11; stage 1 landed 2026-09-11, stage 2 next (updated 2026-09-11)** | [Theory](theory/twiss_dispersion.md) Sections 1–13 (Section 13 = mode degeneracy, checked by the [degeneracy record](history/twiss_dispersion_degeneracy_theory_2026_09_11.md)); [design note](design/twiss_dispersion_analysis.md) records the decided architecture, resolution criterion, verification plan and staging; earlier evidence: [initial checks](history/audit_twiss_dispersion_theory_2026_09_08.md), [notation review](history/audit_twiss_dispersion_notation_2026_09_09.md), [literature review](history/audit_twiss_dispersion_literature_2026_09_09.md). Plan (owner adopted 2026-09-11; stages 1–7 are the design note's staged commits, each full-gate class with its ledgers travelling with it and one full gate on the assembled tree before push; stage 8 is the external-benchmark batch the design note's Staging section describes): (1) `feat(analysis)` symplectic kernel, `Determined`/`AmbiguitySet` vocabularies, `one_turn_matrix` with complex-step, finite-difference and ForwardDiff-extension tags, the two suite Jacobian closures switched to it; (2) `feat(analysis)` 4D eigenmode, Mais–Ripken, Edwards–Teng with the uncoupled-FODO and coupled-construction tests; (3) `feat(analysis)` mode clusters, Krein classification, ambiguity set, the resolution-chord measurement table and frozen default; (4) `feat(analysis)` 6D dispersion routes, `TwissDispersionAnalysis`, `analyze`, option schema, receipts, configuration report, validator block and tree guard, effectiveness contract, snapshot, and the rewording of every placeholder-only statement; (5) `feat(elements)` declaration on the derived `Symplectic6DMap`-only kind set plus `:line`, two tripwires, snapshot; (6) `feat(contracts)` identity contract, validation script with README section, executable example with catalogue and runner entries; (7) `refactor(validation)` `lattice_cells.jl` derives its Jacobian from the helper (optional); (8) external benchmarks, each a validation script with stated tolerances and a history record: MAD-X `twiss` on uncoupled and coupled cells (Edwards–Teng `r11..r22`, `betx/bety`, dispersion) via the map export used in the PTC reference machinery, including the rolled equal-tune FODO where MAD-X reports a false instability; PTC `ptc_twiss` coupled Ripken functions and dispersion in the `(s-l, delta)` convention; Xsuite `twiss` W-matrix normalizer and (X2) dispersion readout after the longitudinal-coordinate conversion of theory Section 9.5; (9) post-campaign neighbour audit. Open after that: transport (Section 10), scan continuation (8.8), supplied-covariance mode selection, a public closed-orbit finder. **Stage 1 landed 2026-09-11** (record: [campaign history](history/twiss_dispersion_analysis_history.md), stage 1 section): symplectic kernel (`src/analysis/symplectic_linear_algebra.jl`), `Determined`/`AmbiguitySet` and the pinned status/reason/kind vocabularies (`src/analysis/Analysis.jl`), `one_turn_matrix` with the complex-step, finite-difference and ForwardDiff tags and the extension method (`src/analysis/one_turn_matrix.jl`, `ext/OctopusForwardDiffRules.jl`), both suite Jacobian closures switched to the helper with the inline closures kept as bit-identity witnesses; registry snapshot unchanged; no `analyze`, no analysis type. Measured defaults proposed in the record (rule: accepted ratio below one tenth, rejected above ten): `symplectic_rtol` for exact provenance 64 eps = 1.4e-14 (window [1.9e-15, 5.8e-14]; accepted max 0.013, rejected min 41), row-scaled verdict 1 (accepted max 0.108, an 8% miss of the one-tenth rule, recorded), `closed_orbit_atol` roundoff-derived 64 eps max(1, ||M~||_F) in scaled coordinates (accepted max 1.7e-3, rejected min 5.8e6), FD input keeps the explicit-tolerance requirement with `10 (eps/(2 step) + step^2)` recommended. Standalone counts: kernel 1618, helper 134 (ForwardDiff active), suite extract 1876 with CUDA active, tripwires 32; 29 injected defects each shown red. No lane and no gate yet: one full gate on the assembled batch before the push, to be recorded in the campaign history. Carried to stage 4 from stage 1: the two-arm FD map-uncertainty formula `max(step^2, eps/(2 step)) ||M||` (measured within 0.08 .. 4.7 of the FD error against 0.84 .. 1430 for the design's one-arm formula), the stale "solenoid under complex step" wording (it complex-steps cleanly), and a transverse-only fixed-point test for coasting maps at delta != 0 (the z slip is physics, -3.9e-5 per turn at delta 1e-4 on the DBA). |
| Strong-beam `momentum_dispersion` is a slope, not canonical η | **lead (opened 2026-09-11)** | `src/elements/strong_beam.jl` builds the strong-beam covariance as `w = w_beta + crab_dispersion*z + momentum_dispersion*pz`, i.e. the slope `η/h` of the physical longitudinal graph (theory (D7)), which equals the canonical `η` of (D8) only when `ζᵀS₄η = 0`. Not a defect for the pure-momentum and pure-crab limits Octopus uses today. Once `TwissDispersionAnalysis` lands it is the canonical source; decide then whether the strong-beam parameter is documented as a slope or converted. Found during the 2026-09-11 design review. |
| Multi-process mixed execution policy (MPI x CPUThreads) | **open (2026-09-06, steps 1-4h closed and the neighbour audit run; the ranks > slices regime and the priced levers remain)** | **Step 2 closed 2026-09-04** (design: [`design/multi_process_policy.md`](design/multi_process_policy.md); environment decision: [`history/mpi_environment_2026_09_04.md`](history/mpi_environment_2026_09_04.md)): `MultiProcessExecutionPolicy(; threads, ranks)` composes the CPU policy; the resolved state is a SLOT on `ResolvedCPUExecutionPolicy`, not a wrapper (a wrapper needs unwrapping at every positional receiver -- the public `track!` entry among them -- and one miss is a MethodError or a silently different path). Resolution is pure; activation opens the communicator, reads nranks/rank FROM it, and records `:multi_process_communicator`. Six collectives (`_mp_allsum!`, `_mp_lane_fold!`, `_mp_allminmax`, `_mp_bcast!`/`_mp_bcast`, `_mp_barrier`, plus `_mp_nranks`/`_mp_rank`/`_mp_is_root`) with a serial passthrough in core and `ext/OctopusMPIExt.jl` for MPI; every float reduction is Allgather + rank-ordered fold, never `MPI_SUM`. Measured: the seam runs at 1/2/4 ranks under MPICH_jll's mpiexec, and a one-rank MPI run reproduces `CPUThreadsExecutionPolicy` coordinate for coordinate (the Phase 0 requirement, with the extension actually loaded). No task shards yet, so `execute!` REFUSES at nranks > 1 naming step 3, and the refusal is pinned so its removal is visible; a launcher that starts P > 1 ranks throws under the MP policy and warns once under an ordinary one. **Step 3a closed 2026-09-04** (record: [`history/multi_process_step3a_2026_09_04.md`](history/multi_process_step3a_2026_09_04.md)): a `TrackingTask` -- the GENERAL one; there is no separate weak-strong task -- now runs divided across ranks. Shard rule: a contiguous run of whole `_REDUCTION_CHUNKS`, so the chunk-ordered folds extend across processes unchanged; rank counts that do not divide 64 are rejected. The shard is derived at a run's entry (sum the ranks' counts, re-derive, verify the local count) rather than stored. `TrackingContext` carries an `index_offset` so the counter RNG keys on the GLOBAL particle index; `Beam(n_global, MultiProcessExecutionPolicy(), ...)` draws the whole beam and keeps this rank's slice, because standardization is a whole-array mean/variance and a per-rank draw is a different beam. The strong-beam luminosity fold crosses ranks through one all-sum of 64 numbers. Measured under mpiexec with radiation excitation ON: tracking and luminosity bitwise identical to the single-process run at 1, 2 and 4 ranks. CPU/MPI/CUDA consistency is stated in the design note: CPU==MPI bitwise, CPU~CUDA at the contract's tolerance, MPI~CUDA by composition (not measurable -- the policy is CPU-storage only). Scaling measured 2026-09-04 (record: [`history/multi_process_tracking_scaling_2026_09_04.md`](history/multi_process_tracking_scaling_2026_09_04.md)): at the production point, 64 ranks x 1 thread is 46.1x over 1x1 against 23.9x for 1 rank x 64 threads -- processes beat threads 1.9x at the same width, because thread scaling caps near 16/process (memory bandwidth, the 2026-08-09 campaign) while ranks do not share that ceiling. Best measured 64 ranks x 2 threads = 61.9x, 2.6x better than any single-process configuration; `-bind-to core` costs nothing and cuts the fastest/slowest rank spread from 35% to 5%; `-bind-to hwthread` is 2.3x slower (it oversubscribes a multi-threaded rank). Per-turn communication is NIL (one integer all-sum per execute!, none per turn), so no code optimization followed -- the win is configuration. Rank count is capped at 64 by the shard rule (P | 64); a 128-rank launch was refused correctly. **Step 3b closed 2026-09-04** (record: [`history/multi_process_step3b_2026_09_04.md`](history/multi_process_step3b_2026_09_04.md)): the diagnostics that reduce the beam to SCALARS divide -- moment observers, BPMs, and the loss counts -- and rank 0 owns the one output file (every rank still runs every observer, because the reductions are collectives). The ledger's open decision is ANSWERED and it went against the attractive option: folding these on the fixed chunk grid buys cross-rank BITWISE agreement, was implemented, and broke "a lost particle is excluded from every reduction, exactly" (the masked row stopped equalling the survivors-only row; recorded means moved up to 206 ulps), because a chunk grid partitions SLOTS and a masked beam has more slots than survivors. So the local accumulation is unchanged -- one rank's numbers do not move at all -- and the ranks' partials fold in rank order, agreeing with an undivided run to 1.7e-14 at 2 ranks and 8.7e-14 at 4, which is the campaign's own parity-tolerance posture. Measured under a launcher at 1/2/4 ranks: 3 moment rows in the file at every count (the proof only rank 0 wrote), loss counts identical (256 particles, 4 dead). **Step 3c closed 2026-09-04** (record: [`history/multi_process_step3c_2026_09_04.md`](history/multi_process_step3c_2026_09_04.md)): per-particle output divides. The seam gains a seventh collective, `_mp_gather_rows`, which collects rows onto rank 0 in rank order; aperture loss rows and coordinate snapshots carry the GLOBAL particle id (the aperture's slot table is indexed by `particle_id - ctx.index_offset`, the file gets the id back). It also FIXED A DEFECT IN 3b: the post-run accounting in `execute!` ran outside the `_with_execution_policy` block, where a collective sees a communicator of one and silently does nothing -- so 3b's loss-summary reduction was never reached by `execute!`, and the gather first returned 42/17/9 rows at 1/2/4 ranks, each rank 0's shard alone. 3b's test called the function inside a scope and passed while the route to it had none. Measured after the fix: 42 loss rows, identical ids, identical snapshot ids and an identical in-file summary (256 particles, 42 dead) at 1, 2 and 4 ranks. **Step 4a closed 2026-09-04** (record: [`history/multi_process_step4a_2026_09_04.md`](history/multi_process_step4a_2026_09_04.md)): the soft-Gaussian `collide!` divides. Five reductions span the ranks -- longitudinal statistics (lane fold carrying the shard offset), the HISTOGRAM the equal-area boundaries are cut from, each slice's transverse moments (with the globally-first member as the shared shift origin, found by a min over global index then summed from its owner), each slice's weight/centroid, and the luminosity (once per collide, not per pair). Slice boundaries and weights are BIT-IDENTICAL across rank counts, which they must be -- they decide slice membership, so a disagreement is a different collision -- and the collide's aggregates agree to 5e-16 at 2 and 4 ranks. The measurement found a defect the statistics alone hid: the equal-area boundaries come from a histogram, not from the (already global) statistics, and each rank was binning its own shard -- luminosity was 2%/11% out at 2/4 ranks and whole slices were empty. `:equal_count` REFUSES (a global sort, not a fold). Scaling measured 2026-09-04 (record: [`history/multi_process_strongstrong_scaling_2026_09_04.md`](history/multi_process_strongstrong_scaling_2026_09_04.md)): at the production point the soft-Gaussian collide runs 3.4587 s serial, 0.2950 s at 16 ranks x 1 thread (**11.72x**), against 2.15x for the best threaded configuration -- which SETTLES the "gaussian collide flatline" rider below: the flatline is the THREAD axis (caps at 2.15x, turns over by 16), and Phase 0 saw no rank scaling because every rank ran 16 threads, already past that turnover. Luminosity agrees to 3e-16 at every rank count. The curve turns over past 16 ranks for a counted reason: one collide issues 1874 collectives (1388 allsum, 452 allminmax, 34 lane_fold) of which 1840 average 33 bytes, and every float reduction is an Allgather + rank-ordered fold costing O(P), so per-collective cost rises with P while compute falls as 1/P. **Optimized 2026-09-04, turnover GONE:** `_run_logical_workers` runs its grid inline when the pool holds one thread (it spawned ~57,600 tasks per collide at one thread for no parallelism), and the lane folds exchange the folded SCALAR rather than 4096 lanes (the fold's shape is already broken by the split within a lane, so it bought nothing at 4096x the bytes). Neither moves a number -- same digest, same luminosity. Re-measured: 3.3891 s serial, 0.1028 s at 64 ranks = **32.97x, monotone**, and the run-to-run spread collapsed (1760% -> 153% at 64 ranks). Traffic fell 1,175,656 -> 61,816 bytes per collide, 19x, with the call count unchanged. **Remaining headroom, measured two ways (seam latency timed directly; size scaling at fixed ranks, the fit predicting the half-size point to 1%):** at 64 ranks the 0.1009 s collide is 52% ideal compute, 18% per-rank inefficiency, 30% collectives. The 1874 CALLS are the target and `collision_pair_batches` is the lever -- the CPU soft-Gaussian is the ONLY collide path in the tree still strictly sequential (the CUDA soft-Gaussian, PIC on both backends, gpic and spectral all batch; `GaussianPoissonSolver` already defaults to `batch_mode=:wavefront` and only its CPU method ignores the field), and PIC's recorded argument (batching preserves each slice's own collision order, so the batched path reproduces the sequential result BIT FOR BIT; the exception is the mesh mode soft-Gaussian does not have) applies. Hoisting what is fixed for a collide -> ~970 calls, merging the two beams per pair -> ~520, wavefront batching (~29 batches) -> ~130; that projects 0.0731 s = **46x from 34x**, and more at higher P. **The two beams are a second, independent axis (owner-raised 2026-09-04), and it attacks the 18% per-rank inefficiency rather than the 30% collectives:** inside `_cpu_gaussian_slice_pair!` the two moment reductions read different beams and neither reads the other's result, and the two `_slice_slice_gaussian_kick!` calls write disjoint arrays (`rep1` vs `rep2`) from moments both computed BEFORE either kick -- so beam 1 and beam 2 are independent within a pair and are currently done one after the other, each over its own 64-chunk grid. A shard's slice is SMALL (a 15-slice, 64-rank run leaves ~1/960 of the beam per rank per slice), so a 64-chunk grid over it is thin and the per-worker cost is mostly grid overhead; issuing both beams as one grid of 128 doubles the width where it is thinnest, and the merged moment buffer is the same edit that turns two all-sums into one. The bit-identity constraint is that the two beams' luminosity accumulators must stay SEPARATE and each fold stay in chunk order -- widen the grid, never merge the folds. The CUDA wavefront route is the existence proof that the shape works: it already carries both beams in one array dimension (`2 * max_batch` columns) and reproduces the sequential result. **Owner constraint (2026-09-04): REUSE the existing option keywords, invent none.** The CPU soft-Gaussian batching switch is the `batch_mode` field `GaussianPoissonSolver` ALREADY carries (`:sequential`/`:wavefront`, defaulting to `:wavefront`) -- the CPU method just does not read it today, which is why `interface.jl` documents `batch_mode` as CUDA-only. That documented scope is ALSO already inaccurate and must be corrected by the same change: it claims "the CPU paths always use collision-time order", but CPU PIC and CPU gpic do batch -- by their own `_pic_batchable` rule (`!_pic_source_slice_grid`), not by `batch_mode`. So the landing shape is one keyword with one meaning on every solver and both backends, and the schema entry gains a CPU consumer beside its CUDA one. Then a deterministic reduction tree (Phase 0's "past ~16 ranks"; worth ~3x per message at 64 ranks, since latency rises 1.4 -> 18.6 us from 2 -> 64 ranks) and non-blocking overlap (Phase 0's "interleaving"). **Will not help:** a communicator per slice -- the shard is contiguous in particle index so every rank holds members of every slice; it needs a longitudinally aligned decomposition, which conflicts with the chunk-aligned bit-identity and needs re-partitioning every turn. **CPU wavefront LANDED 2026-09-04** (record: [`history/soft_gaussian_wavefront_cpu_2026_09_04.md`](history/soft_gaussian_wavefront_cpu_2026_09_04.md)): the CPU soft-Gaussian reads `batch_mode` -- the keyword the solver already had, per the owner's reuse-the-keywords constraint -- and `supported_backends` now names both backends. Three edits: `collision_pair_batches` groups the 225 pairs into 29 conflict-free batches; the two things about a slice that are NOT coordinates (its global count, and WHICH particle is its shift origin) hoist out of the pair loop entirely, since slice membership is fixed once the beams are sliced; and each batch exchanges its slices' origins and shifted sums in one message per beam. The kicks go out as one grid per batch, both beams together -- the owner's second lever, honest scope: at one thread per rank `_run_logical_workers` runs the grid inline, so that width helps the threaded and single-process cases, not the 64x1 configuration. Measured: **1874 collectives -> 222** (8.4x; allsum 1422->190, allminmax 452->32), and at 64 ranks the collide's floor **0.0974 -> 0.0755 s = 1.29x**, 34.9x -> 45.0x over the same evening's 3.3947 s serial, with the non-compute term halved (0.0444 -> 0.0225 against 0.0530 ideal). At 16 ranks it is worth 7%. Bit-identity holds both ways and is pinned both ways: `:sequential` reproduces the pre-change tree at 1/4/8 threads and 1/2/4 ranks, coupled and uncoupled, and `:wavefront` equals `:sequential` to the bit -- the suite compares them on one process (asserting on the RECORDED batch counts, so a path that never batched cannot pass) and the multi-rank child compares them at every rank count. **A 1-ulp trap was found and closed on the way:** `muladd` is "fuse if profitable", so splitting the moment finalize into its own function flipped `_shifted_second_moment` from fused to unfused and moved a coupled collide by 1 ulp with the raw sums bit-identical; 400 of 400 sampled moments in the pre-change tree were FUSED, so the two helpers now say `fma` and the arithmetic is pinned rather than left to the inliner. Every remaining `muladd` in the tree is the same coin. Remaining call-count headroom: the slicing's ~70 per-statistic scalar all-sums vectorize the same way; `_mp_slice_reference` still discards a maximum (30 wasted messages after the hoist, hygiene not speed); `_mp_allsum_impl!` allocates its gather buffer per call. **Step 4b closed 2026-09-05** (record: [`history/multi_process_step4b_2026_09_05.md`](history/multi_process_step4b_2026_09_05.md)): `StrongStrongTask` runs divided. The work was the run holding TWO beams: the shard scope held one `(offset, global_n)` and handed the second beam the first beam's offset, so it now holds one entry per beam keyed by the identity of its `Phase6DRep` (count-keyed lookup is ambiguous in principle -- 256 vs 257 particles give rank 1 of 2 the same 128-particle shard -- and the count form now throws on that case); every reader moved to the representation form; each line tracks under its own beam's `index_offset`; the whole run sits inside one policy scope. The blanket refusal is replaced by per-solver refusals (PIC, gpic, spectral refuse at the task preflight AND at their CPU collide entries until step 4c) and a line-action refusal. Measured under a launcher on two beams of different sizes with radiating lines, line observers and the artifact: the luminosity series is BIT-IDENTICAL at 1, 2 and 4 ranks, fingerprints agree to 2.1e-16 and moment rows to 2.1e-14, only rank 0 wrote, and the one-rank run reproduces `CPUThreadsExecutionPolicy` bit for bit (pinned in-process and through the launcher). (The `batch_mode`-across-solvers row the owner put ahead of 4b closed 2026-09-04 -- record: [`history/batch_mode_one_keyword_2026_09_04.md`](history/batch_mode_one_keyword_2026_09_04.md); every solver reads the keyword on CPU and records a pair-schedule receipt, which 4c's per-solver division can assert on.) **Step 4c, PIC, closed 2026-09-05** (record: [`history/multi_process_step4c_pic_2026_09_05.md`](history/multi_process_step4c_pic_2026_09_05.md)): the PIC collide divides -- each rank deposits its own particles, the padded charge grid is all-summed before every field solve (two planes per direction, three under quadratic interpolation or node mode), the luminosity's two deposits likewise, mesh extents all-reduced so grids and the Green cache are identical per rank, the `:sigma` origin the globally-first member, every skip and every non-finite chokepoint decided from global counts. The defect the first divided run found: `_pic_kbb1` divided by the SHARD's macroparticle count (kicks 1.9x/3.5x at 2/4 ranks). Measured under a launcher at 1/2/4 ranks: bit for bit at one rank on every option route, 7.1e-15 worst across rank counts on the option routes (8.8e-14 on a 64-slice, 4096-pair arm), 72 grid all-sums recorded per two-turn run. At more than one rank the pairs run one at a time on the main thread (`:funneled`); the batched per-wavefront exchange is the performance phase. **The CPU/MPI/CUDA consistency contract for PIC landed 2026-09-05** (same record): `StrongStrongPICMultiProcessConsistencyContract` runs the PIC backend contract's beams and task under the CPU policy in-process, launches `src/contracts/mpi_pic_consistency_child.jl` at each rank count (the child draws the same beams under the multi-process policy, runs the task divided, gathers onto rank 0), compares one rank bitwise and more ranks at rtol 1e-12, and with `cuda=true` runs the CPU/CUDA PIC contract on the same beams and reports the MPI-to-CUDA triangle bound. Measured: bit for bit at one rank; 5.5e-17 abs on the coordinates (2.3e-15 against the beam scale, 0.18 of a 1e-12 pointwise allowance, so the default rtol is 1e-11) and 1.6e-15 rel on the luminosity at two ranks, 5.8e-17 / 1.1e-15 at four; the CUDA leg 5.1e-17 / 9.6e-16, MPI-to-CUDA luminosity bound 2.6e-15. No launcher -> `:skipped` naming the unrun legs; a requested CUDA leg the device cannot run -> `:skipped`, not a pass on two legs. Required by `StrongStrongTask`; the launcher lane asserts `:passed` at 1 and 2 ranks. **Performance phase closed 2026-09-05** (record: [`history/multi_process_pic_scaling_2026_09_05.md`](history/multi_process_pic_scaling_2026_09_05.md)): `pic_cpu_divided.jl` exchanges once per wavefront batch for every pair and both directions and solves every plane ONCE: the nx x ny interiors of the deposits reduce-scatter by whole planes to owner ranks that solve them, the potentials all-gather, every rank takes the gradient -- seven collectives per batch (eight under `:sigma`) for ~24 per pair, none inside a pair, so the pairs run on the worker pool again, the FFT work divided by the rank count and the traffic a third of the all-summed padded planes' -- and the extension's all-sum above 2048 elements is a reduce-scatter (Alltoall + rank-ordered fold + Allgather: 2n per rank instead of nP, a 512 KB plane 6.3 -> 0.25 ms at two ranks). Both are BIT FOR BIT the per-pair exchange, pinned two ways: every two-rank result line of the launcher child byte-identical before and after, and a `:sequential` arm held to the batched default's bits on every gate. The node mesh keeps the per-pair exchange (its three prebuilt planes want their own staging); `:source_slice` never batches. Measured (`profiling/benchmark_collide_cpu.jl`, one thread per rank, cores bound): at the fixed point (640k/256k) 2.47 s serial -> 0.73-0.82 s at 16 ranks (3.0-3.4x), 1.99 s at 64; at the production point (2.56M/1.024M) 6.27 s -> **1.07 s at 16 ranks (5.9x)**, 1.57 at 32, 2.52 at 64 -- the collectives are 41% of the wall at 16 ranks and 84% at 64, because every rank holds a chunk of every slice and must send its partial of every plane and receive every potential (~115 MB each way per collide whatever the rank count); the Alltoall behind the reduce-scatter is the dear one (23 ms per call at 64 ranks, three times the all-gather per byte). Hybrid rank x thread points (16x4, 8x8, 32x2) lose to 16x1. The first cut of this phase (all-summed padded planes, redundant solves) did not scale at all and is recorded. Next levers, measured in the record: overlap the luminosity's exchanges under the next batch (a third of the collective time at 64 ranks), a hand-written exchange for the reduce-scatter, and -- the only way past the per-rank traffic floor -- a longitudinally aligned decomposition, which is an owner decision against the chunk-aligned bit-identity. **Step 4d, the slice-aligned collide, closed 2026-09-05** (record: [`history/multi_process_step4d_sliced_2026_09_05.md`](history/multi_process_step4d_sliced_2026_09_05.md); design reviewed adversarially before it was built, 20 confirmed findings folded in): the owner lifted the chunk-aligned bit-identity for the collide (parity class against serial suffices), so `pic_cpu_sliced.jl` lays the live particles out BY SLICE for one collide -- whole slices per rank below `P <= nslices`, a proportional group of ranks per slice above, migrated in and back by an all-to-all of columns with no index travelling -- and every pair is point-to-point between its two groups and its owners (records to a coordinator, grids from the direction owners after their own Green cache, partial interiors to the owners, potentials to the field members, the luminosity through the coordinator: eight hops per batch, no collective inside a pair). Tracking and everything outside the collide keep the home layout; the collide never writes `z`, so `z` returning bit for bit is the migration's own check, printed per rank. At ONE rank the same code runs with self-delivery and is the CPU collide bit for bit on fourteen option arms (the pin that found the `:sigma` origin defect: an origin is a moving particle). At 2 and 4 ranks every arm is in the parity class (<= 6e-15 on the option arms, 7-9e-14 on the 64-slice arm) and `:sequential` equals `:wavefront` bit for bit. The 4c batched exchange is retired. Measured (`profiling/benchmark_collide_cpu.jl`, one thread per rank, cores bound): production point 6.71 s serial -> **0.88 s at 32 ranks (7.6x)** and 0.90 at 64 (4c: 1.57 and 2.52), fixed point 2.61 -> 0.47 at 16 ranks (5.5x; 32x2 threads 0.52); the curve no longer turns over. At sixteen ranks -- one slice per rank -- production is SLOWER than 4c (1.5-3.1 s against 1.07): a batch's critical path there is one whole slice's deposits and kicks on one rank while its peers wait, and the wavefront's duty cycle idles half the ranks on the average batch; the clocks (every wait named by stage) put 72% of the 64-rank wall in waits, mostly the owners on the coordinator's fold and the tail skew at the end-of-collide all-sums. Re-measured at sixteen ranks with five timed collides: spread over the sockets (`-bind-to socket -map-by socket`) 0.72-1.13 s, level with 4c; core-bound 1.16-1.66; and in every configuration the first timed collide after the two warm ones is 2-3x slower (open item: two warm collides are not enough for this path, cause not yet identified). The contract passes at 1, 2, 4 and 8 ranks (bitwise at one, 5.8e-17 abs / 0.12 of the pointwise allowance / 1.9e-15 luminosity beyond). Next levers, priced in the record: dataflow across batches (a rank starts its next pair when its own inputs are ready; the point-to-point form was built for it -- ~2x at P ~ nslices, removes the 16-rank regression), a data-size rule that lets a whole slice's deposit thread, and cached migration buffers. **Step 4e, dataflow across batches, closed 2026-09-05** (record: [`history/multi_process_step4e_dataflow_2026_09_05.md`](history/multi_process_step4e_dataflow_2026_09_05.md); design reviewed adversarially first, its blocker and must-fixes folded in): the wavefront batch is no longer a barrier. Each rank runs an event loop over its pairs in the collision order, running every stage whose receives have arrived and whose gate is open (relay duties first) and blocking on the union only when a whole scan runs nothing. The gate is read from `order`, never from indices -- the collision time is `-(c1+c2)/2` ascending, so the pair before `(i,j)` on slice `i` is `(i,j+1)`, and the review caught the design gating on a pair that had not run. A buffer belongs to its send until MPI says the send completed; the `:sigma` origins moved into per-pair buffers with codes inside the pair's tag space (their own band collided with the pair band a few hundred pairs in) and the collide checks its largest tag against the communicator's limit; a pair meeting a non-finite extent releases its slices without kicking, so its successors run and every rank throws. Both loops now call the same leaves, so the arithmetic lives in one place. **Bit for bit against the loop it replaces:** the launcher child's every result line on d677931 against this tree at 1, 2 and 4 ranks, zero differing lines; permanently, `:sequential` (batched loop) equals the default bit for bit, an in-process pin forces both loops on the same beams, the `:skewed` and `:sparse` arms run twice, and every arm names the loop it ran. **Which loop is a measured rule, not a preference:** an interleaved A/B of the two on one box puts dataflow 1.4x ahead at 64 ranks, level at 32 and 2x BEHIND at 16 -- a slice's pairs are a chain, so a rank holding a WHOLE slice has nothing to overlap and the loop only adds a wake per hop, while a rank holding a fraction of one has its group's other pairs and its owned planes to get on with. So groups wider than one rank run the dataflow loop and whole slices per rank the batched one. **The warm-up closed 2026-09-05** (record: [`history/multi_process_pic_allocations_2026_09_05.md`](history/multi_process_pic_allocations_2026_09_05.md)): one allocation profile named the whole of it in a single run -- the one-rank passthrough copying every send (250-290 MiB), the virtual positions rebuilt per pair (86-94), the migration rebuilding its columns, permutations and slice states (122-171). All three are pooled or held by reference now, and the collide fell from **0.149 GiB per rank per collide to 0.004-0.013**, with the first timed collide now within noise of the last where it had run 2-4x slower. It also shrank the schedule gap 4e had measured: re-run afterwards, the two loops are within a few percent to a quarter of each other (medians) and tied on best-of-six above sixteen ranks, so 4e's layout rule stands on a much smaller margin and its record carries the correction. **Step 4f, Gaussian-PIC, closed 2026-09-05** (record: [`history/multi_process_step4f_gpic_2026_09_05.md`](history/multi_process_step4f_gpic_2026_09_05.md)): the second solver on the slice-aligned transport. The transport grew one seam, dispatched on the solver -- its record's length, whether it needs the slice's first member at every pair, and four leaves (record, owner mesh, plane solve, kick) -- and everything else is shared; the refactor is bit-neutral for PIC and that is CHECKED, the child's PIC lines byte-identical to d677931's at 1, 2 and 4 ranks. gpic contributes three things, each the SLICE's: fourteen shifted sums per member about the slice's globally-first member, folded in group rank order (and so gpic needs the origin exchange at every pair, where PIC needs it only under `:sigma`); the Gaussian subtraction ON THE PLANE'S OWNER after the fold, because with `neutralize=true` the amplitude is the deposited grid's total and a per-rank residual is not defined; and the control-variate mode, decided once on the owner and carried in the grids message with the moments, the global count and the boundary drifts, because the field members need the source slice's moments for the analytic add-back and are not in the source group. Measured: bit for bit at one rank on all five option routes, 4e-16 to 1.4e-15 at two and four ranks, the same parity class as PIC. **Step 4g, spectral, closed 2026-09-05** (record: [`history/multi_process_step4g_spectral_2026_09_05.md`](history/multi_process_step4g_spectral_2026_09_05.md)): the last solver, and the one with TWO collides, which divide by different means. The 6D map (`longitudinal_kick=true`, the default and the production route) is order-DEPENDENT -- a slice is kicked by the pairs before it and then serves as a source at its kicked positions -- so it takes the slice-aligned transport after all (`spectral_sliced.jl`); the earlier reading that spectral is "order-free" was taken from the TRANSVERSE route alone and every clause of it is false of the default one. Spectral negotiates almost nothing (ONE Dirichlet box for the whole collide), so stages 0 and 1 of the PIC loop do not exist and the solver of a directed interaction is the FIELD slice's group head rather than a dealt third party: the source group sends its two drifted deposits, the head folds and solves, and the potentials never leave the group that needs them -- two messages per direction per pair at `P <= nslices`, no broadcast. Dealing the solve out is the point: one DST solve is ~4x the mesh evaluation it feeds (271 us against 64 us at 64x64, 6000 particles), so redundant solving would cap the speed-up near 1.2x. The transverse map stays on the HOME layout (order-free, no migration): one all-sum for every source slice's plane, a second for the dealt mesh solves, one all-max plus one all-sum per group of field slices for the luminosity, and the kick never leaves a rank. Global scalars both routes needed: the box's rms COUNT and extrema (the non-finite verdict agreed as an integer count FIRST, because a NaN into `_mp_allminmax` is rank-divergent), and `_spectral_luminosity_scale`, which divided by `length(beam.rep)` -- the 4c shard-count defect, live in spectral until now. Measured: bit for bit at one rank on both routes and both methods, ~1e-15 at two and four ranks; 6D scaling at 90k/beam, 15 slices, 64x64, best of five on a shared box: 451 ms undivided, 497/372/230/130/74 ms at 1/2/4/8/16 ranks (6.7x), turning back up to 227 ms at 32 ranks -- more ranks than slices, its own row below. `_reject_undivided_solver` now has nothing in the roster to refuse and became a DENY-BY-DEFAULT tripwire (`_solver_divides`: `false` for `AbstractPoissonSolver`, `true` only where someone divided the collide and said so). **Step 4g's second half closed 2026-09-06** (same record): the 32-rank turn-up was a CEILING with a named cause, fixed rather than recorded. Every step of the collide splits by particle across a slice's group except the DST solve -- one indivisible FFT per drifted plane, ~70% of the work -- and it was pinned to the field slice's group HEAD, so the number of ranks that could solve was the number of SLICES (and, both beams' layouts being built by the same rule from near-identical counts, the two directions' heads are the SAME ranks, not two sets). Measured from each rank's own exchange receipt: 20 of 32 and 23 of 64 ranks solving, the busiest carrying 60 planes at both counts, the 32-rank distribution `60,0,60,0,...` because `first(group)` is always the even rank. Fix: DEAL a pair's four solves (two directions x two drifted planes) across the field group, one solver per (direction, plane), offset by the pair's collision position -- a group of one gives that member both planes, so the `P <= nslices` regime is unchanged message for message and bit for bit. After: 32 of 32 and 64 of 64 solving, busiest 60 planes -> 20. `4*min(n1,n2)` is the hard ceiling (a wavefront batch holds at most `min(n1,n2)` pairs), so `nslices` is the knob that raises it -- which is why tags moved from the pair's index in the COLLIDE to its position in the BATCH (a batch is a barrier; the old keying wanted `ns1*ns2*16` tags against the 32767 MPI guarantees, capping slices near 45). Also: five barriers a batch down to three, evaluation straight from the received payload, and a counts-only helper in place of `_divided_slice_plan` (whose `owns_reference` cost 2 Allreduces per slice for a field spectral never reads). SEPARATE and larger lever, in no MPI code: a DST-I over N interior points has logical size 2(N+1) and FFTW is fast only when that is smooth, so `grid=(256,256)` costs 8.8x `grid=(255,255)` and every power-of-two width is one of the slow ones (2(2^k+1) is twice a Fermat-ish number); `_spectral_note_grid_size` now records and warns from 48 points up. **Step 4h closed 2026-09-06** (same record): the dataflow loop for spectral -- an event loop over the pair dependency graph in place of the batched loop's per-batch barriers, PIC's step 4e applied to spectral's four stages with PIC's gate unchanged. Two things are spectral's own: its tags must separate every pair of the COLLIDE (a batch is a barrier, an event loop is not), and a pair whose luminosity mesh is degenerate still KICKS and skips only its luminosity, because spectral's verdict arrives after the kick where PIC's arrives before. The two loops are held to the same BITS in process at one rank and under the launcher at 1/2/4 on every 6D arm. Measured rule (refs a task holds, arms interleaved in one process, 90k/beam, 15 slices, 63x63): batched wins at 1/2/4/8 ranks (379.4/286.1/179.0/100.0 ms against 387.7/304.1/191.9/113.7), dataflow wins once a slice spans a group -- 55.6 against 69.6 at 16 ranks (25%), 37.4 against 38.2 at 32, 39.0 against 40.8 at 64. Groups wider than one rank get the dataflow loop, which is 4e's rule for PIC arrived at independently. **TWO MEASUREMENTS WITHDRAWN**: step 4g's rank scan went through the bare `collide!` (fresh buffer refs per call, where a task holds them across turns -- 209 ms at 64 ranks against 40.8 for the identical collide) and ran whole-ladder-then-whole-ladder on a loaded box. The claims that the curve turns up past 32 ranks and that the ceiling tracks the slice count do not survive: clean numbers flatten near 32 ranks (10.1x at 32, 9.7x at 64 with 15 slices) and 30 slices give 8.5x at 64 rather than more, so `4*min(n1,n2)` is a real bound on solve concurrency but is not what binds this size. The ceiling evidence with no clock in it stands (20/32 and 23/64 ranks solving before the deal, 32/32 and 64/64 after). **Flattening ATTRIBUTED 2026-09-06** (same record, design note "Where the flattening near thirty-two ranks goes"): read from `_mp_collective_times`, one collide, 90k/beam, 15 slices, 63x63, best of five with a barrier before each. Local compute stops scaling and the SOLVE is why -- it falls 1.59x from 16 to 32 ranks and only 1.31x from 32 to 64, because a pair's four solves are dealt across the field slice's group and a group can use at most TWO of its members per direction however wide it is (groups are 2-3 wide at 32 ranks and 4-5 at 64; the busiest rank carries 30 planes at 32 and 20 at 64). So `2*(number of groups)` bounds the ranks that can solve and is tighter than `4*min(n1,n2)`. The MIGRATION is flat -- four all-to-alls a collide, 14.8 ms at 16 ranks and 8.6 at 64, the largest single MPI term there at 22% of the collide, latency-bound (1.2 KB to each of 64 peers = 4096 tiny messages an exchange). The rest is ranks waiting: local work is 38% of the wall at 16 ranks and 25% at 64. FIXED on the way: `longitudinal_slices` took two collectives PER SLICE (72 of the collide's 91 all-sums, paid by every divided solver); they are now one all-sum over a length-ns vector each, bit-identical, collide down to 35 -- worth under 1 ms, because those clocks were skew and not latency (the lesson is in `experiences.md`). **Merged migration and deal rotation, 2026-09-06** (same record): both structural, both measured, NEITHER moved the clock. `_pic_sliced_migrate_pair_in`/`_pic_sliced_migrate_pair_out!` carry both beams in one exchange per direction (eight rows -- six coordinates, the slice, the beam -- one `_mp_allsum!` over an `(ns1+ns2, P)` count matrix, receiver buckets by `(beam-1)*(maxns+1)+slice` with a stable sort), four exchanges a collide down to two, shared by PIC, gpic and spectral, bit-identical at 1 and 4 ranks: 15.11 -> 14.80 ms at 16 ranks, 9.65 -> 9.76 at 32. That REFUTES the latency reading of the flat migration -- the clock there is skew (min wait 1.09 ms, max 37.57, same collide at 16 ranks). And the owner deal did not rotate: `k % g` ran over the pair's index in the GLOBAL pair order, whose pairs are scattered across a slice, so some group members drew repeatedly and others never; `_spectral_slice_pair_positions` numbers each pair among the pairs on its own slice, cutting the 64-rank solve spread from 7-20 planes (2.9x) to 12-16 (1.33x) with the wall unchanged (41.2 against 39.5 ms). **The collide is CHAIN-BOUND** (same record): `2*nslices-1` batches run in sequence because the longitudinal map is order-dependent, and at 64 ranks the wall tracks the batch count, not the work -- 8 slices 27.1 ms, 15 slices 42.0, 30 slices 225.6, i.e. ~1.5-1.8 ms a batch of which ~0.35 is compute (work-bound would have predicted ~21 ms for 8 slices, chain-bound ~27; it measured 27.1). The luminosity is 13% of that wall (42.6 with, 37.1 without at 64 ranks), not the per-batch cost either. Shortening the chain means changing what the solver computes, so the sliced route is AT ITS CEILING as specified; what remains is per-batch (fewer barriers per batch, or overlapping sends with the next batch's local work, which the dataflow loop already takes as far as its gates allow). **Production benchmark recorded 2026-09-06** (record: [`history/production_benchmark_2026_09_06.md`](history/production_benchmark_2026_09_06.md)): all four solvers on the production strong-strong case plus weak-strong tracking, across CPU threads, MPI and CUDA, 200 turns averaged over turns 100-200. It exposed a REPRODUCIBILITY GAP: neither `test/examples` harness had a multi-process option, so the MPI arms ran through scratch copies carrying a six-line branch that wraps the CPU policy in `MultiProcessExecutionPolicy(threads = ...)`. **That gap is CLOSED 2026-09-06** (record: [`history/harness_mpi_branch_2026_09_06.md`](history/harness_mpi_branch_2026_09_06.md)): both harnesses take `OCTOPUS_USE_MPI=1`, with `OCTOPUS_CPU_THREADS` as the per-rank thread count and `OCTOPUS_RANKS` passed straight through as the policy's `ranks`. SPELLING: the shipped switch is `OCTOPUS_USE_MPI`, matching `OCTOPUS_USE_GPU`. The benchmark record still names `OCTOPUS_MP`, the spelling its scratch copies used, and it stays that way -- `docs/history/` is add-never-rewrite, owner-confirmed 2026-09-07. So both harnesses REFUSE `OCTOPUS_MP` by name (2026-09-07) rather than leaving it unread: an ignored variable would have let an operator following that record report a threads-only run as an MPI one, which is the silently-ignored-configuration defect rather than a typo. Pinned by `test/runtests.jl`, "The harnesses refuse the retired OCTOPUS_MP spelling", on presence at both `=1` and `=0`. The trap the branch had to solve: a package extension attaches only to a PACKAGE, and both harnesses `include` `src/Octopus.jl` into `Main`, so `Base.get_extension(Main.Octopus, :OctopusMPIExt)` is `nothing` under the default load (measured, both load orders) -- a branch bolted on as the files stood would have made every rank a communicator of one and `mpiexec -n 8` eight whole simulations on one artifact path, exit 0 with plausible timings. The switch therefore loads Octopus as a package and asserts the extension attached, and a pre-`include`d `Main.Octopus` is refused with the cause named rather than with Julia 1.12's `importing Octopus into Main conflicts with an existing global`. Three defects in the WEAK-STRONG twin were found while wiring it, all three fixed in the strong-strong harness by the 2026-08-05_b audit and never ported: the task carried no `policy` so `execute!` resolved a fresh default and the built policy reached only `Beam(...)` (U21-17 -- and fatal rather than cosmetic here, since an inferred default is a `CPUThreadsExecutionPolicy` and every rank would have tracked the whole beam silently); `OCTOPUS_USE_GPU` still used the pre-U21-16 `== "1"` grammar; and `OCTOPUS_CPU_THREADS` was not read at all. Measured: both harnesses byte-identical at their defaults before and after, and a one-rank MPI run identical to the threads-only run in every digit on both; two ranks divide under the launcher with one artifact. Pinned by `test/runtests.jl`, "The developer harnesses run divided under an MPI launcher", at `-n 2` with `OCTOPUS_RANKS=2` -- the explicit rank count is what stops the pin passing as a communicator of one. **NEXT:** give a wide group more than two solvers or split the DST, then the chain at P ~ nslices, the deposit's threading rule, the node mesh on the sliced layout, `FFTW.MEASURE` planning (a further 1.3-1.5x at these sizes, NOT taken because MEASURE picks its algorithm by timing and two workspaces in one pool could then get different plans, which the thread-count-invariance and bit-identity pins forbid), the transverse route's per-collide buffers (~18 MiB unpooled) and its mostly-zero `meshes` all-sum, and the campaign's neighbour audit. When the last solver divides, the campaign's targeted neighbour audit falls due -- deferred to that point by owner decision rather than run per step, and its accumulated blast radius is kept in the "Targeted neighbour audit of the multi-process campaign" row. Still refused at nranks>1: task and line ACTIONS only -- a callback Octopus cannot reason about. Every solver in the roster divides. **Superseded: step 3** (weak-strong sharding) -- it must choose the shard rule (chunk-aligned `P | 64` vs lane-aligned `P | 4096`; the design note lists what each buys), carry a global index offset for the counter RNG, replace lengths that enter physics with global counts, broadcast every decision that gates a collective, and give rank 0 the artifact. **Step 1 closed 2026-09-04** (record: [`history/weak_strong_allocation_2026_09_04.md`](history/weak_strong_allocation_2026_09_04.md)): the 1.282 GiB/turn was ONE site -- the sliced strong beam built a fresh mutable `ThinStrongBeam` per slice per particle, 7 x 192 B = 1344 B per particle per turn (the Phase 0 record's "1.25 KB" is the decimal reading of the same number), 97.5% of profiled bytes and all but the per-call constants of the line's allocation, on both loop copies (fused and elementwise luminosity). An isbits `ThinStrongBeamSlice` carrier removes it with the kick arithmetic untouched: production digest 0xbb09efa9e44e2017 unmoved at 1/4/8/16/32/64 threads, allocation 1.282 -> 0.000 GiB/turn and GC share 3-53% -> 0% at every thread count, s/turn 2.23 / 0.69 / 0.45 / 0.65 / 0.63 / 0.73 -> 2.09 / 0.58 / 0.28 / 0.17 / 0.11 / 0.085 at 1/4/8/16/32/64 threads (8.6x at 64; the 8-16-thread cap is gone and the curve is monotone to 64, same protocol, same box). Pinned: "CPU weak-strong strong-beam tracking allocation does not scale with the particle count" (growth 2.000 unfixed, 1.000 fixed) and a `GaussianStrongBeam` arm on the weak-strong thread-invariance pin; `profiling/benchmark_track_cpu.jl` carries the localisation mode (`OCTOPUS_BENCH_ALLOC_PROFILE=1`). The Phase 0 weak-strong P x T inputs are superseded by the after-curve in the record. NEXT: step 2 (policy type + collective seam), which starts by resolving the two environment traps below by test. Phase 0 record follows. Owner-directed. Scope: SINGLE NODE (owner decision 2026-08-19); the per-rank core is reused unchanged — the policy COMPOSES `CPUThreadsExecutionPolicy`, and 1-rank MPI must reproduce today's digests bit-for-bit. Phase 0 measured (record: [`history/multi_process_phase0_2026_08_19.md`](history/multi_process_phase0_2026_08_19.md); instruments: `profiling/benchmark_track_cpu.jl`, `profiling/mpi_collide_prototype.jl`): weak-strong tracking is allocation/GC-bound (1.282 GiB/turn at every thread count, GC 3.2%→30.8%→52.2% at 1/16/64 threads, optimum 16, digest thread-count-invariant) — the pre-fix-1 disease, never treated on this path; strong-strong PIC at 8 ranks x 8 threads socket-bound is **1.86x** over socket-bound 1x16 (2.23x over unbound; 41.0 → 2.6 → 1.15 s/turn lineage), socket binding alone is 17–22% free, and 2x32 loses to 2x16 at perfect locality (the 16-thread optimum is per-process). Transport decided: MPI.jl as weakdep extension over a ~6-function collective seam (serial passthrough in core); Distributed closed by measurement — the deterministic rank-ordered collective already costs 0.10–0.35 s/turn (O(P) fold) at 2–8 ranks over MPI shm, and Distributed's per-message overhead on 450 collectives/turn would eat most of a 2x gain. Decided order: (1) weak-strong allocation extraction, (2) policy + seam with communicator-read receipts, (3) weak-strong MPI (task-level seams only, rank-0 artifact), (4) strong-strong per solver (soft-Gaussian → PIC deposit→solve all-sum → gpic → spectral); determinism: fixed-P bit-repeatability pinned, cross-P at CPU/CUDA parity tolerance class, lane-aligned sharding where `_SLICE_FOLD_LANES` makes cross-P bit-invariance cheap. Two measured env traps gate Phase 1 (HDF5_jll flips to an absent mpi+openmpi artifact when MPIPreferences is visible in the load path; HDF5's own MPIPreferences dep then locks MPI.jl to MPICH_jll defaults — so the prototype ran MPICH_jll under hydra, bb Open MPI unreachable in-process; resolve by test: matching MPI HDF5_jll variant vs pinned serial HDF5 vs accepting MPICH_jll). Open riders: gaussian collide flatline at 2x16/4x16 (2.42/2.46/2.52 despite halved/quartered per-rank particles, then 2x at 8x8 — something per-pair and particle-count-independent dominates); gaussian comm phase 3–6x PIC's for identical code (suspected idle-thread spin vs the serial fold, unprobed); the O(P) fold wants interleaving now and a deterministic tree past ~16 ranks. |
| The MPI suite reads its receipts off a merged stdout, and it is lossy | **CLOSED 2026-09-06** | The multi-process section captures the child's stdout and stderr into one buffer and parses fields off marker lines. Measured over 20 child runs (10 at 2 ranks, 10 at 4): THREE came back corrupted -- two receipts concatenated with the newline lost, a receipt truncated mid-line, and a case with bytes DROPPED so a rank's value never appeared. Every value that survived was correct; the physics was never wrong. But `split(line)[3]` then yields `"MPI-LUM"` or `"5.02448981514298e9MPI-LUM"` or `"3"`, and the luminosity bit-identity pin at `runtests.jl` fails on a correct tree -- observed once in three consecutive gates on an unchanged tree, ~10% per 2-rank child. A pin that flaky is worse than none: it trains a re-run reflex. The anti-vacuity guard could not catch it either -- `!isempty(reported)` passed while the receipt count fell from four to three, so it proved only that the channel had said something. FIXED same day: each rank writes to `rank_<r>.txt` under `OCTOPUS_MPI_RECEIPT_DIR` and `run_ranks` makes a fresh directory per launch and joins the files in rank order, so every assertion reads `outN` unchanged and only the SOURCE of that text moved. The child needs one `redirect_stdout` for its ~218 receipt sites (MPI initialised at its top, the idempotent call the extension already makes); stderr stays on the merged stream and `fail` writes to both, so a rank that dies still says so where the launcher shows it, and the raw stream is kept as `rawN` for that. A hand-run with no receipt directory prints to stdout as before. The missing guard is in too: `got1 == 1`, `got2 == 2`, `got4 == 4` -- N ranks owe N receipts, where `!isempty(reported)` had proved only that the channel said something. Lesson in `experiences.md`. |
| Targeted neighbour audit of the multi-process campaign | **CLOSED 2026-09-06** (record: [`history/neighbour_audit_2026_09_06.md`](history/neighbour_audit_2026_09_06.md)) | **Run 2026-09-06**, the deferral condition met when step 4h closed. Twelve neighbour classes swept read-only in parallel over `5dd9e22..HEAD` (26 files, ~8300 insertions), every lead put to three skeptics with distinct lenses, every survivor re-verified against source by the auditor: 31 leads, 25 survived the skeptics -- rows, not distinct defects, since several classes found the same site independently, so 18 distinct sites -- and 24 of the 25 survived the auditor's re-verification. THREE MAJORS, all the sibling-that-did-not-get-the-fix shape: Gaussian-PIC did not broadcast its luminosity-schedule verdict though that verdict gates point-to-point messages in the shared sliced transport (a rank-divergent `PredicateSchedule` deadlocks the collide; the other three solvers broadcast, each with a comment saying why); the run artifact's `/losses/aperture_counts` was one rank's shard beside rows and summary attributes that are the whole beam's, with the globalized array already computed and handed to the writer that ignored it; and `profiling/benchmark_collide_cpu.jl` refused gaussian_pic and spectral under MPI with a message saying they "still refuse to run divided" -- a hand-copied solver roster stale since 4f/4g. Also fixed: the CUDA activation skipped `_warn_launcher_without_policy`, the launcher fixture resolved its shard OUTSIDE the policy scope (so its "different ranks' shards" never happened and the pin could not tell a correct `_mp_resolve_shard` from a broken one), the gpic bare collide did not open `_with_beam_shards`, and eight pieces of prose. TWO CLEAN CLASSES, stated because a clean result is a result: every `muladd` in the tree (five sites, the two under a bit-identity claim already pinned to `fma`) and all 25 `_run_logical_workers` callers (each writes only its own slot, so the inline path is the same writes in the same order). Deliberately NOT changed, with the reasoning in the record: `:pic_pair_schedule`'s varying receipt shape (five CUDA emitters vary it by design and the only consumer probes with `haskey`), and launching the seam-check child at one thread (its own thread-count sweeps build `threads=2` policies).  The standing obligation is one neighbour audit per campaign (`comprehensive_audit.md`, "The targeted neighbour audit"; owner direction 2026-08-08), and the multi-process campaign is still open -- steps 4b (wire `StrongStrongTask`) and 4c onward (PIC, gpic, spectral) are unlanded. Owner decision 2026-09-04: run it ONCE, after the last solver divides, rather than per step; auditing a moving seam five times would re-walk the same call sites with different answers each time. This row exists so the blast radius accumulated so far is not reconstructed from git when that day comes. **Surfaces the campaign has already touched:** the seam and shard helpers plus the `_run_logical_workers` one-thread inline path (`src/policies/Policies.jl`) and `ext/OctopusMPIExt.jl`; the resolved-policy slot and the sharded `Beam` constructor (`src/beam/Beam.jl`); `TrackingContext.index_offset` (`src/track/Track.jl`, `src/track/phase6d_track.jl`) and the aperture's `particle_id - ctx.index_offset` slot table (`src/elements/aperture.jl`); the isbits `ThinStrongBeamSlice` carrier (`src/elements/strong_beam.jl`); task-level activation, refusals and post-run accounting (`src/tasks/Tasks.jl`, `RunArtifact.jl`, `BeamObservers.jl`, `BPMObserver.jl`); the globalized slicing statistics, histogram and slice moments plus the 2026-09-04 split of `_slice_transverse_moments` into packable pieces (`src/tasks/strongstrong/slicing.jl`); the CPU wavefront schedule (`gaussian.jl`); the option-effectiveness device filter (`src/contracts/Contracts.jl`); and, from the `batch_mode`-one-keyword row closed 2026-09-04 (record: [`history/batch_mode_one_keyword_2026_09_04.md`](history/batch_mode_one_keyword_2026_09_04.md)), the CPU `batch_mode` reads and `:pic_pair_schedule` receipts in `pic_cpu.jl` and `gaussian_pic.jl`, the CUDA schedule receipts in `pic_cuda.jl` and `gaussian_pic_cuda.jl`, the new spectral field, schema, report and the rewritten `_spectral_collide_longitudinal!` with its collision-order luminosity fold (`spectral.jl`, `spectral_cuda.jl`); and from step 4b (2026-09-05, record: [`history/multi_process_step4b_2026_09_05.md`](history/multi_process_step4b_2026_09_05.md)) the representation-keyed shard scope (`_ShardEntry`, `_with_beam_shards`, both `_mp_current_shard` forms in `src/policies/Policies.jl`) and every reader that moved to it (`Tasks.jl`, `strong_beam_track.jl`, `BeamObservers.jl`, `slicing.jl` incl. the `_live_z_stats` shard argument, `gaussian.jl`), the per-beam tracking contexts and the two refusals in `interface.jl`, the `_reject_undivided_solver` hooks in `pic_cpu.jl`, `gaussian_pic.jl` and `spectral.jl`, the global emptiness and liveness refusals at the top of `longitudinal_slices` (a rank-local refusal there hung its peers; reproduced under a launcher by the 4b review), the `:funneled` tripwire in `_record_collective!`, the `PredicateSchedule` refusal on observers in both tasks (`Tasks.jl`), the soft-Gaussian `collide!` scoping its own beams (`gaussian.jl`), and `profiling/benchmark_collide_cpu.jl` scoping both beams. **Neighbours the audit must re-walk, each with the property the change was about:** (1) every `_slice_transverse_moments` caller -- `gaussian_pic.jl`, `spectral.jl`, `pic_cpu.jl` and the contracts share the function that was split, and they got the arithmetic by inheritance rather than by test; (2) **every `muladd` in the tree**, which is the session's sharpest lesson -- fusion is a codegen decision, so any refactor near a caller can move a result by 1 ulp, and only the two shifted-moment helpers have been pinned to `fma` so far; (3) the CPU/CUDA twins of everything above (`pic_cuda.jl`, `gaussian_pic_cuda.jl`, `spectral_cuda.jl`); (4) every `_run_logical_workers` caller, since the one-thread inline path rests on "each worker writes its own slot" being true of ALL of them and that was argued once, not checked per caller; (5) every reduction that reads a length as the beam's rather than the shard's (the `n` vs `n_local` segfault class); (6) every collective reachable from outside `_with_execution_policy` (the step-3b silent-no-op class, which shipped once already); (7) consumers whose receipt name is backend-tagged while their option is now cross-backend -- the one instance found so far, `:cpu_pic_pair_schedule`, became `:pic_pair_schedule` on 2026-09-04 when the PIC solvers' `batch_mode` widened; re-walk for any new one. **Properties to re-run on the neighbours, not just the changes:** "a lost particle is excluded from every reduction, exactly"; fixed-P bit-repeatability and cross-P parity; a one-rank MPI run reproducing `CPUThreadsExecutionPolicy` coordinate for coordinate; and schedule bit-identity wherever a solver gained a batched route. Precedent for shape and scale: [`history/neighbour_audit_2026_08_07.md`](history/neighbour_audit_2026_08_07.md) (it found two real defects, both in the SIBLING file of the fixes it audited). Fan the read-heavy sweeps out; fix what is small and squarely inside the blast radius; price and ledger the rest here. |
| The `:funneled` tripwire tests a thread id, not the driver task | **CLOSED 2026-09-07** (record: [`history/mpi_audit_followups_2026_09_07.md`](history/mpi_audit_followups_2026_09_07.md)) | `_record_collective!` and `_mp_test_all` now ask TWO questions: `_ON_SPAWNED_WORKER[]`, a `ScopedValue` bound around `_run_logical_workers`' spawn branch and inherited by the tasks it spawns, OR the old `Threads.threadid() != 1`. Neither subsumes the other -- the flag catches every spawned worker whatever thread it lands on, the thread test catches a collective from some other task entirely. **CORRECTION to the audit's own evidence:** the audit said thread 1 is in the default pool so workers run on it "frequently". Measured on Julia 1.12.4 over 256 spawned tasks: `--threads=4`, `--threads=auto` and `JULIA_NUM_THREADS=4` all give ONE interactive thread with the driver on id 1 and spawned tasks on 2..N+1 -- **0** on the driver's id, so the thread test does catch them. Only `--threads=N,0` (no interactive thread) puts the pool on the driver's thread, and there **104 of 256** land on id 1. The exposure is real but narrower than stated, and that is why the gate never saw it. The flag is deliberately NOT bound on the two INLINE branches: a body run inline is on the caller's task on the main thread, where a collective is legal, and the divided PIC collide depends on that -- it forces `pool_workers = 1` when divided so its per-pair collectives run one at a time on the main thread. The first draft bound all three branches and would have refused the `:node` collide at every rank count; a read-only blast-radius sweep caught it before the gate did. Pinned twice: in-process that every spawned worker is flagged and a collective from one is refused, and a `--threads=4,0` subprocess that asserts workers DO land on the driver's thread there and are still refused by the flag -- the case the thread test cannot see. Cost measured: one `ScopedValue` binding per `_run_logical_workers` call, 45 ns, ~0.04 ms/turn against a 2.6-8.4 s/turn collide. |
| `_run_logical_workers`' pool-of-one inline path is exercised nowhere | **CLOSED 2026-09-07** | Covered by a one-thread SUBPROCESS (`test/runtests.jl`, "The pool-of-one inline worker path agrees with the spawning one"): it asserts the inline grid writes the same slots in the same order as the spawning branch the parent just ran, and that the inline path leaves `_ON_SPAWNED_WORKER` false. The audit's proposed fix -- launching the MPI seam-check child at one thread -- stays rejected and the reason is in the testset: that child's thread-count sweeps build `MultiProcessExecutionPolicy(threads=2)`, which a one-thread process rejects. A plain subprocess needs no MPI and no launcher. |
| The luminosity-schedule broadcast is hand-copied per solver | **CLOSED 2026-09-07, and it found a live defect** (record: [`history/mpi_audit_followups_2026_09_07.md`](history/mpi_audit_followups_2026_09_07.md)) | The broadcast moved into `_luminosity_schedule_evaluated` (`interface.jl`), the one memoized consult, guarded on `schedule !== nothing` and inside the memo miss so the call counts stay symmetric across ranks; the four hand-written copies in `pic_cpu.jl`, `spectral.jl` (x2) and `gaussian_pic.jl` are gone. **Pulling the thread found a defect the audit had not:** the SPECTRAL solver never routed through that function at all. `_spectral_compute_luminosity` was a private copy of the whole consult -- its own `should_run`, its own receipt, and NO MEMO -- so the U5-2 defect the memo was written to fix in 2026-08 was still live in it. Measured on a schedule alternating true/false: spectral consulted **8 times over 4 turns** with the file-writing gate disagreeing with the solver on every turn, while pic, gpic and soft-Gaussian consulted 4 and agreed. That disagreement is exactly the "gate says evaluated, solver declined, artifact gets a NaN row" failure, with NaN reserved for "evaluated and numerically failed". Spectral now shares the body and keeps its own `:spectral_luminosity_schedule` receipt through a `consumer` argument. |
| Loss accounting on the artifact's CRASH path is rank-local, and gathers from a rank-local failure | **CLOSED 2026-09-07, NOT A DEFECT; the labelling gap was real and is fixed** | Both halves are deliberate and documented at the site. The crash path runs OUTSIDE the execution-policy scope on purpose -- the catch block says so: "an exception is in flight, the ranks may not agree on having thrown, and a collective issued by some of them would hang the rest. Outside the scope every collective is its serial passthrough, so a crashed run flushes what THIS rank saw rather than deadlocking trying to agree." So `_ra_gathered_loss_rows` is a passthrough there and cannot hang, and the rank-local summary is the information actually available. What WAS missing is what the row itself proposed -- "write this rank's rows, LABELLED as such". A crashed four-rank run wrote `dead=1` where a successful one writes `dead=4`, with nothing in the file to tell them apart. `/losses` now carries `summary_folded`, true on the success path and false on the crash path, pinned on both. |
| The artifact does not record `summary_logged` | **CLOSED 2026-09-07** | `/losses` now carries `summary_logged` beside the other summary attributes, so `sum(aperture_counts) == summary_logged` -- the reconciliation `loss_summary`'s docstring tells a reader to make, and the equality the 2026-09-06 F2 fix makes true across ranks -- is checkable inside the file. Written as `Int64` like every other attribute here (HDF5 has no native bool and a round-tripped `Bool` would depend on HDF5.jl's mapping); absent in a file written before 2026-09-07, where the reader returns `nothing`, the same shape it already used for an absent `aperture_s`. Pinned on both the success and crash write paths. |
| PIC `:node` / `:source_slice` grid modes take the per-pair route when divided | **CLOSED 2026-09-07, NOT A DEFECT** | Established, and it is deliberate rather than an oversight. `src/tasks/strongstrong/pic_cpu.jl` computes `sliced = _mp_multi_process_active() && !node_grid && !share_grid`, and the comment three lines above it says so: "The slice-aligned collide is what a multi-process policy runs on the `:slice_pair` mesh -- at one rank too. The node and source-slice meshes keep the per-pair paths below." That per-pair route is divided-aware: it takes `divided = _mp_nranks() > 1`, builds `_divided_slice_plan` for both beams, and passes `divided` into `_pic_prebuild_node_caches!` and `_pic_collide_pair!`. And it is PINNED under a launcher: `test/mpi_seam_check_fixture.jl`'s PIC variant table carries `:node`, `:source_slice` and `:node2` arms, and `test/runtests.jl` asserts for each of them, at 1, 2 AND 4 ranks, that the pair-schedule receipt reads `exchange=per_pair schedule=none` -- so the route each rank took is asserted, not assumed. No change. (The same reading also settles the receipt-shape item the audit withdrew: the fixture normalises the absent `schedule` field with a `hasproperty` fallback, which is why the varying shape harms nothing.) |
| Tracked `Manifest.toml` predates `Project.toml` | **CLOSED 2026-09-07** | Re-resolved and committed. **The 2026-09-04 diagnosis was wrong about the cause, and that is why it read as blocked.** It recorded `CUDA_Runtime_jll` as "pinned to 0.23.0 by an explicit requirement" needing "that pin revisited (it is the GPU box's runtime selection)". There is no pin: `grep pinned Manifest.toml` is empty and `Pkg.status` marks nothing pinned, and moving the untracked `LocalPreferences.toml` (which sets `CUDA_Runtime_jll.version = "13.0"`) aside reproduces the identical failure. The "explicit requirements" in the resolver log are the MANIFEST'S OWN recorded versions, which `Pkg.resolve()` holds fixed -- so the message was reporting that the tracked Manifest is internally INCONSISTENT (it names `CUDA_Runtime_jll` 0.23.0 beside `CUDA_Driver_jll` 13.3.0, and the registry says 13.3.0 admits only [0.1.0, 0.19.0-0.21.0]). `Pkg.resolve()` can never repair that, because it is the thing being held fixed. Deleting the Manifest and resolving fresh succeeds in one step. Resolved deltas are the same ones `Pkg.test` was silently re-resolving to on every run since 2026-08-06 -- CUDA 6.2.1 -> 6.3.1, CUDA_Runtime_jll 0.23.0 -> 0.24.4, CUDA_Driver_jll 13.3.0 -> 13.3.4, JLD2, SpecialFunctions, Crayons, ExprTools -- so the committed environment is the one every recent gate actually ran against, now recorded instead of rediscovered. Verified after: `CUDA.functional()` true, runtime 13.0.0 (the local preference is still honoured), driver 13.3.0, RTX 4500 Ada, a device reduction correct; full gate green with CUDA active and NO re-resolve banner in the log. `docs/guides/development_workflow.md` no longer calls its own "reproduces the exact environment" claim aspirational. |
| Run-artifact niceties deferred at step 4 | **open (2026-08-18)** | The artifact campaign closed 2026-08-18 (all four steps; full record: the archive’s "The run artifact: one output file per task" row and [`design/run_artifact.md`](design/run_artifact.md)). Three recorded niceties were deliberately deferred, none load-bearing. ~~s-position attributes on probe groups~~ **landed 2026-08-19**: probes stamp the design’s `attrs: name, s` at bind — the position comes from the ONE arc traversal (`_collect_spec_s!`, the U11-1 walker, gaining a probe arm that keeps the zero-length-advances-nothing contract), carried observer-keyed through a prepare scope so both tasks’ different prepare chains need no signature change; the reader surfaces `s` beside the columns (`nothing` for task-hook probes and older files). Pinned: WS line probes at 1.0/1.5 with a hook probe reading nothing, and SS probes reading their OWN line’s arc length (2.0 vs 3.0). Remaining: element names for the weak-strong luminosity channel labels (positional `strong_beam_<i>` until runtime elements carry names; the luminosity channels’ own `s` attribute belongs with this, the producer-identity work), and the live text mirror as a first-class option (the design’s `tail -f` posture; today nothing streams text). |
| CUDA in-suite vs standalone last-bit divergence: mechanism SOLVED | **CLOSED 2026-09-07** (record: [`history/cuda_lastbit_fma_2026_09_07.md`](history/cuda_lastbit_fma_2026_09_07.md)) | It was FMA CONTRACTION, and there was never an in-suite/standalone divergence. The CUDA kernel writes `acc += (zi - mu) * (zi - mu)` and the NVPTX backend contracts it into ONE `fma.rn.f64` -- measured in the kernel's PTX: one fma, zero `mul.f64`, zero `add.f64` -- while Julia's host codegen does not contract, so the CPU rounded twice where the device rounded once. Emulating the lane fold on the host reproduces each backend EXACTLY: `acc + d*d` gives the CPU's 0.007009746250353312, `fma(d, d, acc)` gives the device's ...311. That also answers the row's sharpest clue -- the MEAN never moved because `Val(1)` is `acc += zi`, a bare add with no multiply to contract. **CORRECTION to the model:** observation 11's "verified bitwise equal on both backends standalone under --check-bounds" is measured FALSE. CPU and CUDA differ in EVERY context -- standalone, standalone under `--check-bounds=yes`, and in-suite -- and the 4096 lane partials are byte-identical between process contexts. The discriminator's own output always said so: `recompute_matches_first_gpu = true` with `recompute_matches_cpu = false` is exactly "the GPU always contracts, the CPU never did". Three of the twelve observations were spent on a distinction that was not there. FIXED by pinning `_lane_z_moment`'s `Val(2)` branch to `fma` (`slicing.jl`), the direction `_shifted_second_moment`/`_shifted_cross_moment` already take and the more accurate one. The sigma assert is restored from the `<= 1 ulp` bound to bitwise. Gate: 215 testsets green, CUDA active, and the discriminator fired ZERO times where it fired in every preceding gate. |
| The observer schema-vs-report check is an identity for derived reports | **CLOSED 2026-09-07** | Confirmed and narrowed, and the coverage gap filled from a different direction. The comparison is REAL for the two observers with hand-written reports (`MomentObserver`, `CoordinateSnapshotObserver`, which name their entry symbols literally against separately written schemas) and an IDENTITY for `BPMObserver`, whose report iterates the same `_BPM_OBSERVER_OPTION_SCHEMA` const its accessor returns (`BPMObserver.jl:412` and `:416`) -- both sides are `keys` of ONE object. Kept for the two it bites on. THE ROW WAS ALSO STALE: it said "five observers whose reports are hand-written"; 2026-08-11's legacy cut and the 2026-08-18/19 artifact sweep took the concrete observer count from six to three, so the vacuous fraction went 1-in-6 to 1-in-3. WHAT REPLACES THE MISSING COVERAGE: the FIELD check the docstring has claimed all along ("schema keys match public fields") -- observers were the one family without it while every sibling had it. It had been tried and REVERTED because three observers exposed `capacity` while storing `buffer_capacity`; those observers were deleted and `capacity=ConfigurationOptionMeta` appears nowhere in src/ today, so the recorded obstacle is gone. Measured before enabling: 2-of-2, 2-of-2 and 12-of-12 schema keys are fields. Shown failing on an injected non-field key, pinned in `test/runtests.jl`. **NOT done, with the reason measured rather than assumed:** the sibling families also check declared defaults against constructor defaults, and observers cannot yet, because two declared defaults are SENTINELS -- `MomentObserver.moments` declares `nothing` and builds all 27 moments, `BPMObserver.rng_id` declares 0 and builds 2. A naive default check fails on both, on correct data. Doing it needs the meta to say which kind of default it carries (a `default_provenance` field distinguishing "this IS the constructor's default" from "this is a sentinel"), which is a change across `ConfigurationOptionMeta` and its call sites -- new row below. Full record: [`history/metadata_cluster_2026_09_07.md`](history/metadata_cluster_2026_09_07.md). |
| Three metadata-validator checks are tautologies | **CLOSED 2026-09-07** | Confirmed as a PROOF, not an observation, then replaced. `validate_element_metadata`'s friendly-schema, friendly-construction_help and friendly-example-kind checks each compared `f(T)` with `f(friendly)` where every one of those accessors is `_element_meta_or_nothing(x)` followed by a field read (`Knowledge.jl` parameter_schema :746-754, construction_help :779-787, example_spec :762-770). `T` sits in `ELEMENT_META_BY_SPEC_TYPE` and `friendly` in `ELEMENT_META_BY_FRIENDLY_TYPE` pointing at the SAME `ElementMeta`, so all three compared one field of one object with itself -- unfailable for any content, while their messages claimed the friendly constructor had been checked against the raw spec. REPLACED BY TWO whose sides come from different places: (1) `_element_meta_or_nothing(friendly) === meta`, the table binding stated honestly, which fails when a second `@element_spec` block or `register_friendly_alias!` (:448-452) rebinds the friendly type; and (2) the NON-CIRCULAR half the old three only claimed to be -- build through the friendly constructor from the example's own parameters and compare `kind` of the product, where `kind` reads the type parameter (:610) and the constructor hardcodes its own `ElementSpec{k}`, neither consulting the registry. MEASURED BEFORE ENABLING, so it is not a latent gate break: all 31 registered friendly constructors build from their own example and yield the declared kind. SHOWN FAILING, which is the point of the row: a probe declaring `DriftSpec` as another kind's friendly constructor -- table binding consistent, product wrong -- passes all three old checks and is rejected by both new ones ("builds drift, not the declared u12_8_probe"). Pinned in `test/runtests.jl` beside the `k3_liar` probe, including the restore of drift's legitimate binding that registering the probe overwrites (a plain `delete!` there leaves the registry broken for every later testset -- measured while writing it). |
| Observer declared defaults are unchecked, because two are sentinels | **open (2026-09-07, from U4-18)** | Every schema family except observers checks that a declared default matches what the constructor builds. Observers cannot, and the blocker is measured, not guessed: `MomentObserver.moments` declares `nothing` and the constructor builds the full 27-`Moment` tuple; `BPMObserver.rng_id` declares 0 and the constructor builds 2. Both are legitimate sentinel meanings ("nothing" = all moments; 0 = assign one), so a naive `declared == built` check fails on a correct tree -- which is why it must not simply be added. THE FIX SHAPE: give `ConfigurationOptionMeta` (`src/policies/Policies.jl`) a `default_provenance` keyword defaulting to today's meaning (`:constructor`), so no existing call site changes, and mark the two sentinels `:sentinel`. The validator then checks `declared == built` for `:constructor` options and skips `:sentinel` ones VISIBLY. Landing it means auditing every declared default across all families to say which it is -- that audit is the work, and it is exactly the "a check that cannot fail" campaign continued. Full record: [`history/metadata_cluster_2026_09_07.md`](history/metadata_cluster_2026_09_07.md). |
| Three metadata channels are unvalidated free text | **one of three CLOSED 2026-09-07 (2026-08-06, U12-6)** | `ParamMeta.unit`, `physics_keywords` and `description` are presented as authoritative and none was checked for truth. **THE ROW'S OWN EVIDENCE WAS WRONG ABOUT `unit`, IN THE DIRECTION THAT UNDERSTATED IT.** It said `unit` "is only ever concatenated into help output" and rated the whole row Low because "no automated consumer acts on them today". There IS an automated consumer: `_perturb_is_physical` (`src/contracts/Contracts.jl:2987`) reads `pmeta.unit in ("m", "rad")`, and its answer picks the perturbation the parameter-effectiveness contract applies to an integer-declared parameter -- a physical `1.0e-3` or an enum-style step (`:3019`). So the row's own injection, `unit="furlong"` on a length, does not merely print oddly: it downgrades that parameter to a weaker probe while the contract goes on passing. That is this campaign's "a check that cannot fail" shape one level out. **CLOSED for `unit`:** it now draws from `ALLOWED_PARAM_UNITS` (`src/knowledge/Knowledge.jl`), the same shape as the neighbouring `ALLOWED_PHYSICS_KEYWORDS`, checked in `validate_element_metadata`. Measured before enabling, over all 255 `ParamMeta` declarations: the entire declared vocabulary is `rad` (9), `m` (7), `Hz` (3) and empty (3, plus 233 that set no unit) -- no typos to fix, so this passes on today's tree and bites on the next one. Shown failing on an injected `unit="furlong"` -- the row's own injection, now caught -- pinned in `test/runtests.jl`. The docstring states what membership does NOT mean: `Hz` is admitted but is not physical to the contract, and widening the vocabulary is not a change to the perturbation policy. **STILL OPEN, the other two thirds:** keyword ACCURACY (membership in `ALLOWED_PHYSICS_KEYWORDS` is checked; whether `[:radiation, :beam_beam, :collimation]` on a plain kick is TRUE is not) and `description` accuracy (`"Thick quadrupole magnet with fringe fields"` on a thin element still passes). Both are semantic, neither has a behavioural consumer today, and each needs a curated cross-check or an agent pass -- which is why they stay Low while `unit` did not. AGENTS.md stakes agent orientation on these fields, so this still bounds what "self-describing source" guarantees. Full record: [`history/metadata_cluster_2026_09_07.md`](history/metadata_cluster_2026_09_07.md). |
| `validate_configuration_metadata` enumerates types by hand | **CLOSED 2026-09-07** | **THE ROW WAS STALE IN EVERY SPECIFIC IT NAMED, AND RE-READING THE CODE RATHER THAN THE ROW IS WHAT SHOWED IT.** Its claim was that a hardcoded type enumeration leaves a new solver or observer unchecked until someone edits a tuple, with `Contracts.jl` carrying a second hand-copy of the solver list. All of that has since been repaired by other rows: the solver and observer loops were totalized by U3-4, the policy and schedule loops by U12-3 (`_concrete_octopus_subtypes`), and `Contracts.jl`'s roster is derived by U4-7 (`_solver_contract_types`), whose tripwire FAILS the contract when a derived solver has no probe entry (`Contracts.jl:3669`). Its three measured staleness examples are all gone too: `GaussianPICPoissonSolver` and `BPMObserver` are covered, and `strong_strong_task_option_schema` exists with its own key-completeness check. **WHAT WAS ACTUALLY LEFT was the difference between those repairs, which no row had noticed:** two of the four tree walks recursed and two did not. The solver and observer loops used a bare one-level `subtypes` and SKIPPED abstract children (`isabstracttype(T) && continue`) instead of descending, so a concrete solver or observer under an intermediate abstract type was invisible to the guard whose whole purpose is to find it -- the trap U12-3 had already fixed for policies, and which U4-7's own docstring names for this very type tree. Both now use the shared recursive helper, so all four walks follow one rule, and the two duplicated comment blocks are gone (the reasoning already lives in the helper's docstring). **Not live today, asserted rather than assumed:** neither tree has an abstract intermediate, pinned as `all(!isabstracttype, subtypes(...))` -- the assertion that stops being true the day one appears. Shown failing on the real two-deep policy tree in process (a one-level walk misses `CUDAExecutionPolicy` and `GPUExecutionPolicy`), and end-to-end in a CHILD PROCESS that injects an abstract intermediate with a concrete leaf into Octopus and asserts the validator refuses it by name. The child is not fastidiousness: a type definition cannot be undone, so injecting it in process would fail every later `validate_configuration_metadata()` in the session. Full record: [`history/metadata_cluster_2026_09_07.md`](history/metadata_cluster_2026_09_07.md). |
| CPU threading: remaining measured headroom | **open leads (2026-08-10 campaign remainder)** | The campaign closed at PIC 41.0→3.0, gpic 65.5→6.8, spectral 5.0→2.6 s/collide with bit-identical digests; the ceiling is memory bandwidth, measured three ways. Remaining leads, by measured headroom: `FFTW.set_num_threads` is still nowhere (threaded FFTW plans reassociate — measure the bit cost before adopting), and further gains need less traffic per unit of work, not finer division of the same traffic. The CPU **6D spectral** performance campaign (four boundary solves per slice pair) remains DEMOTED (2026-07-24) but open. **LEAD 2026-09-06, CLOSED 2026-09-07 AS A MEASUREMENT ARTIFACT** (record: [`history/weak_strong_64thread_lead_2026_09_07.md`](history/weak_strong_64thread_lead_2026_09_07.md)). The lead was that weak-strong tracking at 64 threads is not linear in the turn count -- an interval rate climbing 0.313 -> 0.749 -> 0.816 s over turns 50-100/100-150/150-200, with a fitted intercept of -0.5 s where every other mode sat at a physical 13-19 s -- and that GC was the first suspect. Both halves were run and both fail. **(a) The probe the row specified refutes the GC suspicion outright:** four 50-turn windows WITHIN one process at the production point give 0.0735/0.0628/0.0578/0.0551 s/turn at 64 threads -- monotonically FALLING, the opposite of the reported climb -- with GC at 0.0% in every window but one (5.5%) and allocation at 0.000 GiB/turn. GC cannot be the mechanism because step 1 (2026-09-04) already took this path to zero allocation; Phase 0's 3.2/30.8/52.2% GC shares describe the tree BEFORE that fix, and the row cited them without re-reading them against it. **(b) The reported rate was never a within-run measurement.** The benchmark's own methods say a `TrackingTask` records no turn timings, so the case was run at 50/100/150/200 turns and the rate taken by differencing SEPARATE whole processes -- each paying its own load, JIT, beam build and artifact write, on a shared node whose load that record logs at 0.4-6.9. Replicated twice here: replicate 1 gives 0.731/0.431/0.436 (FALLS) and replicate 2 gives 0.408/0.552/0.678 (climbs). One replicate of the identical protocol falls and the other climbs, so the direction is not a property of the code. Two runs of the SAME configuration differ by up to 10.04 s (6.8-11.8%), which is 0.16-0.20 s/turn of noise per 50-turn interval and two such errors per derived difference; the record's whole 0.313-0.816 spread is 0.50, inside that. And the intercept -- the row's strongest evidence -- does not reproduce: +46.0 s and +44.9 s at R^2 0.981/0.988. A negative intercept is what least squares returns when the shortest run lands on a quiet node. **The rule this replaces:** not "do not quote a 64-thread number without its run length" but **do not derive a per-turn cost by differencing whole-process wall times on a shared node**. **CORRECTED 2026-09-07 by the instrumented re-run** (record: [`history/weak_strong_rerun_instrumented_2026_09_07.md`](history/weak_strong_rerun_instrumented_2026_09_07.md)): this row previously quoted ~0.055-0.074 s/turn at 64 threads and ~0.15-0.21 at 16 as the production case and derived "a real ~2.9x over 16". THOSE FIGURES ARE FROM `profiling/benchmark_track_cpu.jl`, whose line has no observer and no artifact -- a different workload from the harness at the same particle count -- and the row carried them with the attribution and the caveat stripped. Measured on the harness the production benchmark actually used, 200 turns at 1.024M: the arms are STEP FUNCTIONS, not drifts (16t 0.4870 for turns 12-47 then 0.6529 for 48-200; 64t 0.3722 for 34-73 then 0.5392 for 85-200, with a DOWNWARD step at turn 34 that no degradation mechanism produces), and the regime-matched speedup is ~1.21x, not 2.9x -- with a 1.03-1.41x spread across window choices, n=1 per arm, order not randomized, and node load rising throughout. "Gets faster as a run proceeds" was also a fact about the profiling line only. The ORIGINAL RETRACTION STANDS: the 0.313/0.749/0.816 climb is an artifact of the subtraction. A 64-thread arm drove a 128-core node's 1-minute load average only to 10.02, which is not 64-way concurrency and is the likeliest reason the arm buys 1.21x rather than anything near 4x -- unmeasured, worth a probe. **NEW ROW BELOW** for the gap this exposes: `TrackingTask` has no per-turn timing facility while `StrongStrongTask` does. Running record: [`history/cpu_threading_2026_08_09.md`](history/cpu_threading_2026_08_09.md) and the archive's CPU-threading row. |
| `TrackingTask` records no per-turn timings, so weak-strong timing is external arithmetic | **CLOSED 2026-09-07** | `TrackingTask` now carries `record_turn_times`, read back by the same exported `turn_timings` accessor that serves `StrongStrongTask` -- one keyword, one meaning, two tasks, rather than a second spelling for one idea. Metadata in a new exported `tracking_task_option_schema()`, checked against a CONSTRUCTED task (the U5-4 rule), plus the FIFTH tree walk (`AbstractTask`): policies, solvers, schedules and observers were totalized and tasks were the family still resting on a reviewer noticing -- which this repository's record says fails (U3-5 shipped a task option with no schema, U5-11 a schema unexported). **THE ARTIFACT-LEDGER ROUTE THE DESIGN NOTE PROPOSED WAS CHECKED AND REJECTED**, and `docs/design/run_artifact.md` now says so: the per-execution ledger OVERWRITES (one `(current_turn, elapsed)` pair per `execute!`, live progress rather than history), and `task.artifact === nothing` is a term of the fast-path gate, so attaching an artifact to obtain timings moves the run OFF the loop a benchmark measures -- the route cannot instrument its own motivating case. **THE TRAP, CLOSED STRUCTURALLY:** `TrackingTask` has TWO turn loops and their witnesses disagree -- the weak-strong harness always attaches an artifact and takes the general loop, while `profiling/benchmark_track_cpu.jl` builds a bare task and takes the fast one, so instrumenting only the general loop leaves a green harness and an empty vector for the case the work was for. `_execute_fast_tracking_turns!` therefore takes `record`/`times`/`backend` as REQUIRED POSITIONAL parameters (one call site, so a half-finished job is a compile error), and the `:tracking_turn_timing` receipt carries a `fast_path` field so the suite pins WHICH LOOP RAN rather than that some loop produced numbers. **A BUG IN THE INSTRUMENT BEING MIRRORED, FIXED IN THE SAME COMMIT:** `StrongStrongDiagnostics.record_turn_times` declares "Synchronize at complete-turn boundaries" but synchronized only at the tail, so of the N+1 boundaries the OPENING one was missing and turn 1 -- only turn 1 -- absorbed whatever was queued at entry (beam upload, warm-up launches). It survived because the production benchmark averages turns 100-200, and because the facility had NEVER been tested: `record_turn_times`, `turn_timings` and `diagnostic_summary` appear zero times in `test/runtests.jl`. This is the first in-suite pin of complete-turn timing on either task. Semantics: per-rank under MPI with no collective in the loop or the accessor (the reader takes the max across ranks), cleared per `execute!` so index i is turn `first_turn + i - 1` under a repositioned call, opt-in for storage (8 bytes/turn unbounded against 1e7-turn workloads). Measured payoff: `OCTOPUS_BENCH_TURN_SERIES=1` reports `first_quarter_mean=0.0222 last_quarter_mean=0.0145 drift=0.652x` inside ONE run -- falling, the opposite of what differencing whole-process wall times reported. Record: [`history/tracking_task_turn_timing_2026_09_07.md`](history/tracking_task_turn_timing_2026_09_07.md). |
| The fused luminosity route does not yet cover WRAPPED strong beams | **CLOSED 2026-09-08** | `luminosity_tracking=:fused` now carries luminosity through all three wrappers (record: [`history/luminosity_wrapper_support_2026_09_08.md`](history/luminosity_wrapper_support_2026_09_08.md)). Each forwards the accumulator it used to drop, and all three have the same shape, so each luminous form mirrors the method beside it: `CompositeLine` delegates to `fusedTrackLuminous` over its ops, `MisalignedElement` and `RefTilted` transform, forward to `inner`, transform back. **THE LEDGERED ORDER WAS LOAD-BEARING AND WAS FOLLOWED.** `strong_beams`, the artifact's channel labels and the per-turn push loop all derive from ONE scan, so making that scan wrapper-aware (`_strong_beams_of`, depth-first in line order) fixed all three at once. Relaxing the guard first would have given a wrapped beam a channel that is never written -- the same silent bug the guard exists to prevent, reintroduced by its own fix. **The guard is SCOPED, not lifted:** `:isolated` still refuses wrapped beams, because a wrapper cannot be taken apart to isolate what is inside it without destroying the rigid-body semantics a sub-line exists for; the refusal now names `luminosity_tracking = :fused` as the way out. Measured: a wrapper that does not move the beam does not move the number (nested `===` flat), one that does, does (misaligned differs, and agrees with itself however nested). `RefTilted` is pinned for the first time -- it had been declared in `_hidden_strong_beam_count` since that helper was written and never fixtured, and the same walk now decides which beams get artifact channels, so a miss would be a missing channel rather than only a missed guard. Attribution with two beams is pinned BACK TO BACK deliberately: with a drift between them this fixture's second luminosity is a legitimate 0.0, which is indistinguishable from an accumulator that fills only for the first element. |
| A public `TrackingTask` field can have no schema entry, invisibly | **open (2026-09-08)** | `validate_configuration_metadata` checks that schema keys ARE fields; it never checks the converse, so a public field with no metadata entry is structurally invisible. `loss_report` is exactly that: a `TrackingTask` constructor keyword and field since long before the task schema existed, absent from `tracking_task_option_schema()`, and no check can notice. Same family as the four "a check that cannot fail" defects closed 2026-09-07 (the observer schema-vs-report identity, `ParamMeta.unit`, the docs index, the gpic pin's tuned tolerance). THE FIX is not simply to add the missing entry -- it is to make the omission impossible, by checking both directions for every schema that claims to describe a type's public options, and then adding whatever entries that turns up. Found while designing the `luminosity_tracking` option, by an agent reading how a new option must be declared. |
| Bmad reference cases for `misalign_convention=:bmad` | blocked on a tool | The **default** convention has no reference case: its reference point, rotation order, and now the frame a misalignment is quoted in against a `ref_tilt` are all read from Bmad source, never measured. `:madx` is pinned at ~5e-13 throughout. Needs Bmad in the validation path; one comparison would close all three. *(2026-08-14: a fourth rider folds in — an external reference case for `ThinAcceleratingCavitySpec`, which MAD-X cannot express (no reference-energy-changing cavity) and Bmad's `lcavity` can.)* *(2026-08-15: a fifth rider — a PURE curved quadrupole reference, `h != 0` with `b0 = 0`. MAD-X 5.03.06's SBEND `k0` is measured IGNORED by its PTC translation (outputs bit-identical with k0 absent, 0, 0.12, 1e-6), so `h != b0` has no spelling there; Bmad's independent `g` vs field would express it. The h = b0 curved channels ARE now PTC-pinned — `cfbend_quad_heavy` at the polygon working point, `cfbend_skew` for the odd Psi_1 branch, `cfbend_k3`/`cfbend_k5` for high orders through EFCOMP — all at ~5e-13.)* |
| CUDA near-identity pin wobbles (the family ledger) | **ALL THREE MEMBERS CLOSED 2026-09-07** | **MEMBER 2 IS CLOSED, AND IT WAS NEVER A NEW PHENOMENON** (record: [`history/gpic_wobble_mechanism_2026_09_07.md`](history/gpic_wobble_mechanism_2026_09_07.md)). It is the CUDA PIC route's float-atomic deposition nondeterminism -- documented, measured and priced at the TOP OF `src/tasks/strongstrong/pic_cuda.jl` since the 2026-08-05_b audit (U3-6), with a recorded spread of 6.9e-16 to 8.3e-16 against the 6.3e-16 this row called unexplained. **The measurement that dissolved it:** every probe had compared PIC against GPIC and none had compared a route against ITSELF. One process, the failing geometry: PIC alone gives 2 distinct results over 12 calls, GPIC alone gives 2, and the union is 2 -- a single route disagrees with itself. Quantified: PIC's own call-to-call spread 0.00e+00, GPIC's 7.358e-16, and the pic-vs-gpic spread the pin asserts on is 7.358e-16, the same number to the digit. **Every element of the row's framing was a sampling artifact.** "Exactly two profiles": the sequential route shows THREE. "The coin belongs to the gpic fallback": which route shows it FLIPS between configurations (gpic under async+slice_pair; PIC under plain wavefront and under sequential; the spread is the same 7.36e-16 wherever it lands). "Only reachable in-suite": reproduced standalone in about a minute. It hides at n=64 because atomic collisions are rare there -- n=2..64 give 1 distinct result and n=256 gives 10 of 10 -- so a quiet process reads frozen (the 20/20 and 160/160 both come from this), while prior CUDA work changes occupancy enough to expose it. **FIXED: the pin, not the solver.** Determinism would mean ordered reductions instead of atomic deposition, which `pic_cuda.jl` prices and declines; that is an owner-level trade-off and the physics is unaffected at rounding level. What was wrong is that the pin asserted near-equality between two draws of a nondeterministic route, with atol=2e-15 tuned as "3x the measured wobble norm" of a misidentified phenomenon. The tolerance is now DERIVED IN-PROCESS: a second PIC draw at identical inputs measures the route's own spread at that geometry and the comparison admits 4x it, with 2e-15 kept as a floor. Discriminating power is unchanged -- a wrong fallback diverges wholesale at O(kick)~1e-15 and rtol=2e-12 catches it regardless of atol. **Two of my own accounts were withdrawn by measurement and are kept in the record:** a cached-grid account (refuted by instrumenting the cache -- the grid is bit-identical every call, always a fresh build, never a hit) and a `slice_pair_green_growth` lever (confounded: four configurations run sequentially in ONE process varied knob and position together; it vanished when each was given its own process). THE LESSON: when a near-identity pin wobbles, the first question is not how the two things differ but whether either is reproducible alone. PRIOR CONTEXT FOLLOWS.  **MEMBERS 1 AND 3 SOLVED AND FIXED** (record: [`history/cuda_lastbit_fma_2026_09_07.md`](history/cuda_lastbit_fma_2026_09_07.md)): both were the device contracting `acc += (zi - mu) * (zi - mu)` into one `fma.rn.f64` while the host did not. One fix closes both, because `_masked_rms` (member 1) and `_live_z_stats` (member 3) call the SAME `_lane_z_moment(..., Val(2))` -- which is also why member 1's failure was on the unmasked arm and member 3's on the masked one: the arm never mattered, the `Val(2)` did. The host branch now accumulates with `fma`; CPU and CUDA agree bitwise; the sigma pin is back to `==`. **MEMBER 2 IS A DIFFERENT PHENOMENON AND STAYS OPEN.** Contraction cannot produce it: a contraction decision is fixed for a compiled kernel, while member 2's signature is two BIT-IDENTICAL profiles alternating between two calls WITHIN one process, each collide drawing one. The family split this row already suspected is now explained -- member 2 varies within a process, members 1 and 3 never did. **The leading hypothesis is DEAD and the mechanism class is measured (2026-09-07).** The cuFFT plan-eviction account is impossible in this stack: cuFFT holds a handle cache (`cuFFT/*/src/wrappers.jl:138`) but NEVER registers it as reclaimable -- `grep -rn register_reclaimable cuFFT/*/src/` is empty while cuBLAS registers three (`cuBLAS.jl:317-319`) -- and `purge!` is reachable only through that registry, so CUDA.jl's reclaim path cannot evict a cuFFT plan. What the deposit actually does is `CUDA.@atomic charge[...] += w`, a FLOAT atomic add, and float addition is not associative. Measured, 64 particles on a 16x16 grid, 200 reps per geometry in one quiet process: threads=32/blocks=2 gives FOUR distinct charge arrays, while blocks=1 at 64/128/256/512 threads gives ONE each -- and the same one. So the ordering is repeatable within a block and not across blocks, since block scheduling order varies; 2000 reps at blocks=1 gave a single profile, which is why standalone probes read 20/20 "frozen". `_cuda_pic_threads` returns a fixed 256 unless a task scope installs a `ResolvedCUDAPICLaunchConfig` (`interface.jl:306-314`), so the same deposit runs in one block outside a task and possibly several inside one. NOT a defect: nondeterministic ordering is inherent to float atomic deposition, and the alternative (per-block partials plus an ordered reduction) changes every PIC result to buy reproducibility the physics does not need -- the derived atol is the right response and now has a reason rather than an empirical bound. **MECHANISM NOT ESTABLISHED -- two accounts tried and both withdrawn, 2026-09-07.** (a) The cuFFT plan-eviction account is DEAD and stays dead: cuFFT never registers its handle cache as reclaimable (`grep -rn register_reclaimable cuFFT/*/src/` empty; cuBLAS registers three), so CUDA.jl's reclaim path cannot evict a plan. (b) A block-count account was proposed and is wrong: the failing route has 64 particles at `deposit_threads = 256`, so `blocks = cld(64,256) = 1`. (c) An ALLOCATION-LAYOUT account was then measured on a SYNTHETIC 64-particle CIC deposit at that geometry -- 400 deposits over ten pool states gave exactly two profiles, 242/158, matching the ledger's signature -- and it does NOT survive the real route: the actual paired PIC/gpic fixture, with the pool disturbed between the arms (one large alloc/free to 512 MiB, and separately up to 1499 live fragments kept across the second arm), gives ONE profile and max|gpic-pic| = 0.0 over 160 trials. Shape-matching ("exactly two profiles") is not reproduction, and it was persuasive enough to be recorded before it was tested against the real route -- the wrong turn is kept here on purpose. WHAT IS GAINED: standalone stability is now bounded at 160/160 bit-exact including deliberate pool growth and fragmentation, up from the ledger's 20/20, so whatever in-suite state flips the coin is NOT reachable by these perturbations. NEXT INSTRUMENT must come from the suite, not a standalone probe: the capture trap is armed, and the datum worth having is a firing with the pool state and the `:cuda_pic_launch` receipts captured beside the arrays. Retained from (c) as a true but unapplied fact: float atomic adds are order-sensitive and a deposit's result CAN depend on layout.  the failing route has 64 particles at `deposit_threads = 256`, so `blocks = cld(64, 256) = 1` -- block count cannot be the lever. The lever is ALLOCATION LAYOUT. Measured at the real geometry (64 particles, threads=256, blocks=1), 400 deposits across ten CUDA memory-pool states: EXACTLY TWO distinct charge arrays, 242 and 158 occurrences. Same launch shape, same input, different pool layout, two bit-identical profiles each drawn often -- the ledger's signature ("exactly TWO reproducible profiles, and EACH collide call draws one") reproduced on demand in one quiet process. A quiet standalone process reuses the same addresses every run, which is why it read 20/20 bit-exact; a late-suite process's grown, fragmented pool gives the paired PIC and gpic calls different layouts, which is why it straddles. This vindicates the TESTSET's own original attribution ("allocation-layout-dependent scheduling", `test/runtests.jl`), which observation 7's cuFFT "refined model" talked the row out of. The capture trap stays armed at atol=2e-15; evidence file [`history/gpic_wobble_capture_2026_08_19.txt`](history/gpic_wobble_capture_2026_08_19.txt). The original observations 1-12 are preserved below as the record of how this was found. |

## Open questions from studies

- **Observable-matched slicing nodes (Phase 3c, one experiment):** minimize the
  kick error (or a luminosity-weighted error) directly over `{z_k, w_k}` at
  fixed `ns` to get the true lower bound the slicing rules should be scored
  against. The Gauss-Hermite result is direct evidence that source-fidelity
  objectives and kick accuracy come apart. Per-machine result, acceptable for a
  fixed design point; hypothesis, not established practice.
- **Hadron-side slicing stopping criterion:** Furman's `Q ~ 4/sqrt(tau)`
  applies to the electron ring; the proton ring has no comparable `tau`, so
  nothing masks its residual. Pick a long-term emittance-growth rate or
  diffusion coefficient instead.
- **`:equal_width` carries a second convergence parameter** (`slice_width`): a
  user who fixes the width will see convergence stall. Consider deriving the
  width from `ns` by default, or rejecting a fixed width above some `ns`.

## Watch

| CI segfault on Julia 1.12.6 | **open (2026-08-12)** | CPU-only CI on ubuntu (`ubuntu-latest`, Julia resolved by the floating '1.12' pin to 1.12.6) dies with signal 11 right after the "MomentObserver append mode continues one table" testset; every printed testset passes. The identical suite passes on 1.12.4 locally, GPU-active AND with `CUDA_VISIBLE_DEVICES=""`. CI is pinned to 1.12.4 as mitigation; local 1.12.6 reproduction was launched 2026-08-12 (fresh depot, CPU-only) to obtain the native crash location — prime suspects are native-library finalizers (HDF5) or a 1.12.6 runtime regression. Loosen the CI pin only after diagnosis. **Correction and repro status (2026-08-13):** the version-flip framing was wrong — 1.12.6 was released 2026-04-10 and is still the newest 1.12.x (official versions.json), so *every* CI run since the workflow landed (2026-07-21) resolved '1.12' to 1.12.6, ~330 of them green. Exactly two segfault-class failures exist (derived from the complete 18-failure CI ledger: 12 are the known 2026-08-04 F2 deterministic-abort cluster starting at `baf0255`, 2 are 2026-07-23 dev-era reds, and the two 2026-08-06 entries are a runner-provisioning failure in "Set up job" and a cancelled job — neither reached the suite): 2026-08-11T01:15Z on main (4ed84e4, test step died at 11.1 min where the green run just before took 17.0) and 2026-08-12T18:42Z on paper-cpc (12325fc — the *same commit* had passed on main at 18:32Z; died at 13.3 min vs 16.7). Same-commit pass/fail ten minutes apart and two different death points → flaky and timing-dependent, not a deterministic 1.12.6 regression; the 1.12.4 pin is a toolchain freeze justified by local-gate parity, not a fix, and its three green runs since prove nothing at a ~2/330 failure rate. The ubuntu24 runner-image update in that window (20260810, published 2026-08-11T10:37Z) postdates the first failure, so an image roll does not explain onset either. The 2026-08-12 local 1.12.6 reproduction came back **GREEN** (fresh depot, CPU-only, `--threads=4`, 170 testsets — `gate_1126_nogpu.log`), so the crash has not been seen off CI; a 6-run 1.12.6 stress loop pinned to 4 CPUs (`taskset`, `JULIA_CPU_THREADS=4`, core dumps enabled) was launched 2026-08-13 to emulate runner contention. Mechanism note: all Octopus HDF5 use is scoped `h5open() do` blocks, and the installed HDF5.jl 0.17.3 already defers finalizers via `trylock` with every API call behind `liblock` (JuliaIO/HDF5.jl#990/#1048), so the classic finalizer race *should* be closed — the mechanism is genuinely unknown until a native backtrace exists. The raw CI logs (crash banner, runner-image build) are auth-walled to the API; they need a logged-in browser: runs 31629024074 (Aug 12) and 31448717154 (Aug 11). Version-asymmetry caveat on the pin: the v1.12.4→v1.12.6 backports (85 commits) include GC/threading *fixes* 1.12.4 lacks — a missing GC write barrier (JuliaLang/julia#61120, heap-corruption class), `jl_exit_threaded_region` on interrupt (#60822), uninitialized memory in the GC stack (#60651) — plus a new heap-size-based full-GC heuristic (#60376) that changes when finalizer bursts fire, a plausible modulator for a latent race firing on 16 GB CI runners and never on a 503 GB idle node. So 1.12.6 is the strictly more-fixed binary and the pin buys local parity only; if the stress loop stays green, the decision to loosen should weigh #61120 against the two observed 1.12.6 flakes. **Stress result (2026-08-13):** 3 of 3 completed pinned-4-CPU 1.12.6 full suites green (~22 min each); the loop was stopped at 3 of 6 planned runs to free the repository for the survey/F16 campaign — at the observed CI rate (~2/330) six local runs were never a strong detector, and the ongoing detector is CI statistics after any future un-pin. The crash remains unreproduced off CI. **Recommendation (2026-08-14, owner decision):** re-float the pin to '1.12' (= 1.12.6). The evidence: ~330 green 1.12.6 runs against 2 flakes; 1.12.4 lacks 1.12.6's GC write-barrier fix (#61120, heap-corruption class) and threaded-region fix (#60822), so the pin trades a known-fixed silent-corruption class for the avoidance of a ~0.6%-per-run visible flake; a flaky red is loud and re-runnable, a write-barrier miss is neither. Against: the recorded "loosen only after diagnosis" condition, which this row's later evidence supersedes in the recommender's judgment but which the owner set the mitigation under. **Approved and acted (2026-08-14):** the pin floats again; ci.yml carries the operational note (a signal-11 red after a passing testset summary is the known flake — re-run, and tally it HERE). Flake tally since re-float: **0 signal-11 flakes in 67 runs** (through `52ac035`, 2026-09-07Z; counted from the Actions API, 64 green and 3 red). The three reds are NOT this flake and are not tallied as one: they are 2026-09-07 `6b5abe8`/`e15b48e`/`a2d3286`, all the same real regression -- a new harness-MPI testset that hard-asserted an MPI child could launch, where CPU-only CI cannot, and whose unguarded `readdir` threw an IOError instead of failing the assertion. Named cause, fixed in `33173e0`, green since. They are exactly the case the operational note distinguishes: a signal-11 red arrives AFTER a passing testset summary; these arrived as a named assertion inside one. WHAT 0-IN-67 IS WORTH, stated rather than implied: at the observed pre-pin rate (~2/330 = 0.6%/run) the expectation over 67 runs is ~0.4 flakes, so zero is consistent with the rate being unchanged and does not yet distinguish "gone" from "still 0.6%". It is, however, 17x the evidence the re-float decision was taken on (0 in 4). The row stays open as the ledger on its own terms. This row stays open as the statistics ledger until either the crash reproduces somewhere debuggable or a Julia point release changes the question. |

(The CI row above is a statistics ledger, not a work item: tally any signal-11
flake after a passing testset summary here, per the note in `ci.yml`.)
