# TODO

Open work only. This file was restructured on 2026-08-16: the record of
completed campaigns — closed rows, narratives, measurements, wrong turns and
their corrections, all previously in this file — is frozen verbatim in
[`history/todo_ledger_archive.md`](history/todo_ledger_archive.md), and the
recurring lessons distilled from that record live in
[`experiences.md`](experiences.md). Code and test comments citing a "todo row"
by U-number resolve against the archive. Standing
deliberately-not-being-done decisions also live in `experiences.md`.

When an item below closes: move its row (with the closing measurements) to the
archive under a dated note, and if it taught something reusable, add the
lesson to `experiences.md`.

## Open, most actionable first

| item | state | notes |
|---|---|---|
| Multi-process mixed execution policy (MPI x CPUThreads) | **open (2026-09-04, steps 1-2 closed)** | **Step 2 closed 2026-09-04** (design: [`design/multi_process_policy.md`](design/multi_process_policy.md); environment decision: [`history/mpi_environment_2026_09_04.md`](history/mpi_environment_2026_09_04.md)): `MultiProcessExecutionPolicy(; threads, ranks)` composes the CPU policy; the resolved state is a SLOT on `ResolvedCPUExecutionPolicy`, not a wrapper (a wrapper needs unwrapping at every positional receiver -- the public `track!` entry among them -- and one miss is a MethodError or a silently different path). Resolution is pure; activation opens the communicator, reads nranks/rank FROM it, and records `:multi_process_communicator`. Six collectives (`_mp_allsum!`, `_mp_lane_fold!`, `_mp_allminmax`, `_mp_bcast!`/`_mp_bcast`, `_mp_barrier`, plus `_mp_nranks`/`_mp_rank`/`_mp_is_root`) with a serial passthrough in core and `ext/OctopusMPIExt.jl` for MPI; every float reduction is Allgather + rank-ordered fold, never `MPI_SUM`. Measured: the seam runs at 1/2/4 ranks under MPICH_jll's mpiexec, and a one-rank MPI run reproduces `CPUThreadsExecutionPolicy` coordinate for coordinate (the Phase 0 requirement, with the extension actually loaded). No task shards yet, so `execute!` REFUSES at nranks > 1 naming step 3, and the refusal is pinned so its removal is visible; a launcher that starts P > 1 ranks throws under the MP policy and warns once under an ordinary one. **Step 3a closed 2026-09-04** (record: [`history/multi_process_step3a_2026_09_04.md`](history/multi_process_step3a_2026_09_04.md)): a `TrackingTask` -- the GENERAL one; there is no separate weak-strong task -- now runs divided across ranks. Shard rule: a contiguous run of whole `_REDUCTION_CHUNKS`, so the chunk-ordered folds extend across processes unchanged; rank counts that do not divide 64 are rejected. The shard is derived at a run's entry (sum the ranks' counts, re-derive, verify the local count) rather than stored. `TrackingContext` carries an `index_offset` so the counter RNG keys on the GLOBAL particle index; `Beam(n_global, MultiProcessExecutionPolicy(), ...)` draws the whole beam and keeps this rank's slice, because standardization is a whole-array mean/variance and a per-rank draw is a different beam. The strong-beam luminosity fold crosses ranks through one all-sum of 64 numbers. Measured under mpiexec with radiation excitation ON: tracking and luminosity bitwise identical to the single-process run at 1, 2 and 4 ranks. CPU/MPI/CUDA consistency is stated in the design note: CPU==MPI bitwise, CPU~CUDA at the contract's tolerance, MPI~CUDA by composition (not measurable -- the policy is CPU-storage only). Scaling measured 2026-09-04 (record: [`history/multi_process_tracking_scaling_2026_09_04.md`](history/multi_process_tracking_scaling_2026_09_04.md)): at the production point, 64 ranks x 1 thread is 46.1x over 1x1 against 23.9x for 1 rank x 64 threads -- processes beat threads 1.9x at the same width, because thread scaling caps near 16/process (memory bandwidth, the 2026-08-09 campaign) while ranks do not share that ceiling. Best measured 64 ranks x 2 threads = 61.9x, 2.6x better than any single-process configuration; `-bind-to core` costs nothing and cuts the fastest/slowest rank spread from 35% to 5%; `-bind-to hwthread` is 2.3x slower (it oversubscribes a multi-threaded rank). Per-turn communication is NIL (one integer all-sum per execute!, none per turn), so no code optimization followed -- the win is configuration. Rank count is capped at 64 by the shard rule (P | 64); a 128-rank launch was refused correctly. **Step 3b closed 2026-09-04** (record: [`history/multi_process_step3b_2026_09_04.md`](history/multi_process_step3b_2026_09_04.md)): the diagnostics that reduce the beam to SCALARS divide -- moment observers, BPMs, and the loss counts -- and rank 0 owns the one output file (every rank still runs every observer, because the reductions are collectives). The ledger's open decision is ANSWERED and it went against the attractive option: folding these on the fixed chunk grid buys cross-rank BITWISE agreement, was implemented, and broke "a lost particle is excluded from every reduction, exactly" (the masked row stopped equalling the survivors-only row; recorded means moved up to 206 ulps), because a chunk grid partitions SLOTS and a masked beam has more slots than survivors. So the local accumulation is unchanged -- one rank's numbers do not move at all -- and the ranks' partials fold in rank order, agreeing with an undivided run to 1.7e-14 at 2 ranks and 8.7e-14 at 4, which is the campaign's own parity-tolerance posture. Measured under a launcher at 1/2/4 ranks: 3 moment rows in the file at every count (the proof only rank 0 wrote), loss counts identical (256 particles, 4 dead). **Step 3c closed 2026-09-04** (record: [`history/multi_process_step3c_2026_09_04.md`](history/multi_process_step3c_2026_09_04.md)): per-particle output divides. The seam gains a seventh collective, `_mp_gather_rows`, which collects rows onto rank 0 in rank order; aperture loss rows and coordinate snapshots carry the GLOBAL particle id (the aperture's slot table is indexed by `particle_id - ctx.index_offset`, the file gets the id back). It also FIXED A DEFECT IN 3b: the post-run accounting in `execute!` ran outside the `_with_execution_policy` block, where a collective sees a communicator of one and silently does nothing -- so 3b's loss-summary reduction was never reached by `execute!`, and the gather first returned 42/17/9 rows at 1/2/4 ranks, each rank 0's shard alone. 3b's test called the function inside a scope and passed while the route to it had none. Measured after the fix: 42 loss rows, identical ids, identical snapshot ids and an identical in-file summary (256 particles, 42 dead) at 1, 2 and 4 ranks. **Step 4a closed 2026-09-04** (record: [`history/multi_process_step4a_2026_09_04.md`](history/multi_process_step4a_2026_09_04.md)): the soft-Gaussian `collide!` divides. Five reductions span the ranks -- longitudinal statistics (lane fold carrying the shard offset), the HISTOGRAM the equal-area boundaries are cut from, each slice's transverse moments (with the globally-first member as the shared shift origin, found by a min over global index then summed from its owner), each slice's weight/centroid, and the luminosity (once per collide, not per pair). Slice boundaries and weights are BIT-IDENTICAL across rank counts, which they must be -- they decide slice membership, so a disagreement is a different collision -- and the collide's aggregates agree to 5e-16 at 2 and 4 ranks. The measurement found a defect the statistics alone hid: the equal-area boundaries come from a histogram, not from the (already global) statistics, and each rank was binning its own shard -- luminosity was 2%/11% out at 2/4 ranks and whole slices were empty. `:equal_count` REFUSES (a global sort, not a fold). Scaling measured 2026-09-04 (record: [`history/multi_process_strongstrong_scaling_2026_09_04.md`](history/multi_process_strongstrong_scaling_2026_09_04.md)): at the production point the soft-Gaussian collide runs 3.4587 s serial, 0.2950 s at 16 ranks x 1 thread (**11.72x**), against 2.15x for the best threaded configuration -- which SETTLES the "gaussian collide flatline" rider below: the flatline is the THREAD axis (caps at 2.15x, turns over by 16), and Phase 0 saw no rank scaling because every rank ran 16 threads, already past that turnover. Luminosity agrees to 3e-16 at every rank count. The curve turns over past 16 ranks for a counted reason: one collide issues 1874 collectives (1388 allsum, 452 allminmax, 34 lane_fold) of which 1840 average 33 bytes, and every float reduction is an Allgather + rank-ordered fold costing O(P), so per-collective cost rises with P while compute falls as 1/P. **Optimized 2026-09-04, turnover GONE:** `_run_logical_workers` runs its grid inline when the pool holds one thread (it spawned ~57,600 tasks per collide at one thread for no parallelism), and the lane folds exchange the folded SCALAR rather than 4096 lanes (the fold's shape is already broken by the split within a lane, so it bought nothing at 4096x the bytes). Neither moves a number -- same digest, same luminosity. Re-measured: 3.3891 s serial, 0.1028 s at 64 ranks = **32.97x, monotone**, and the run-to-run spread collapsed (1760% -> 153% at 64 ranks). Traffic fell 1,175,656 -> 61,816 bytes per collide, 19x, with the call count unchanged. **Remaining headroom, measured two ways (seam latency timed directly; size scaling at fixed ranks, the fit predicting the half-size point to 1%):** at 64 ranks the 0.1009 s collide is 52% ideal compute, 18% per-rank inefficiency, 30% collectives. The 1874 CALLS are the target and `collision_pair_batches` is the lever -- the CPU soft-Gaussian is the ONLY collide path in the tree still strictly sequential (the CUDA soft-Gaussian, PIC on both backends, gpic and spectral all batch; `GaussianPoissonSolver` already defaults to `batch_mode=:wavefront` and only its CPU method ignores the field), and PIC's recorded argument (batching preserves each slice's own collision order, so the batched path reproduces the sequential result BIT FOR BIT; the exception is the mesh mode soft-Gaussian does not have) applies. Hoisting what is fixed for a collide -> ~970 calls, merging the two beams per pair -> ~520, wavefront batching (~29 batches) -> ~130; that projects 0.0731 s = **46x from 34x**, and more at higher P. **The two beams are a second, independent axis (owner-raised 2026-09-04), and it attacks the 18% per-rank inefficiency rather than the 30% collectives:** inside `_cpu_gaussian_slice_pair!` the two moment reductions read different beams and neither reads the other's result, and the two `_slice_slice_gaussian_kick!` calls write disjoint arrays (`rep1` vs `rep2`) from moments both computed BEFORE either kick -- so beam 1 and beam 2 are independent within a pair and are currently done one after the other, each over its own 64-chunk grid. A shard's slice is SMALL (a 15-slice, 64-rank run leaves ~1/960 of the beam per rank per slice), so a 64-chunk grid over it is thin and the per-worker cost is mostly grid overhead; issuing both beams as one grid of 128 doubles the width where it is thinnest, and the merged moment buffer is the same edit that turns two all-sums into one. The bit-identity constraint is that the two beams' luminosity accumulators must stay SEPARATE and each fold stay in chunk order -- widen the grid, never merge the folds. The CUDA wavefront route is the existence proof that the shape works: it already carries both beams in one array dimension (`2 * max_batch` columns) and reproduces the sequential result. **Owner constraint (2026-09-04): REUSE the existing option keywords, invent none.** The CPU soft-Gaussian batching switch is the `batch_mode` field `GaussianPoissonSolver` ALREADY carries (`:sequential`/`:wavefront`, defaulting to `:wavefront`) -- the CPU method just does not read it today, which is why `interface.jl` documents `batch_mode` as CUDA-only. That documented scope is ALSO already inaccurate and must be corrected by the same change: it claims "the CPU paths always use collision-time order", but CPU PIC and CPU gpic do batch -- by their own `_pic_batchable` rule (`!_pic_source_slice_grid`), not by `batch_mode`. So the landing shape is one keyword with one meaning on every solver and both backends, and the schema entry gains a CPU consumer beside its CUDA one. Then a deterministic reduction tree (Phase 0's "past ~16 ranks"; worth ~3x per message at 64 ranks, since latency rises 1.4 -> 18.6 us from 2 -> 64 ranks) and non-blocking overlap (Phase 0's "interleaving"). **Will not help:** a communicator per slice -- the shard is contiguous in particle index so every rank holds members of every slice; it needs a longitudinally aligned decomposition, which conflicts with the chunk-aligned bit-identity and needs re-partitioning every turn. **CPU wavefront LANDED 2026-09-04** (record: [`history/soft_gaussian_wavefront_cpu_2026_09_04.md`](history/soft_gaussian_wavefront_cpu_2026_09_04.md)): the CPU soft-Gaussian reads `batch_mode` -- the keyword the solver already had, per the owner's reuse-the-keywords constraint -- and `supported_backends` now names both backends. Three edits: `collision_pair_batches` groups the 225 pairs into 29 conflict-free batches; the two things about a slice that are NOT coordinates (its global count, and WHICH particle is its shift origin) hoist out of the pair loop entirely, since slice membership is fixed once the beams are sliced; and each batch exchanges its slices' origins and shifted sums in one message per beam. The kicks go out as one grid per batch, both beams together -- the owner's second lever, honest scope: at one thread per rank `_run_logical_workers` runs the grid inline, so that width helps the threaded and single-process cases, not the 64x1 configuration. Measured: **1874 collectives -> 222** (8.4x; allsum 1422->190, allminmax 452->32), and at 64 ranks the collide's floor **0.0974 -> 0.0755 s = 1.29x**, 34.9x -> 45.0x over the same evening's 3.3947 s serial, with the non-compute term halved (0.0444 -> 0.0225 against 0.0530 ideal). At 16 ranks it is worth 7%. Bit-identity holds both ways and is pinned both ways: `:sequential` reproduces the pre-change tree at 1/4/8 threads and 1/2/4 ranks, coupled and uncoupled, and `:wavefront` equals `:sequential` to the bit -- the suite compares them on one process (asserting on the RECORDED batch counts, so a path that never batched cannot pass) and the multi-rank child compares them at every rank count. **A 1-ulp trap was found and closed on the way:** `muladd` is "fuse if profitable", so splitting the moment finalize into its own function flipped `_shifted_second_moment` from fused to unfused and moved a coupled collide by 1 ulp with the raw sums bit-identical; 400 of 400 sampled moments in the pre-change tree were FUSED, so the two helpers now say `fma` and the arithmetic is pinned rather than left to the inliner. Every remaining `muladd` in the tree is the same coin. Remaining call-count headroom: the slicing's ~70 per-statistic scalar all-sums vectorize the same way; `_mp_slice_reference` still discards a maximum (30 wasted messages after the hoist, hygiene not speed); `_mp_allsum_impl!` allocates its gather buffer per call. **Step 4b closed 2026-09-05** (record: [`history/multi_process_step4b_2026_09_05.md`](history/multi_process_step4b_2026_09_05.md)): `StrongStrongTask` runs divided. The work was the run holding TWO beams: the shard scope held one `(offset, global_n)` and handed the second beam the first beam's offset, so it now holds one entry per beam keyed by the identity of its `Phase6DRep` (count-keyed lookup is ambiguous in principle -- 256 vs 257 particles give rank 1 of 2 the same 128-particle shard -- and the count form now throws on that case); every reader moved to the representation form; each line tracks under its own beam's `index_offset`; the whole run sits inside one policy scope. The blanket refusal is replaced by per-solver refusals (PIC, gpic, spectral refuse at the task preflight AND at their CPU collide entries until step 4c) and a line-action refusal. Measured under a launcher on two beams of different sizes with radiating lines, line observers and the artifact: the luminosity series is BIT-IDENTICAL at 1, 2 and 4 ranks, fingerprints agree to 2.1e-16 and moment rows to 2.1e-14, only rank 0 wrote, and the one-rank run reproduces `CPUThreadsExecutionPolicy` bit for bit (pinned in-process and through the launcher). (The `batch_mode`-across-solvers row the owner put ahead of 4b closed 2026-09-04 -- record: [`history/batch_mode_one_keyword_2026_09_04.md`](history/batch_mode_one_keyword_2026_09_04.md); every solver reads the keyword on CPU and records a pair-schedule receipt, which 4c's per-solver division can assert on.) **Step 4c, PIC, closed 2026-09-05** (record: [`history/multi_process_step4c_pic_2026_09_05.md`](history/multi_process_step4c_pic_2026_09_05.md)): the PIC collide divides -- each rank deposits its own particles, the padded charge grid is all-summed before every field solve (two planes per direction, three under quadratic interpolation or node mode), the luminosity's two deposits likewise, mesh extents all-reduced so grids and the Green cache are identical per rank, the `:sigma` origin the globally-first member, every skip and every non-finite chokepoint decided from global counts. The defect the first divided run found: `_pic_kbb1` divided by the SHARD's macroparticle count (kicks 1.9x/3.5x at 2/4 ranks). Measured under a launcher at 1/2/4 ranks: bit for bit at one rank on every option route, 7.1e-15 worst across rank counts on the option routes (8.8e-14 on a 64-slice, 4096-pair arm), 72 grid all-sums recorded per two-turn run. At more than one rank the pairs run one at a time on the main thread (`:funneled`); the batched per-wavefront exchange is the performance phase. **The CPU/MPI/CUDA consistency contract for PIC landed 2026-09-05** (same record): `StrongStrongPICMultiProcessConsistencyContract` runs the PIC backend contract's beams and task under the CPU policy in-process, launches `src/contracts/mpi_pic_consistency_child.jl` at each rank count (the child draws the same beams under the multi-process policy, runs the task divided, gathers onto rank 0), compares one rank bitwise and more ranks at rtol 1e-12, and with `cuda=true` runs the CPU/CUDA PIC contract on the same beams and reports the MPI-to-CUDA triangle bound. Measured: bit for bit at one rank; 5.5e-17 abs on the coordinates (2.3e-15 against the beam scale, 0.18 of a 1e-12 pointwise allowance, so the default rtol is 1e-11) and 1.6e-15 rel on the luminosity at two ranks, 5.8e-17 / 1.1e-15 at four; the CUDA leg 5.1e-17 / 9.6e-16, MPI-to-CUDA luminosity bound 2.6e-15. No launcher -> `:skipped` naming the unrun legs; a requested CUDA leg the device cannot run -> `:skipped`, not a pass on two legs. Required by `StrongStrongTask`; the launcher lane asserts `:passed` at 1 and 2 ranks. **Performance phase closed 2026-09-05** (record: [`history/multi_process_pic_scaling_2026_09_05.md`](history/multi_process_pic_scaling_2026_09_05.md)): `pic_cpu_divided.jl` exchanges once per wavefront batch for every pair and both directions and solves every plane ONCE: the nx x ny interiors of the deposits reduce-scatter by whole planes to owner ranks that solve them, the potentials all-gather, every rank takes the gradient -- seven collectives per batch (eight under `:sigma`) for ~24 per pair, none inside a pair, so the pairs run on the worker pool again, the FFT work divided by the rank count and the traffic a third of the all-summed padded planes' -- and the extension's all-sum above 2048 elements is a reduce-scatter (Alltoall + rank-ordered fold + Allgather: 2n per rank instead of nP, a 512 KB plane 6.3 -> 0.25 ms at two ranks). Both are BIT FOR BIT the per-pair exchange, pinned two ways: every two-rank result line of the launcher child byte-identical before and after, and a `:sequential` arm held to the batched default's bits on every gate. The node mesh keeps the per-pair exchange (its three prebuilt planes want their own staging); `:source_slice` never batches. Measured (`profiling/benchmark_collide_cpu.jl`, one thread per rank, cores bound): at the fixed point (640k/256k) 2.47 s serial -> 0.73-0.82 s at 16 ranks (3.0-3.4x), 1.99 s at 64; at the production point (2.56M/1.024M) 6.27 s -> **1.07 s at 16 ranks (5.9x)**, 1.57 at 32, 2.52 at 64 -- the collectives are 41% of the wall at 16 ranks and 84% at 64, because every rank holds a chunk of every slice and must send its partial of every plane and receive every potential (~115 MB each way per collide whatever the rank count); the Alltoall behind the reduce-scatter is the dear one (23 ms per call at 64 ranks, three times the all-gather per byte). Hybrid rank x thread points (16x4, 8x8, 32x2) lose to 16x1. The first cut of this phase (all-summed padded planes, redundant solves) did not scale at all and is recorded. Next levers, measured in the record: overlap the luminosity's exchanges under the next batch (a third of the collective time at 64 ranks), a hand-written exchange for the reduce-scatter, and -- the only way past the per-rank traffic floor -- a longitudinally aligned decomposition, which is an owner decision against the chunk-aligned bit-identity. **Step 4d, the slice-aligned collide, closed 2026-09-05** (record: [`history/multi_process_step4d_sliced_2026_09_05.md`](history/multi_process_step4d_sliced_2026_09_05.md); design reviewed adversarially before it was built, 20 confirmed findings folded in): the owner lifted the chunk-aligned bit-identity for the collide (parity class against serial suffices), so `pic_cpu_sliced.jl` lays the live particles out BY SLICE for one collide -- whole slices per rank below `P <= nslices`, a proportional group of ranks per slice above, migrated in and back by an all-to-all of columns with no index travelling -- and every pair is point-to-point between its two groups and its owners (records to a coordinator, grids from the direction owners after their own Green cache, partial interiors to the owners, potentials to the field members, the luminosity through the coordinator: eight hops per batch, no collective inside a pair). Tracking and everything outside the collide keep the home layout; the collide never writes `z`, so `z` returning bit for bit is the migration's own check, printed per rank. At ONE rank the same code runs with self-delivery and is the CPU collide bit for bit on fourteen option arms (the pin that found the `:sigma` origin defect: an origin is a moving particle). At 2 and 4 ranks every arm is in the parity class (<= 6e-15 on the option arms, 7-9e-14 on the 64-slice arm) and `:sequential` equals `:wavefront` bit for bit. The 4c batched exchange is retired. Measured (`profiling/benchmark_collide_cpu.jl`, one thread per rank, cores bound): production point 6.71 s serial -> **0.88 s at 32 ranks (7.6x)** and 0.90 at 64 (4c: 1.57 and 2.52), fixed point 2.61 -> 0.47 at 16 ranks (5.5x; 32x2 threads 0.52); the curve no longer turns over. At sixteen ranks -- one slice per rank -- production is SLOWER than 4c (1.5-3.1 s against 1.07): a batch's critical path there is one whole slice's deposits and kicks on one rank while its peers wait, and the wavefront's duty cycle idles half the ranks on the average batch; the clocks (every wait named by stage) put 72% of the 64-rank wall in waits, mostly the owners on the coordinator's fold and the tail skew at the end-of-collide all-sums. Re-measured at sixteen ranks with five timed collides: spread over the sockets (`-bind-to socket -map-by socket`) 0.72-1.13 s, level with 4c; core-bound 1.16-1.66; and in every configuration the first timed collide after the two warm ones is 2-3x slower (open item: two warm collides are not enough for this path, cause not yet identified). The contract passes at 1, 2, 4 and 8 ranks (bitwise at one, 5.8e-17 abs / 0.12 of the pointwise allowance / 1.9e-15 luminosity beyond). Next levers, priced in the record: dataflow across batches (a rank starts its next pair when its own inputs are ready; the point-to-point form was built for it -- ~2x at P ~ nslices, removes the 16-rank regression), a data-size rule that lets a whole slice's deposit thread, and cached migration buffers. **Step 4e, dataflow across batches, closed 2026-09-05** (record: [`history/multi_process_step4e_dataflow_2026_09_05.md`](history/multi_process_step4e_dataflow_2026_09_05.md); design reviewed adversarially first, its blocker and must-fixes folded in): the wavefront batch is no longer a barrier. Each rank runs an event loop over its pairs in the collision order, running every stage whose receives have arrived and whose gate is open (relay duties first) and blocking on the union only when a whole scan runs nothing. The gate is read from `order`, never from indices -- the collision time is `-(c1+c2)/2` ascending, so the pair before `(i,j)` on slice `i` is `(i,j+1)`, and the review caught the design gating on a pair that had not run. A buffer belongs to its send until MPI says the send completed; the `:sigma` origins moved into per-pair buffers with codes inside the pair's tag space (their own band collided with the pair band a few hundred pairs in) and the collide checks its largest tag against the communicator's limit; a pair meeting a non-finite extent releases its slices without kicking, so its successors run and every rank throws. Both loops now call the same leaves, so the arithmetic lives in one place. **Bit for bit against the loop it replaces:** the launcher child's every result line on d677931 against this tree at 1, 2 and 4 ranks, zero differing lines; permanently, `:sequential` (batched loop) equals the default bit for bit, an in-process pin forces both loops on the same beams, the `:skewed` and `:sparse` arms run twice, and every arm names the loop it ran. **Which loop is a measured rule, not a preference:** an interleaved A/B of the two on one box puts dataflow 1.4x ahead at 64 ranks, level at 32 and 2x BEHIND at 16 -- a slice's pairs are a chain, so a rank holding a WHOLE slice has nothing to overlap and the loop only adds a wake per hop, while a rank holding a fraction of one has its group's other pairs and its owned planes to get on with. So groups wider than one rank run the dataflow loop and whole slices per rank the batched one. **The warm-up closed 2026-09-05** (record: [`history/multi_process_pic_allocations_2026_09_05.md`](history/multi_process_pic_allocations_2026_09_05.md)): one allocation profile named the whole of it in a single run -- the one-rank passthrough copying every send (250-290 MiB), the virtual positions rebuilt per pair (86-94), the migration rebuilding its columns, permutations and slice states (122-171). All three are pooled or held by reference now, and the collide fell from **0.149 GiB per rank per collide to 0.004-0.013**, with the first timed collide now within noise of the last where it had run 2-4x slower. It also shrank the schedule gap 4e had measured: re-run afterwards, the two loops are within a few percent to a quarter of each other (medians) and tied on best-of-six above sixteen ranks, so 4e's layout rule stands on a much smaller margin and its record carries the correction. **Step 4f, Gaussian-PIC, closed 2026-09-05** (record: [`history/multi_process_step4f_gpic_2026_09_05.md`](history/multi_process_step4f_gpic_2026_09_05.md)): the second solver on the slice-aligned transport. The transport grew one seam, dispatched on the solver -- its record's length, whether it needs the slice's first member at every pair, and four leaves (record, owner mesh, plane solve, kick) -- and everything else is shared; the refactor is bit-neutral for PIC and that is CHECKED, the child's PIC lines byte-identical to d677931's at 1, 2 and 4 ranks. gpic contributes three things, each the SLICE's: fourteen shifted sums per member about the slice's globally-first member, folded in group rank order (and so gpic needs the origin exchange at every pair, where PIC needs it only under `:sigma`); the Gaussian subtraction ON THE PLANE'S OWNER after the fold, because with `neutralize=true` the amplitude is the deposited grid's total and a per-rank residual is not defined; and the control-variate mode, decided once on the owner and carried in the grids message with the moments, the global count and the boundary drifts, because the field members need the source slice's moments for the analytic add-back and are not in the source group. Measured: bit for bit at one rank on all five option routes, 4e-16 to 1.4e-15 at two and four ranks, the same parity class as PIC. **Step 4g, spectral, closed 2026-09-05** (record: [`history/multi_process_step4g_spectral_2026_09_05.md`](history/multi_process_step4g_spectral_2026_09_05.md)): the last solver, and the one with TWO collides, which divide by different means. The 6D map (`longitudinal_kick=true`, the default and the production route) is order-DEPENDENT -- a slice is kicked by the pairs before it and then serves as a source at its kicked positions -- so it takes the slice-aligned transport after all (`spectral_sliced.jl`); the earlier reading that spectral is "order-free" was taken from the TRANSVERSE route alone and every clause of it is false of the default one. Spectral negotiates almost nothing (ONE Dirichlet box for the whole collide), so stages 0 and 1 of the PIC loop do not exist and the solver of a directed interaction is the FIELD slice's group head rather than a dealt third party: the source group sends its two drifted deposits, the head folds and solves, and the potentials never leave the group that needs them -- two messages per direction per pair at `P <= nslices`, no broadcast. Dealing the solve out is the point: one DST solve is ~4x the mesh evaluation it feeds (271 us against 64 us at 64x64, 6000 particles), so redundant solving would cap the speed-up near 1.2x. The transverse map stays on the HOME layout (order-free, no migration): one all-sum for every source slice's plane, a second for the dealt mesh solves, one all-max plus one all-sum per group of field slices for the luminosity, and the kick never leaves a rank. Global scalars both routes needed: the box's rms COUNT and extrema (the non-finite verdict agreed as an integer count FIRST, because a NaN into `_mp_allminmax` is rank-divergent), and `_spectral_luminosity_scale`, which divided by `length(beam.rep)` -- the 4c shard-count defect, live in spectral until now. Measured: bit for bit at one rank on both routes and both methods, ~1e-15 at two and four ranks; 6D scaling at 90k/beam, 15 slices, 64x64, best of five on a shared box: 451 ms undivided, 497/372/230/130/74 ms at 1/2/4/8/16 ranks (6.7x), turning back up to 227 ms at 32 ranks -- more ranks than slices, its own row below. `_reject_undivided_solver` now has nothing in the roster to refuse and became a DENY-BY-DEFAULT tripwire (`_solver_divides`: `false` for `AbstractPoissonSolver`, `true` only where someone divided the collide and said so). **NEXT:** the chain at P ~ nslices, the deposit's threading rule, the node mesh on the sliced layout, the spectral 6D map above nslices ranks (32 ranks over 15 slices ran 227 ms against 74 ms at 16 -- every slice becomes a group there, and the dataflow loop of 4e is the candidate), the spectral batch's FIVE barriers (the luminosity's extents could ride the deposit wait and its mesh the payload wait, cutting two of them; `wait_deposits` was 60 ms of a 131 ms collide at eight ranks), and the campaign's neighbour audit. When the last solver divides, the campaign's targeted neighbour audit falls due -- deferred to that point by owner decision rather than run per step, and its accumulated blast radius is kept in the "Targeted neighbour audit of the multi-process campaign" row. Still refused at nranks>1: task and line ACTIONS only -- a callback Octopus cannot reason about. Every solver in the roster divides. **Superseded: step 3** (weak-strong sharding) -- it must choose the shard rule (chunk-aligned `P | 64` vs lane-aligned `P | 4096`; the design note lists what each buys), carry a global index offset for the counter RNG, replace lengths that enter physics with global counts, broadcast every decision that gates a collective, and give rank 0 the artifact. **Step 1 closed 2026-09-04** (record: [`history/weak_strong_allocation_2026_09_04.md`](history/weak_strong_allocation_2026_09_04.md)): the 1.282 GiB/turn was ONE site -- the sliced strong beam built a fresh mutable `ThinStrongBeam` per slice per particle, 7 x 192 B = 1344 B per particle per turn (the Phase 0 record's "1.25 KB" is the decimal reading of the same number), 97.5% of profiled bytes and all but the per-call constants of the line's allocation, on both loop copies (fused and elementwise luminosity). An isbits `ThinStrongBeamSlice` carrier removes it with the kick arithmetic untouched: production digest 0xbb09efa9e44e2017 unmoved at 1/4/8/16/32/64 threads, allocation 1.282 -> 0.000 GiB/turn and GC share 3-53% -> 0% at every thread count, s/turn 2.23 / 0.69 / 0.45 / 0.65 / 0.63 / 0.73 -> 2.09 / 0.58 / 0.28 / 0.17 / 0.11 / 0.085 at 1/4/8/16/32/64 threads (8.6x at 64; the 8-16-thread cap is gone and the curve is monotone to 64, same protocol, same box). Pinned: "CPU weak-strong strong-beam tracking allocation does not scale with the particle count" (growth 2.000 unfixed, 1.000 fixed) and a `GaussianStrongBeam` arm on the weak-strong thread-invariance pin; `profiling/benchmark_track_cpu.jl` carries the localisation mode (`OCTOPUS_BENCH_ALLOC_PROFILE=1`). The Phase 0 weak-strong P x T inputs are superseded by the after-curve in the record. NEXT: step 2 (policy type + collective seam), which starts by resolving the two environment traps below by test. Phase 0 record follows. Owner-directed. Scope: SINGLE NODE (owner decision 2026-08-19); the per-rank core is reused unchanged — the policy COMPOSES `CPUThreadsExecutionPolicy`, and 1-rank MPI must reproduce today's digests bit-for-bit. Phase 0 measured (record: [`history/multi_process_phase0_2026_08_19.md`](history/multi_process_phase0_2026_08_19.md); instruments: `profiling/benchmark_track_cpu.jl`, `profiling/mpi_collide_prototype.jl`): weak-strong tracking is allocation/GC-bound (1.282 GiB/turn at every thread count, GC 3.2%→30.8%→52.2% at 1/16/64 threads, optimum 16, digest thread-count-invariant) — the pre-fix-1 disease, never treated on this path; strong-strong PIC at 8 ranks x 8 threads socket-bound is **1.86x** over socket-bound 1x16 (2.23x over unbound; 41.0 → 2.6 → 1.15 s/turn lineage), socket binding alone is 17–22% free, and 2x32 loses to 2x16 at perfect locality (the 16-thread optimum is per-process). Transport decided: MPI.jl as weakdep extension over a ~6-function collective seam (serial passthrough in core); Distributed closed by measurement — the deterministic rank-ordered collective already costs 0.10–0.35 s/turn (O(P) fold) at 2–8 ranks over MPI shm, and Distributed's per-message overhead on 450 collectives/turn would eat most of a 2x gain. Decided order: (1) weak-strong allocation extraction, (2) policy + seam with communicator-read receipts, (3) weak-strong MPI (task-level seams only, rank-0 artifact), (4) strong-strong per solver (soft-Gaussian → PIC deposit→solve all-sum → gpic → spectral); determinism: fixed-P bit-repeatability pinned, cross-P at CPU/CUDA parity tolerance class, lane-aligned sharding where `_SLICE_FOLD_LANES` makes cross-P bit-invariance cheap. Two measured env traps gate Phase 1 (HDF5_jll flips to an absent mpi+openmpi artifact when MPIPreferences is visible in the load path; HDF5's own MPIPreferences dep then locks MPI.jl to MPICH_jll defaults — so the prototype ran MPICH_jll under hydra, bb Open MPI unreachable in-process; resolve by test: matching MPI HDF5_jll variant vs pinned serial HDF5 vs accepting MPICH_jll). Open riders: gaussian collide flatline at 2x16/4x16 (2.42/2.46/2.52 despite halved/quartered per-rank particles, then 2x at 8x8 — something per-pair and particle-count-independent dominates); gaussian comm phase 3–6x PIC's for identical code (suspected idle-thread spin vs the serial fold, unprobed); the O(P) fold wants interleaving now and a deterministic tree past ~16 ranks. |
| Targeted neighbour audit of the multi-process campaign | **open (2026-09-04, owner-directed); DEFERRED BY DESIGN until MPI is implemented for ALL Poisson solvers** | The standing obligation is one neighbour audit per campaign (`comprehensive_audit.md`, "The targeted neighbour audit"; owner direction 2026-08-08), and the multi-process campaign is still open -- steps 4b (wire `StrongStrongTask`) and 4c onward (PIC, gpic, spectral) are unlanded. Owner decision 2026-09-04: run it ONCE, after the last solver divides, rather than per step; auditing a moving seam five times would re-walk the same call sites with different answers each time. This row exists so the blast radius accumulated so far is not reconstructed from git when that day comes. **Surfaces the campaign has already touched:** the seam and shard helpers plus the `_run_logical_workers` one-thread inline path (`src/policies/Policies.jl`) and `ext/OctopusMPIExt.jl`; the resolved-policy slot and the sharded `Beam` constructor (`src/beam/Beam.jl`); `TrackingContext.index_offset` (`src/track/Track.jl`, `src/track/phase6d_track.jl`) and the aperture's `particle_id - ctx.index_offset` slot table (`src/elements/aperture.jl`); the isbits `ThinStrongBeamSlice` carrier (`src/elements/strong_beam.jl`); task-level activation, refusals and post-run accounting (`src/tasks/Tasks.jl`, `RunArtifact.jl`, `BeamObservers.jl`, `BPMObserver.jl`); the globalized slicing statistics, histogram and slice moments plus the 2026-09-04 split of `_slice_transverse_moments` into packable pieces (`src/tasks/strongstrong/slicing.jl`); the CPU wavefront schedule (`gaussian.jl`); the option-effectiveness device filter (`src/contracts/Contracts.jl`); and, from the `batch_mode`-one-keyword row closed 2026-09-04 (record: [`history/batch_mode_one_keyword_2026_09_04.md`](history/batch_mode_one_keyword_2026_09_04.md)), the CPU `batch_mode` reads and `:pic_pair_schedule` receipts in `pic_cpu.jl` and `gaussian_pic.jl`, the CUDA schedule receipts in `pic_cuda.jl` and `gaussian_pic_cuda.jl`, the new spectral field, schema, report and the rewritten `_spectral_collide_longitudinal!` with its collision-order luminosity fold (`spectral.jl`, `spectral_cuda.jl`); and from step 4b (2026-09-05, record: [`history/multi_process_step4b_2026_09_05.md`](history/multi_process_step4b_2026_09_05.md)) the representation-keyed shard scope (`_ShardEntry`, `_with_beam_shards`, both `_mp_current_shard` forms in `src/policies/Policies.jl`) and every reader that moved to it (`Tasks.jl`, `strong_beam_track.jl`, `BeamObservers.jl`, `slicing.jl` incl. the `_live_z_stats` shard argument, `gaussian.jl`), the per-beam tracking contexts and the two refusals in `interface.jl`, the `_reject_undivided_solver` hooks in `pic_cpu.jl`, `gaussian_pic.jl` and `spectral.jl`, the global emptiness and liveness refusals at the top of `longitudinal_slices` (a rank-local refusal there hung its peers; reproduced under a launcher by the 4b review), the `:funneled` tripwire in `_record_collective!`, the `PredicateSchedule` refusal on observers in both tasks (`Tasks.jl`), the soft-Gaussian `collide!` scoping its own beams (`gaussian.jl`), and `profiling/benchmark_collide_cpu.jl` scoping both beams. **Neighbours the audit must re-walk, each with the property the change was about:** (1) every `_slice_transverse_moments` caller -- `gaussian_pic.jl`, `spectral.jl`, `pic_cpu.jl` and the contracts share the function that was split, and they got the arithmetic by inheritance rather than by test; (2) **every `muladd` in the tree**, which is the session's sharpest lesson -- fusion is a codegen decision, so any refactor near a caller can move a result by 1 ulp, and only the two shifted-moment helpers have been pinned to `fma` so far; (3) the CPU/CUDA twins of everything above (`pic_cuda.jl`, `gaussian_pic_cuda.jl`, `spectral_cuda.jl`); (4) every `_run_logical_workers` caller, since the one-thread inline path rests on "each worker writes its own slot" being true of ALL of them and that was argued once, not checked per caller; (5) every reduction that reads a length as the beam's rather than the shard's (the `n` vs `n_local` segfault class); (6) every collective reachable from outside `_with_execution_policy` (the step-3b silent-no-op class, which shipped once already); (7) consumers whose receipt name is backend-tagged while their option is now cross-backend -- the one instance found so far, `:cpu_pic_pair_schedule`, became `:pic_pair_schedule` on 2026-09-04 when the PIC solvers' `batch_mode` widened; re-walk for any new one. **Properties to re-run on the neighbours, not just the changes:** "a lost particle is excluded from every reduction, exactly"; fixed-P bit-repeatability and cross-P parity; a one-rank MPI run reproducing `CPUThreadsExecutionPolicy` coordinate for coordinate; and schedule bit-identity wherever a solver gained a batched route. Precedent for shape and scale: [`history/neighbour_audit_2026_08_07.md`](history/neighbour_audit_2026_08_07.md) (it found two real defects, both in the SIBLING file of the fixes it audited). Fan the read-heavy sweeps out; fix what is small and squarely inside the blast radius; price and ledger the rest here. |
| Tracked `Manifest.toml` predates `Project.toml` | **open (2026-09-04)** | Every `Pkg.test` prints "The project dependencies or compat requirements have changed since the manifest was last resolved" and re-resolves into a temp env (the step-1 gate ran with CUDA 6.2.1 -> 6.3.1, JLD2 0.6.5 -> 0.6.6, SpecialFunctions 2.8.0 -> 2.9.0 re-resolved). The tracked Manifest was last committed 2026-08-04; Project.toml gained the `Logging` test dep on 2026-08-06. The development guide's claim that the Manifest "reproduces the exact environment" is false until it is re-resolved and committed; class "docs plus anything else" -> full gate, and the gate log should then show no re-resolve banner. **Blocked on this box (measured 2026-09-04):** `Pkg.resolve()` fails before reaching anything else -- `CUDA_Runtime_jll` is pinned to 0.23.0 by an explicit requirement while `CUDA_Driver_jll` 13.3.0 admits only [0.1.0, 0.19.0-0.21.0], so the two cannot be satisfied together. Resolving needs that pin revisited (it is the GPU box's runtime selection), which is why multi-process step 2 left the Manifest alone and reached its test dependency through `[targets] test`. |
| Run-artifact niceties deferred at step 4 | **open (2026-08-18)** | The artifact campaign closed 2026-08-18 (all four steps; full record: the archive’s "The run artifact: one output file per task" row and [`design/run_artifact.md`](design/run_artifact.md)). Three recorded niceties were deliberately deferred, none load-bearing. ~~s-position attributes on probe groups~~ **landed 2026-08-19**: probes stamp the design’s `attrs: name, s` at bind — the position comes from the ONE arc traversal (`_collect_spec_s!`, the U11-1 walker, gaining a probe arm that keeps the zero-length-advances-nothing contract), carried observer-keyed through a prepare scope so both tasks’ different prepare chains need no signature change; the reader surfaces `s` beside the columns (`nothing` for task-hook probes and older files). Pinned: WS line probes at 1.0/1.5 with a hook probe reading nothing, and SS probes reading their OWN line’s arc length (2.0 vs 3.0). Remaining: element names for the weak-strong luminosity channel labels (positional `strong_beam_<i>` until runtime elements carry names; the luminosity channels’ own `s` attribute belongs with this, the producer-identity work), and the live text mirror as a first-class option (the design’s `tail -f` posture; today nothing streams text). |
| CUDA in-suite vs standalone last-bit divergence: mechanism unknown | **open (2026-08-19)** | The distilled open question from the wobble-tally row's observations 10-12 (evidence and receipts live THERE; this row is the investigation ticket). Established: for the pinned `Random.Xoshiro(29)` 30,000-point vector, `_cuda_live_z_stats`'s masked-arm sigma is 0.007009746250353312 in ANY standalone process and 0.007009746250353311 (1 ulp low) in ANY full-suite process — deterministic on both sides, reproducible in-process (discriminator receipt: recompute matches first GPU value), with n_live/zmin/zmax/mean bitwise equal in all contexts. The kernel (`_cuda_lane_z_moment_kernel!`) is launch-shape-invariant by construction (stride is the compile-time `_SLICE_FOLD_LANES`, lane content a function of lane id only, host fold serial); no fastmath/math_mode setter exists in src/ or test/; so SOMETHING in suite execution history changes the compiled arithmetic or device context for the Val(2) pass. Investigation plan, in order of information per hour: (1) dump the 4096 `lane_sums` for the pinned masked arm in both contexts and diff — all-lanes-equal localizes the flip to the host fold/final scalar ops; few-lanes-moving names the particles and the arithmetic; (2) if lanes move, compare the kernel's PTX/SASS between contexts (`CUDA.@device_code_ptx`) — a codegen difference is then directly visible; (3) if PTX is identical, bisect the suite prefix (binary search over testset ranges ahead of "Slice geometry") for the state-setter. Member 2 of the family (the gpic A/B two-profile coin, bit-exact across processes) plausibly shares the same root: whatever state distinguishes contexts may also pick between its two profiles — check both once (1) lands. The discriminator in the "Slice geometry" testset logs every occurrence meanwhile; the 1-ulp bound (owner decision 2026-08-19) keeps the gate honest without hiding the phenomenon. |
| The observer schema-vs-report check is an identity for derived reports | **open (2026-08-06, U4-18)** | `validate_configuration_metadata` compares each observer's option-schema keys against its `configuration_report` entry names. That is a real check for the five observers whose reports are hand-written tuples, and an IDENTITY for any observer that derives its report by iterating the schema — `configuration_report(::BPMObserver)` does exactly that, so it passes for any possible schema, including a wrong one. A "every option names a field of the observer" substitute was tried and reverted: an option is a CONSTRUCTOR KEYWORD, not a field, and `BeamMomentObserver`, `JLD2BeamMomentObserver` and `MomentObserver` all legitimately expose `capacity` while storing `buffer_capacity`, so the check failed on correct code. Making this non-vacuous needs the constructor's keyword set, which Julia does not expose directly — `Base.kwarg_decl` on the method is the usual route and is fragile across versions. The other half of this lead (a `StrongStrongTask` default compared against a hand-written literal rather than the real constructor) is already closed by U5-4. |
| Three metadata-validator checks are tautologies | **open (2026-08-06, U12-8)** | `validate_element_metadata`'s friendly-schema, friendly-construction_help and friendly-example-kind checks compare two values that both route through `_element_meta_or_nothing`, which returns the identical `ElementMeta` object for a spec type and its friendly constructor: measured, 30 of 30 kinds resolve to the SAME meta and none to a different one. So the comparison is `x == x` for every registered kind. The checks are not useless — they bite when `register_friendly_alias!` points a friendly type at a different meta, which is confirmed to be caught — but that is alias misregistration, not the schema agreement their error messages claim, and the messages should say so. Making them non-tautological needs the friendly constructor's own keyword set, the same obstacle recorded for U4-18. |
| Three metadata channels are unvalidated free text | **open (2026-08-06, U12-6)** | `ParamMeta.unit`, `physics_keywords` and `description` are presented as authoritative and none is checked for truth. `unit` is only ever concatenated into help output; keywords are checked for MEMBERSHIP in `ALLOWED_PHYSICS_KEYWORDS` but never for accuracy; `description` is stored and echoed. Measured by injection: `unit="furlong"` on a length, `[:radiation, :beam_beam, :collimation]` on a plain kick, and `description = "Thick quadrupole magnet with fringe fields"` on a THIN element all pass unnoticed. AGENTS.md stakes agent orientation on exactly these fields ("Agents use these tags to locate related implementations and examples"), so this bounds what "self-describing source" currently guarantees. No automated consumer acts on them today, which is why it is Low. A dimensional check on `unit` against the parameter's use is the only one of the three that looks mechanically checkable; keyword and description accuracy are semantic and would need either a curated cross-check or an agent pass. |
| `validate_configuration_metadata` enumerates types by hand | **open (2026-08-05 row remainder)** | The remainder of the "metadata validator caught 1 of 13" row after its later closures (declared defaults are now runtime-checked per U12-1; declared-parameter-is-read is the effectiveness contract): the configuration validator's hardcoded type enumeration means a new solver or observer is unchecked until someone edits the tuple, and `Contracts.jl` carries a second hand-copy of the solver list — the same shape U9-2 closed for elements, still open for configurations. Measured staleness when written: `GaussianPICPoissonSolver` absent entirely, `BPMObserver` absent, no task-level schema. Full record: the archive's "element metadata validator" row. |
| CPU threading: remaining measured headroom | **open leads (2026-08-10 campaign remainder)** | The campaign closed at PIC 41.0→3.0, gpic 65.5→6.8, spectral 5.0→2.6 s/collide with bit-identical digests; the ceiling is memory bandwidth, measured three ways. Remaining leads, by measured headroom: `FFTW.set_num_threads` is still nowhere (threaded FFTW plans reassociate — measure the bit cost before adopting), and further gains need less traffic per unit of work, not finer division of the same traffic. The CPU **6D spectral** performance campaign (four boundary solves per slice pair) remains DEMOTED (2026-07-24) but open. Running record: [`history/cpu_threading_2026_08_09.md`](history/cpu_threading_2026_08_09.md) and the archive's CPU-threading row. |
| Bmad reference cases for `misalign_convention=:bmad` | blocked on a tool | The **default** convention has no reference case: its reference point, rotation order, and now the frame a misalignment is quoted in against a `ref_tilt` are all read from Bmad source, never measured. `:madx` is pinned at ~5e-13 throughout. Needs Bmad in the validation path; one comparison would close all three. *(2026-08-14: a fourth rider folds in — an external reference case for `ThinAcceleratingCavitySpec`, which MAD-X cannot express (no reference-energy-changing cavity) and Bmad's `lcavity` can.)* *(2026-08-15: a fifth rider — a PURE curved quadrupole reference, `h != 0` with `b0 = 0`. MAD-X 5.03.06's SBEND `k0` is measured IGNORED by its PTC translation (outputs bit-identical with k0 absent, 0, 0.12, 1e-6), so `h != b0` has no spelling there; Bmad's independent `g` vs field would express it. The h = b0 curved channels ARE now PTC-pinned — `cfbend_quad_heavy` at the polygon working point, `cfbend_skew` for the odd Psi_1 branch, `cfbend_k3`/`cfbend_k5` for high orders through EFCOMP — all at ~5e-13.)* |
| Spectral rms CPU/CUDA lane-fold 1-ulp counterexample | **open (2026-08-18, one observation; owner-directed note)** | The N5 bit-identity pin ("The spectral rms and Dirichlet box are bit-identical across backends") failed ONCE, at `_masked_rms(v, nothing)`: CPU 9.94237577349687e-5 vs CUDA 9.942375773496871e-5 — 1 ulp — for a specific 30,000-point `randn` vector. Established: the two lane folds read as identical by construction (same `_SLICE_FOLD_LANES` partition, same in-lane accumulation order, same serial host fold — which SHOULD force bit equality for every input); 0 of 5000 fresh draws reproduce a mismatch (95% upper bound ~6e-4/draw); systematic FMA contraction is excluded by that rate (it would mismatch nearly every draw). The offending vector was NOT capturable: the test drew from the UNSEEDED task RNG (`set_global_rng!` seeds only the Octopus counter RNG), so the vector was a deterministic function of the suite configuration — dozens of green gates tested one lucky draw, and the run-artifact campaign's testset edits shifted the stream onto the counterexample, failing every post-campaign full gate deterministically while standalone probes passed. The FLAKE is closed (the test now draws from a local `Xoshiro(61)`; pinned vector verified equal on both backends under check-bounds). The open QUESTION is the mechanism: a rare rounding-boundary corner of an unnoticed codegen asymmetry, or a hardware transient — this GPU runs with ECC DISABLED (`nvidia-smi -q`), so a single flipped low mantissa bit in one lane partial is a live hypothesis for a one-off. Actionable leads: (1) if ANY bit-identity pin mismatches again, capture the data first (write `v` to a file before comparing) — with the vector in hand the divergence is a mechanical lane-by-lane trace; (2) a bulk differential hunt (millions of draws, or adversarial vectors built to sit on rounding boundaries of the lane partials) would separate "rare structural corner" (reproducible once found) from "transient" (never reproducible); (3) if structural, compare the PTX of `_cuda_lane_z_moment_kernel!` against the CPU loop's asm for the s2 pass before trusting either. *(TALLY, 2026-08-19 -- this row is the ledger for CUDA near-identity pin wobbles. Second member: "CUDA GaussianPIC singular-reference fallback matches PIC" failed in-suite THREE times the same night (gates at atol=2e-18 twice on different assertion sites, then once ABOVE a derivation-raised atol=1e-16 with identical printed array ends -- which kills any small-wobble account, since a wrong route diverges at O(kick)~1e-15 and rtol=2e-12 alone would catch it). Measured: 20 repetitions x both arms under the suite's own --check-bounds/--threads are BIT-EXACT, worst abs diff 0.0, before AND after an artifact-carrying strong-strong task -- so the trigger is suite state my probes do not reproduce. Surviving hypotheses: a hidden mid-array NaN (isapprox is false against NaN at ANY tolerance and truncated printing hides the middle), or a gross mid-array divergence, either way appearing only under suite history. Acted on per this row's own lead (1): the pin now CAPTURES on mismatch -- both arrays dumped whole to a kept tempdir with a route/column tag before the comparison discards the evidence (`checked()` in the testset) -- and the raised atol stays with its derivation recorded in the test comment. The next recurrence, local or CI, is a mechanical diff. Four observations total on this row, all 2026-08-18/19, none reproduced in isolation; the capture trap is the instrument the row now waits on.)* *(FIFTH+SIXTH observations and the trap's FIRST FIRING, 2026-08-19, during the multi-process Phase 0 full-suite gate: 2 of 49 fail in "CUDA GaussianPIC singular-reference fallback matches PIC", both on `route_wavefront_true` column 4 (py), one per beam. Evidence committed whole: [`history/gpic_wobble_capture_2026_08_19.txt`](history/gpic_wobble_capture_2026_08_19.txt). The mechanical diff KILLS two hypotheses: no NaN or Inf anywhere in either array (the hidden-NaN account is dead for this recurrence), and no gross divergence (max |diff| = 2.9e-16). What it is instead: 13 of 64 py entries differ, at CONTIGUOUS center-of-array indices, on the zero-marginal-width route where symmetry puts py at pure roundoff -- both arrays are SMOOTH ~1e-16-scale noise profiles (values rise and fall coherently with index), differing by up to 2.9e-16, and the array-isapprox atol=1e-16 sits below the ~6e-16 norm of that difference over 13 entries. Thirteen coherent smoothly-varying entries also kill the single-bit-flip/ECC account for THIS failure: the two solvers computed DIFFERENT deterministic roundoff-noise profiles in-suite while being bit-exact standalone, 20 of 20. New lead from the shape: whatever differs between suite state and standalone state must be compilation or planner state on this route -- check whether the field/Green FFT planning the fallback reaches (FFTW wisdom or MEASURE-class planning on host-side pieces, CUFFT plan selection/reuse across the pic-then-gpic calls) is machine-state-dependent; a state-dependent plan legitimately moves last-bit noise and would explain suite-only failure with standalone bit-exactness.)* *(SEVENTH observation, the gate re-run the same evening -- and the mechanism narrows to a BINARY coin. Same testset, 2 of 49 again, but different sites: `route_wavefront_true` b1c4 AND `route_sequential_true` b2c4, one beam each. The decisive datum: the second firing's wavefront b1c4 arrays are VALUE-FOR-VALUE the first firing's with expected/actual SWAPPED (appended to the same evidence file) -- so exactly TWO reproducible py-noise profiles exist for this route, and EACH collide call draws one of them. The pin fails iff the paired PIC and GaussianPIC calls straddle the draw, which they can only do if per-process state changes BETWEEN two closely spaced collides in one testset. Refined model, consistent with every observation on this row: a CUFFT-class handle (plan) evicted and recreated between the paired calls picks the other of two algorithms with different last-bit rounding -- CUDA.jl frees cached handles under GPU memory pressure, late-suite the pool has grown into most of free VRAM, and standalone processes never evict (hence 20/20 bit-exact both arms: a per-process draw can never fail a WITHIN-process comparison). Also fits the 2026-08-18 onset (the artifact campaign grew the suite's GPU footprint) and today's elevated rate (a 26-day IJulia kernel is holding 3.3 GiB of the 24.5 GiB card, shrinking headroom -- candidate, not proven). Directly testable: in one process, force pool pressure (allocate/free large CUDA arrays) between the paired collides and watch for the flip; and compare the two profiles' FFT stages. If confirmed, the fix conversation is about plan-cache pinning for paired-comparison tests or a derivation-based atol for a zero-symmetry column, not about tracking down a phantom.)* *(EIGHTH observation, gate attempt 3 the same evening: `route_wavefront_false` b1c4 alone, 48/1 -- the THIRD distinct route, and the decisive bitwise result: across all three suite processes the b1c4 pair is drawn from EXACTLY TWO bit-identical profiles, gate1 (expected,actual)=(A,B), gates 2 and 3 =(B,A), every vector equal to its counterpart to the bit; norm(A-B)=6.3e-16 against the pin's atol=1e-16. Two bit-exact profiles across processes EXCLUDES free-running atomic reordering as the whole story at this size (n=64, grid 16x16): the recorded atomic-deposition account survives only in its two-orderings-of-one-contended-pair form, which is also the simplest model that fits everything. Killing a 26-day 3.3 GiB IJulia squatter mid-evening did not stop the coin (observation 8 came after), so external VRAM headroom is not the trigger's dominant term. Owner decision (2026-08-19): the pin STAYS at atol=1e-16; posture is re-run-until-aligned, tallying each red here. The measured wobble norm 6.3e-16 vs atol 1e-16 means a straddling draw ALWAYS fails the gate; per-gate straddle rate tonight 3 of 3 (routes straddling: 2, 2, 1 of 3). Evidence file carries all three firings whole.)* *(NINTH observation, gate attempt 4: `route_wavefront_true` b1c4 alone, 48/1, (expected,actual)=(A,B) -- the SAME two profiles a fourth time, no new profile has ever appeared. Straddle counts per gate: 2, 2, 1, 1 of the four zero-width/rank-one comparisons; four consecutive red gates under the re-run-until-aligned posture. Gate attempt 5 aligned -- zero straddles, zero captures, suite green whole -- and carried the Phase 0 commit; the straddle series 2,2,1,1,0 is tonight's measured coin.)* *(DECISION REVISED, 2026-08-19 later the same evening, owner: the re-run-until-aligned posture above is superseded -- the pin's atol is raised 1e-16 -> 2e-15 with the measured derivation (3x above the wobble norm 6.3e-16, 4x under the wrong-route wholesale floor ~8e-15 in norm, rtol=2e-12 untouched and still the wholesale-divergence catch). The capture trap stays armed at the new bound: any future firing is a NEW phenomenon, not the tallied A/B coin, and this row remains its ledger. Derivation recorded beside the original in the testset comment.)* *(TENTH observation, 2026-08-19, the atol-change gate itself -- and a THIRD family member: "Slice geometry is bit-identical across backends" failed 57/1 at `sc.sigma == sg.sigma`, CPU vs CUDA `_live_z_stats` sigma 1 ulp apart (0.0070478337686169625 vs ...63) on a 30,000-point vector. Same defect class as member 1 to the letter: `set_global_rng!(seed=29)` seeds only the counter RNG, so the bare `randn(n)`/`rand(n)` drew from the PER-RUN Test RNG -- 1-ulp roulette, unlucky draw. Reproduction handle printed by Test: outermost Xoshiro(0x805ec498f9fbe4ef, 0x8235fa97f158ef61, 0x2f8e7e3431c095e9, 0x8ab97dc77a8d52d3, 0xbfdb9bda20a6e464). Fixed by member 1's own precedent: the draws now come from a local Random.Xoshiro(29), and the pinned vectors are verified bitwise equal on both backends, both mask arms, under --check-bounds. The gpic pin at its new atol=2e-15 passed this gate cleanly -- the two changes are independent. Standing lesson sharpened: EVERY bit-identity pin whose input comes from a bare randn/rand is playing this roulette; sweep candidates when one next fires.)* *(ELEVENTH observation, 2026-08-19, the very next gate -- and it changes the member-3 account: the PINNED Xoshiro(29) vector ALSO failed sigma by 1 ulp (CPU 0.007009746250353312, CUDA ...311, masked arm), while the same vector is verified bitwise equal on both backends standalone under --check-bounds, both arms. So member 3 is NOT (only) draw roulette: on a FIXED input, in-suite CUDA sigma moved one ulp while in-suite CPU matched standalone exactly. Mechanism narrowed by reading: `_cuda_lane_z_moment_kernel!` is launch-shape-INVARIANT (stride is the constant `_SLICE_FOLD_LANES`, lane content a function of t only, host fold serial), and the MEAN passed bitwise, so the flip lives in the Val(2) lane pass or its 4096-element host fold -- ambient launch config is exonerated for this kernel. That shape (a single lane partial off by one low bit, once, unreproducible standalone) is the row's original ECC-off transient hypothesis, now with two firings in consecutive gates after ~4 h of continuous GPU load (card read 32 C idle immediately after, so not heat-soaked at rest; load-time state unknown). DISCRIMINATOR ARMED in the testset: on any sigma mismatch the device stats are recomputed in-process and logged -- a transient cannot repeat, ambient state must; the next firing answers the row's central question mechanically. Note the family split: member 2 (gpic A/B) is bit-exact two-state = NOT a transient; member 3's evidence now leans transient. They are different phenomena sharing one ledger.)* *(TWELFTH observation, the very next gate -- THE DISCRIMINATOR ANSWERED: recompute_matches_first_gpu = true, recompute_matches_cpu = false, with the SAME pair of sigma values as observation 11 (0.007009746250353312 CPU / ...311 CUDA, masked arm). The transient account is DEAD for member 3: the in-suite CUDA value reproduces deterministically within the process, and across processes. Member 3 is now fully characterized as deterministic per-process state: the CUDA masked-sigma arithmetic in-suite differs from standalone for boundary-sitting inputs -- no math_mode/fastmath setter exists anywhere in src/ or test/ (grepped), the kernel is launch-shape-invariant by construction, and the mean passes bitwise, so the mechanism is still open (compiled-code or device-context state; the next instrument is a lane_sums dump in both contexts to see WHICH of the 4096 lanes move). OPERATIONAL consequence: the pinned Xoshiro(29) vector sits ON the flip boundary, so the suite now fails DETERMINISTICALLY -- and reinterpreting tonight's roulette record (5 green, 1 red on fresh draws), roughly 1 in 6 random vectors sits on the boundary under suite state. The pin's literal claim (CUDA sigma BITWISE equals CPU in any process state) is measured FALSE; the true invariant is: count/min/max/mean bitwise equal, sigma within 1 ulp. Owner decision (2026-08-19): the sigma assert becomes a <= 1 ulp bound with this derivation recorded at the assert; count/extrema/mean stay bitwise; the pinned Xoshiro(29) vector stays (now valuable precisely BECAUSE it sits on the boundary -- it exercises the 1-ulp path deterministically in-suite); the discriminator stays armed and logs every occurrence. The open mechanism question keeps this tally entry live: WHY the in-suite process computes a different last bit than a standalone process for the identical kernel and input -- next instrument is a lane_sums dump in both contexts.)* |

## Open questions from studies

- **Observable-matched slicing nodes (Phase 3c, one experiment):** minimize the
  kick error (or a luminosity-weighted error) directly over `{z_k, w_k}` at
  fixed `ns` to get the true lower bound the slicing rules should be scored
  against. The Gauss-Hermite result is direct evidence that source-fidelity
  objectives and kick accuracy come apart. Per-machine result, acceptable for a
  fixed design point; hypothesis, not established practice.
- **Hadron-side slicing stopping criterion:** Furman's `Q ~ 4/sqrt(tau)`
  applies to the electron ring; the proton ring has no comparable `tau`, so
  nothing masks its residual. Pick a long-term emittance-growth rate or
  diffusion coefficient instead.
- **`:equal_width` carries a second convergence parameter** (`slice_width`): a
  user who fixes the width will see convergence stall. Consider deriving the
  width from `ns` by default, or rejecting a fixed width above some `ns`.

## Watch

| CI segfault on Julia 1.12.6 | **open (2026-08-12)** | CPU-only CI on ubuntu (`ubuntu-latest`, Julia resolved by the floating '1.12' pin to 1.12.6) dies with signal 11 right after the "MomentObserver append mode continues one table" testset; every printed testset passes. The identical suite passes on 1.12.4 locally, GPU-active AND with `CUDA_VISIBLE_DEVICES=""`. CI is pinned to 1.12.4 as mitigation; local 1.12.6 reproduction was launched 2026-08-12 (fresh depot, CPU-only) to obtain the native crash location — prime suspects are native-library finalizers (HDF5) or a 1.12.6 runtime regression. Loosen the CI pin only after diagnosis. **Correction and repro status (2026-08-13):** the version-flip framing was wrong — 1.12.6 was released 2026-04-10 and is still the newest 1.12.x (official versions.json), so *every* CI run since the workflow landed (2026-07-21) resolved '1.12' to 1.12.6, ~330 of them green. Exactly two segfault-class failures exist (derived from the complete 18-failure CI ledger: 12 are the known 2026-08-04 F2 deterministic-abort cluster starting at `baf0255`, 2 are 2026-07-23 dev-era reds, and the two 2026-08-06 entries are a runner-provisioning failure in "Set up job" and a cancelled job — neither reached the suite): 2026-08-11T01:15Z on main (4ed84e4, test step died at 11.1 min where the green run just before took 17.0) and 2026-08-12T18:42Z on paper-cpc (12325fc — the *same commit* had passed on main at 18:32Z; died at 13.3 min vs 16.7). Same-commit pass/fail ten minutes apart and two different death points → flaky and timing-dependent, not a deterministic 1.12.6 regression; the 1.12.4 pin is a toolchain freeze justified by local-gate parity, not a fix, and its three green runs since prove nothing at a ~2/330 failure rate. The ubuntu24 runner-image update in that window (20260810, published 2026-08-11T10:37Z) postdates the first failure, so an image roll does not explain onset either. The 2026-08-12 local 1.12.6 reproduction came back **GREEN** (fresh depot, CPU-only, `--threads=4`, 170 testsets — `gate_1126_nogpu.log`), so the crash has not been seen off CI; a 6-run 1.12.6 stress loop pinned to 4 CPUs (`taskset`, `JULIA_CPU_THREADS=4`, core dumps enabled) was launched 2026-08-13 to emulate runner contention. Mechanism note: all Octopus HDF5 use is scoped `h5open() do` blocks, and the installed HDF5.jl 0.17.3 already defers finalizers via `trylock` with every API call behind `liblock` (JuliaIO/HDF5.jl#990/#1048), so the classic finalizer race *should* be closed — the mechanism is genuinely unknown until a native backtrace exists. The raw CI logs (crash banner, runner-image build) are auth-walled to the API; they need a logged-in browser: runs 31629024074 (Aug 12) and 31448717154 (Aug 11). Version-asymmetry caveat on the pin: the v1.12.4→v1.12.6 backports (85 commits) include GC/threading *fixes* 1.12.4 lacks — a missing GC write barrier (JuliaLang/julia#61120, heap-corruption class), `jl_exit_threaded_region` on interrupt (#60822), uninitialized memory in the GC stack (#60651) — plus a new heap-size-based full-GC heuristic (#60376) that changes when finalizer bursts fire, a plausible modulator for a latent race firing on 16 GB CI runners and never on a 503 GB idle node. So 1.12.6 is the strictly more-fixed binary and the pin buys local parity only; if the stress loop stays green, the decision to loosen should weigh #61120 against the two observed 1.12.6 flakes. **Stress result (2026-08-13):** 3 of 3 completed pinned-4-CPU 1.12.6 full suites green (~22 min each); the loop was stopped at 3 of 6 planned runs to free the repository for the survey/F16 campaign — at the observed CI rate (~2/330) six local runs were never a strong detector, and the ongoing detector is CI statistics after any future un-pin. The crash remains unreproduced off CI. **Recommendation (2026-08-14, owner decision):** re-float the pin to '1.12' (= 1.12.6). The evidence: ~330 green 1.12.6 runs against 2 flakes; 1.12.4 lacks 1.12.6's GC write-barrier fix (#61120, heap-corruption class) and threaded-region fix (#60822), so the pin trades a known-fixed silent-corruption class for the avoidance of a ~0.6%-per-run visible flake; a flaky red is loud and re-runnable, a write-barrier miss is neither. Against: the recorded "loosen only after diagnosis" condition, which this row's later evidence supersedes in the recommender's judgment but which the owner set the mitigation under. **Approved and acted (2026-08-14):** the pin floats again; ci.yml carries the operational note (a signal-11 red after a passing testset summary is the known flake — re-run, and tally it HERE). Flake tally since re-float: 0 in 4 runs (through `ac40f62`, 2026-08-15Z). This row stays open as the statistics ledger until either the crash reproduces somewhere debuggable or a Julia point release changes the question. |

(The CI row above is a statistics ledger, not a work item: tally any signal-11
flake after a passing testset summary here, per the note in `ci.yml`.)
