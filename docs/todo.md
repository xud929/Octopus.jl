# TODO

Open work only. This file was restructured on 2026-08-16: the record of
completed campaigns — closed rows, narratives, measurements, wrong turns and
their corrections, all previously in this file — is frozen verbatim in
[`history/todo_ledger_archive.md`](history/todo_ledger_archive.md), and the
recurring lessons distilled from that record live in
[`experiences.md`](experiences.md). Code and test comments citing a "todo row"
by U-number resolve against the archive. Standing
deliberately-not-being-done decisions also live in `experiences.md`.

When an item below closes: move its row (with the closing measurements) to the
archive under a dated note, and if it taught something reusable, add the
lesson to `experiences.md`.

## Open, most actionable first

| item | state | notes |
|---|---|---|
| Multi-process mixed execution policy (MPI x CPUThreads) | **open (2026-09-04, step 1 closed)** | **Step 1 closed 2026-09-04** (record: [`history/weak_strong_allocation_2026_09_04.md`](history/weak_strong_allocation_2026_09_04.md)): the 1.282 GiB/turn was ONE site -- the sliced strong beam built a fresh mutable `ThinStrongBeam` per slice per particle, 7 x 192 B = 1344 B per particle per turn (the Phase 0 record's "1.25 KB" is the decimal reading of the same number), 97.5% of profiled bytes and all but the per-call constants of the line's allocation, on both loop copies (fused and elementwise luminosity). An isbits `ThinStrongBeamSlice` carrier removes it with the kick arithmetic untouched: production digest 0xbb09efa9e44e2017 unmoved at 1/4/8/16/32/64 threads, allocation 1.282 -> 0.000 GiB/turn and GC share 3-53% -> 0% at every thread count, s/turn 2.23 / 0.69 / 0.45 / 0.65 / 0.63 / 0.73 -> 2.09 / 0.58 / 0.28 / 0.17 / 0.11 / 0.085 at 1/4/8/16/32/64 threads (8.6x at 64; the 8-16-thread cap is gone and the curve is monotone to 64, same protocol, same box). Pinned: "CPU weak-strong strong-beam tracking allocation does not scale with the particle count" (growth 2.000 unfixed, 1.000 fixed) and a `GaussianStrongBeam` arm on the weak-strong thread-invariance pin; `profiling/benchmark_track_cpu.jl` carries the localisation mode (`OCTOPUS_BENCH_ALLOC_PROFILE=1`). The Phase 0 weak-strong P x T inputs are superseded by the after-curve in the record. NEXT: step 2 (policy type + collective seam), which starts by resolving the two environment traps below by test. Phase 0 record follows. Owner-directed. Scope: SINGLE NODE (owner decision 2026-08-19); the per-rank core is reused unchanged — the policy COMPOSES `CPUThreadsExecutionPolicy`, and 1-rank MPI must reproduce today's digests bit-for-bit. Phase 0 measured (record: [`history/multi_process_phase0_2026_08_19.md`](history/multi_process_phase0_2026_08_19.md); instruments: `profiling/benchmark_track_cpu.jl`, `profiling/mpi_collide_prototype.jl`): weak-strong tracking is allocation/GC-bound (1.282 GiB/turn at every thread count, GC 3.2%→30.8%→52.2% at 1/16/64 threads, optimum 16, digest thread-count-invariant) — the pre-fix-1 disease, never treated on this path; strong-strong PIC at 8 ranks x 8 threads socket-bound is **1.86x** over socket-bound 1x16 (2.23x over unbound; 41.0 → 2.6 → 1.15 s/turn lineage), socket binding alone is 17–22% free, and 2x32 loses to 2x16 at perfect locality (the 16-thread optimum is per-process). Transport decided: MPI.jl as weakdep extension over a ~6-function collective seam (serial passthrough in core); Distributed closed by measurement — the deterministic rank-ordered collective already costs 0.10–0.35 s/turn (O(P) fold) at 2–8 ranks over MPI shm, and Distributed's per-message overhead on 450 collectives/turn would eat most of a 2x gain. Decided order: (1) weak-strong allocation extraction, (2) policy + seam with communicator-read receipts, (3) weak-strong MPI (task-level seams only, rank-0 artifact), (4) strong-strong per solver (soft-Gaussian → PIC deposit→solve all-sum → gpic → spectral); determinism: fixed-P bit-repeatability pinned, cross-P at CPU/CUDA parity tolerance class, lane-aligned sharding where `_SLICE_FOLD_LANES` makes cross-P bit-invariance cheap. Two measured env traps gate Phase 1 (HDF5_jll flips to an absent mpi+openmpi artifact when MPIPreferences is visible in the load path; HDF5's own MPIPreferences dep then locks MPI.jl to MPICH_jll defaults — so the prototype ran MPICH_jll under hydra, bb Open MPI unreachable in-process; resolve by test: matching MPI HDF5_jll variant vs pinned serial HDF5 vs accepting MPICH_jll). Open riders: gaussian collide flatline at 2x16/4x16 (2.42/2.46/2.52 despite halved/quartered per-rank particles, then 2x at 8x8 — something per-pair and particle-count-independent dominates); gaussian comm phase 3–6x PIC's for identical code (suspected idle-thread spin vs the serial fold, unprobed); the O(P) fold wants interleaving now and a deterministic tree past ~16 ranks. |
| Tracked `Manifest.toml` predates `Project.toml` | **open (2026-09-04)** | Every `Pkg.test` prints "The project dependencies or compat requirements have changed since the manifest was last resolved" and re-resolves into a temp env (the step-1 gate ran with CUDA 6.2.1 -> 6.3.1, JLD2 0.6.5 -> 0.6.6, SpecialFunctions 2.8.0 -> 2.9.0 re-resolved). The tracked Manifest was last committed 2026-08-04; Project.toml gained the `Logging` test dep on 2026-08-06. The development guide's claim that the Manifest "reproduces the exact environment" is false until it is re-resolved and committed; class "docs plus anything else" -> full gate, and the gate log should then show no re-resolve banner. |
| Run-artifact niceties deferred at step 4 | **open (2026-08-18)** | The artifact campaign closed 2026-08-18 (all four steps; full record: the archive’s "The run artifact: one output file per task" row and [`design/run_artifact.md`](design/run_artifact.md)). Three recorded niceties were deliberately deferred, none load-bearing. ~~s-position attributes on probe groups~~ **landed 2026-08-19**: probes stamp the design’s `attrs: name, s` at bind — the position comes from the ONE arc traversal (`_collect_spec_s!`, the U11-1 walker, gaining a probe arm that keeps the zero-length-advances-nothing contract), carried observer-keyed through a prepare scope so both tasks’ different prepare chains need no signature change; the reader surfaces `s` beside the columns (`nothing` for task-hook probes and older files). Pinned: WS line probes at 1.0/1.5 with a hook probe reading nothing, and SS probes reading their OWN line’s arc length (2.0 vs 3.0). Remaining: element names for the weak-strong luminosity channel labels (positional `strong_beam_<i>` until runtime elements carry names; the luminosity channels’ own `s` attribute belongs with this, the producer-identity work), and the live text mirror as a first-class option (the design’s `tail -f` posture; today nothing streams text). |
| CUDA in-suite vs standalone last-bit divergence: mechanism unknown | **open (2026-08-19)** | The distilled open question from the wobble-tally row's observations 10-12 (evidence and receipts live THERE; this row is the investigation ticket). Established: for the pinned `Random.Xoshiro(29)` 30,000-point vector, `_cuda_live_z_stats`'s masked-arm sigma is 0.007009746250353312 in ANY standalone process and 0.007009746250353311 (1 ulp low) in ANY full-suite process — deterministic on both sides, reproducible in-process (discriminator receipt: recompute matches first GPU value), with n_live/zmin/zmax/mean bitwise equal in all contexts. The kernel (`_cuda_lane_z_moment_kernel!`) is launch-shape-invariant by construction (stride is the compile-time `_SLICE_FOLD_LANES`, lane content a function of lane id only, host fold serial); no fastmath/math_mode setter exists in src/ or test/; so SOMETHING in suite execution history changes the compiled arithmetic or device context for the Val(2) pass. Investigation plan, in order of information per hour: (1) dump the 4096 `lane_sums` for the pinned masked arm in both contexts and diff — all-lanes-equal localizes the flip to the host fold/final scalar ops; few-lanes-moving names the particles and the arithmetic; (2) if lanes move, compare the kernel's PTX/SASS between contexts (`CUDA.@device_code_ptx`) — a codegen difference is then directly visible; (3) if PTX is identical, bisect the suite prefix (binary search over testset ranges ahead of "Slice geometry") for the state-setter. Member 2 of the family (the gpic A/B two-profile coin, bit-exact across processes) plausibly shares the same root: whatever state distinguishes contexts may also pick between its two profiles — check both once (1) lands. The discriminator in the "Slice geometry" testset logs every occurrence meanwhile; the 1-ulp bound (owner decision 2026-08-19) keeps the gate honest without hiding the phenomenon. |
| The observer schema-vs-report check is an identity for derived reports | **open (2026-08-06, U4-18)** | `validate_configuration_metadata` compares each observer's option-schema keys against its `configuration_report` entry names. That is a real check for the five observers whose reports are hand-written tuples, and an IDENTITY for any observer that derives its report by iterating the schema — `configuration_report(::BPMObserver)` does exactly that, so it passes for any possible schema, including a wrong one. A "every option names a field of the observer" substitute was tried and reverted: an option is a CONSTRUCTOR KEYWORD, not a field, and `BeamMomentObserver`, `JLD2BeamMomentObserver` and `MomentObserver` all legitimately expose `capacity` while storing `buffer_capacity`, so the check failed on correct code. Making this non-vacuous needs the constructor's keyword set, which Julia does not expose directly — `Base.kwarg_decl` on the method is the usual route and is fragile across versions. The other half of this lead (a `StrongStrongTask` default compared against a hand-written literal rather than the real constructor) is already closed by U5-4. |
| Three metadata-validator checks are tautologies | **open (2026-08-06, U12-8)** | `validate_element_metadata`'s friendly-schema, friendly-construction_help and friendly-example-kind checks compare two values that both route through `_element_meta_or_nothing`, which returns the identical `ElementMeta` object for a spec type and its friendly constructor: measured, 30 of 30 kinds resolve to the SAME meta and none to a different one. So the comparison is `x == x` for every registered kind. The checks are not useless — they bite when `register_friendly_alias!` points a friendly type at a different meta, which is confirmed to be caught — but that is alias misregistration, not the schema agreement their error messages claim, and the messages should say so. Making them non-tautological needs the friendly constructor's own keyword set, the same obstacle recorded for U4-18. |
| Three metadata channels are unvalidated free text | **open (2026-08-06, U12-6)** | `ParamMeta.unit`, `physics_keywords` and `description` are presented as authoritative and none is checked for truth. `unit` is only ever concatenated into help output; keywords are checked for MEMBERSHIP in `ALLOWED_PHYSICS_KEYWORDS` but never for accuracy; `description` is stored and echoed. Measured by injection: `unit="furlong"` on a length, `[:radiation, :beam_beam, :collimation]` on a plain kick, and `description = "Thick quadrupole magnet with fringe fields"` on a THIN element all pass unnoticed. AGENTS.md stakes agent orientation on exactly these fields ("Agents use these tags to locate related implementations and examples"), so this bounds what "self-describing source" currently guarantees. No automated consumer acts on them today, which is why it is Low. A dimensional check on `unit` against the parameter's use is the only one of the three that looks mechanically checkable; keyword and description accuracy are semantic and would need either a curated cross-check or an agent pass. |
| `validate_configuration_metadata` enumerates types by hand | **open (2026-08-05 row remainder)** | The remainder of the "metadata validator caught 1 of 13" row after its later closures (declared defaults are now runtime-checked per U12-1; declared-parameter-is-read is the effectiveness contract): the configuration validator's hardcoded type enumeration means a new solver or observer is unchecked until someone edits the tuple, and `Contracts.jl` carries a second hand-copy of the solver list — the same shape U9-2 closed for elements, still open for configurations. Measured staleness when written: `GaussianPICPoissonSolver` absent entirely, `BPMObserver` absent, no task-level schema. Full record: the archive's "element metadata validator" row. |
| CPU threading: remaining measured headroom | **open leads (2026-08-10 campaign remainder)** | The campaign closed at PIC 41.0→3.0, gpic 65.5→6.8, spectral 5.0→2.6 s/collide with bit-identical digests; the ceiling is memory bandwidth, measured three ways. Remaining leads, by measured headroom: `FFTW.set_num_threads` is still nowhere (threaded FFTW plans reassociate — measure the bit cost before adopting), and further gains need less traffic per unit of work, not finer division of the same traffic. The CPU **6D spectral** performance campaign (four boundary solves per slice pair) remains DEMOTED (2026-07-24) but open. Running record: [`history/cpu_threading_2026_08_09.md`](history/cpu_threading_2026_08_09.md) and the archive's CPU-threading row. |
| Bmad reference cases for `misalign_convention=:bmad` | blocked on a tool | The **default** convention has no reference case: its reference point, rotation order, and now the frame a misalignment is quoted in against a `ref_tilt` are all read from Bmad source, never measured. `:madx` is pinned at ~5e-13 throughout. Needs Bmad in the validation path; one comparison would close all three. *(2026-08-14: a fourth rider folds in — an external reference case for `ThinAcceleratingCavitySpec`, which MAD-X cannot express (no reference-energy-changing cavity) and Bmad's `lcavity` can.)* *(2026-08-15: a fifth rider — a PURE curved quadrupole reference, `h != 0` with `b0 = 0`. MAD-X 5.03.06's SBEND `k0` is measured IGNORED by its PTC translation (outputs bit-identical with k0 absent, 0, 0.12, 1e-6), so `h != b0` has no spelling there; Bmad's independent `g` vs field would express it. The h = b0 curved channels ARE now PTC-pinned — `cfbend_quad_heavy` at the polygon working point, `cfbend_skew` for the odd Psi_1 branch, `cfbend_k3`/`cfbend_k5` for high orders through EFCOMP — all at ~5e-13.)* |
| Spectral rms CPU/CUDA lane-fold 1-ulp counterexample | **open (2026-08-18, one observation; owner-directed note)** | The N5 bit-identity pin ("The spectral rms and Dirichlet box are bit-identical across backends") failed ONCE, at `_masked_rms(v, nothing)`: CPU 9.94237577349687e-5 vs CUDA 9.942375773496871e-5 — 1 ulp — for a specific 30,000-point `randn` vector. Established: the two lane folds read as identical by construction (same `_SLICE_FOLD_LANES` partition, same in-lane accumulation order, same serial host fold — which SHOULD force bit equality for every input); 0 of 5000 fresh draws reproduce a mismatch (95% upper bound ~6e-4/draw); systematic FMA contraction is excluded by that rate (it would mismatch nearly every draw). The offending vector was NOT capturable: the test drew from the UNSEEDED task RNG (`set_global_rng!` seeds only the Octopus counter RNG), so the vector was a deterministic function of the suite configuration — dozens of green gates tested one lucky draw, and the run-artifact campaign's testset edits shifted the stream onto the counterexample, failing every post-campaign full gate deterministically while standalone probes passed. The FLAKE is closed (the test now draws from a local `Xoshiro(61)`; pinned vector verified equal on both backends under check-bounds). The open QUESTION is the mechanism: a rare rounding-boundary corner of an unnoticed codegen asymmetry, or a hardware transient — this GPU runs with ECC DISABLED (`nvidia-smi -q`), so a single flipped low mantissa bit in one lane partial is a live hypothesis for a one-off. Actionable leads: (1) if ANY bit-identity pin mismatches again, capture the data first (write `v` to a file before comparing) — with the vector in hand the divergence is a mechanical lane-by-lane trace; (2) a bulk differential hunt (millions of draws, or adversarial vectors built to sit on rounding boundaries of the lane partials) would separate "rare structural corner" (reproducible once found) from "transient" (never reproducible); (3) if structural, compare the PTX of `_cuda_lane_z_moment_kernel!` against the CPU loop's asm for the s2 pass before trusting either. *(TALLY, 2026-08-19 -- this row is the ledger for CUDA near-identity pin wobbles. Second member: "CUDA GaussianPIC singular-reference fallback matches PIC" failed in-suite THREE times the same night (gates at atol=2e-18 twice on different assertion sites, then once ABOVE a derivation-raised atol=1e-16 with identical printed array ends -- which kills any small-wobble account, since a wrong route diverges at O(kick)~1e-15 and rtol=2e-12 alone would catch it). Measured: 20 repetitions x both arms under the suite's own --check-bounds/--threads are BIT-EXACT, worst abs diff 0.0, before AND after an artifact-carrying strong-strong task -- so the trigger is suite state my probes do not reproduce. Surviving hypotheses: a hidden mid-array NaN (isapprox is false against NaN at ANY tolerance and truncated printing hides the middle), or a gross mid-array divergence, either way appearing only under suite history. Acted on per this row's own lead (1): the pin now CAPTURES on mismatch -- both arrays dumped whole to a kept tempdir with a route/column tag before the comparison discards the evidence (`checked()` in the testset) -- and the raised atol stays with its derivation recorded in the test comment. The next recurrence, local or CI, is a mechanical diff. Four observations total on this row, all 2026-08-18/19, none reproduced in isolation; the capture trap is the instrument the row now waits on.)* *(FIFTH+SIXTH observations and the trap's FIRST FIRING, 2026-08-19, during the multi-process Phase 0 full-suite gate: 2 of 49 fail in "CUDA GaussianPIC singular-reference fallback matches PIC", both on `route_wavefront_true` column 4 (py), one per beam. Evidence committed whole: [`history/gpic_wobble_capture_2026_08_19.txt`](history/gpic_wobble_capture_2026_08_19.txt). The mechanical diff KILLS two hypotheses: no NaN or Inf anywhere in either array (the hidden-NaN account is dead for this recurrence), and no gross divergence (max |diff| = 2.9e-16). What it is instead: 13 of 64 py entries differ, at CONTIGUOUS center-of-array indices, on the zero-marginal-width route where symmetry puts py at pure roundoff -- both arrays are SMOOTH ~1e-16-scale noise profiles (values rise and fall coherently with index), differing by up to 2.9e-16, and the array-isapprox atol=1e-16 sits below the ~6e-16 norm of that difference over 13 entries. Thirteen coherent smoothly-varying entries also kill the single-bit-flip/ECC account for THIS failure: the two solvers computed DIFFERENT deterministic roundoff-noise profiles in-suite while being bit-exact standalone, 20 of 20. New lead from the shape: whatever differs between suite state and standalone state must be compilation or planner state on this route -- check whether the field/Green FFT planning the fallback reaches (FFTW wisdom or MEASURE-class planning on host-side pieces, CUFFT plan selection/reuse across the pic-then-gpic calls) is machine-state-dependent; a state-dependent plan legitimately moves last-bit noise and would explain suite-only failure with standalone bit-exactness.)* *(SEVENTH observation, the gate re-run the same evening -- and the mechanism narrows to a BINARY coin. Same testset, 2 of 49 again, but different sites: `route_wavefront_true` b1c4 AND `route_sequential_true` b2c4, one beam each. The decisive datum: the second firing's wavefront b1c4 arrays are VALUE-FOR-VALUE the first firing's with expected/actual SWAPPED (appended to the same evidence file) -- so exactly TWO reproducible py-noise profiles exist for this route, and EACH collide call draws one of them. The pin fails iff the paired PIC and GaussianPIC calls straddle the draw, which they can only do if per-process state changes BETWEEN two closely spaced collides in one testset. Refined model, consistent with every observation on this row: a CUFFT-class handle (plan) evicted and recreated between the paired calls picks the other of two algorithms with different last-bit rounding -- CUDA.jl frees cached handles under GPU memory pressure, late-suite the pool has grown into most of free VRAM, and standalone processes never evict (hence 20/20 bit-exact both arms: a per-process draw can never fail a WITHIN-process comparison). Also fits the 2026-08-18 onset (the artifact campaign grew the suite's GPU footprint) and today's elevated rate (a 26-day IJulia kernel is holding 3.3 GiB of the 24.5 GiB card, shrinking headroom -- candidate, not proven). Directly testable: in one process, force pool pressure (allocate/free large CUDA arrays) between the paired collides and watch for the flip; and compare the two profiles' FFT stages. If confirmed, the fix conversation is about plan-cache pinning for paired-comparison tests or a derivation-based atol for a zero-symmetry column, not about tracking down a phantom.)* *(EIGHTH observation, gate attempt 3 the same evening: `route_wavefront_false` b1c4 alone, 48/1 -- the THIRD distinct route, and the decisive bitwise result: across all three suite processes the b1c4 pair is drawn from EXACTLY TWO bit-identical profiles, gate1 (expected,actual)=(A,B), gates 2 and 3 =(B,A), every vector equal to its counterpart to the bit; norm(A-B)=6.3e-16 against the pin's atol=1e-16. Two bit-exact profiles across processes EXCLUDES free-running atomic reordering as the whole story at this size (n=64, grid 16x16): the recorded atomic-deposition account survives only in its two-orderings-of-one-contended-pair form, which is also the simplest model that fits everything. Killing a 26-day 3.3 GiB IJulia squatter mid-evening did not stop the coin (observation 8 came after), so external VRAM headroom is not the trigger's dominant term. Owner decision (2026-08-19): the pin STAYS at atol=1e-16; posture is re-run-until-aligned, tallying each red here. The measured wobble norm 6.3e-16 vs atol 1e-16 means a straddling draw ALWAYS fails the gate; per-gate straddle rate tonight 3 of 3 (routes straddling: 2, 2, 1 of 3). Evidence file carries all three firings whole.)* *(NINTH observation, gate attempt 4: `route_wavefront_true` b1c4 alone, 48/1, (expected,actual)=(A,B) -- the SAME two profiles a fourth time, no new profile has ever appeared. Straddle counts per gate: 2, 2, 1, 1 of the four zero-width/rank-one comparisons; four consecutive red gates under the re-run-until-aligned posture. Gate attempt 5 aligned -- zero straddles, zero captures, suite green whole -- and carried the Phase 0 commit; the straddle series 2,2,1,1,0 is tonight's measured coin.)* *(DECISION REVISED, 2026-08-19 later the same evening, owner: the re-run-until-aligned posture above is superseded -- the pin's atol is raised 1e-16 -> 2e-15 with the measured derivation (3x above the wobble norm 6.3e-16, 4x under the wrong-route wholesale floor ~8e-15 in norm, rtol=2e-12 untouched and still the wholesale-divergence catch). The capture trap stays armed at the new bound: any future firing is a NEW phenomenon, not the tallied A/B coin, and this row remains its ledger. Derivation recorded beside the original in the testset comment.)* *(TENTH observation, 2026-08-19, the atol-change gate itself -- and a THIRD family member: "Slice geometry is bit-identical across backends" failed 57/1 at `sc.sigma == sg.sigma`, CPU vs CUDA `_live_z_stats` sigma 1 ulp apart (0.0070478337686169625 vs ...63) on a 30,000-point vector. Same defect class as member 1 to the letter: `set_global_rng!(seed=29)` seeds only the counter RNG, so the bare `randn(n)`/`rand(n)` drew from the PER-RUN Test RNG -- 1-ulp roulette, unlucky draw. Reproduction handle printed by Test: outermost Xoshiro(0x805ec498f9fbe4ef, 0x8235fa97f158ef61, 0x2f8e7e3431c095e9, 0x8ab97dc77a8d52d3, 0xbfdb9bda20a6e464). Fixed by member 1's own precedent: the draws now come from a local Random.Xoshiro(29), and the pinned vectors are verified bitwise equal on both backends, both mask arms, under --check-bounds. The gpic pin at its new atol=2e-15 passed this gate cleanly -- the two changes are independent. Standing lesson sharpened: EVERY bit-identity pin whose input comes from a bare randn/rand is playing this roulette; sweep candidates when one next fires.)* *(ELEVENTH observation, 2026-08-19, the very next gate -- and it changes the member-3 account: the PINNED Xoshiro(29) vector ALSO failed sigma by 1 ulp (CPU 0.007009746250353312, CUDA ...311, masked arm), while the same vector is verified bitwise equal on both backends standalone under --check-bounds, both arms. So member 3 is NOT (only) draw roulette: on a FIXED input, in-suite CUDA sigma moved one ulp while in-suite CPU matched standalone exactly. Mechanism narrowed by reading: `_cuda_lane_z_moment_kernel!` is launch-shape-INVARIANT (stride is the constant `_SLICE_FOLD_LANES`, lane content a function of t only, host fold serial), and the MEAN passed bitwise, so the flip lives in the Val(2) lane pass or its 4096-element host fold -- ambient launch config is exonerated for this kernel. That shape (a single lane partial off by one low bit, once, unreproducible standalone) is the row's original ECC-off transient hypothesis, now with two firings in consecutive gates after ~4 h of continuous GPU load (card read 32 C idle immediately after, so not heat-soaked at rest; load-time state unknown). DISCRIMINATOR ARMED in the testset: on any sigma mismatch the device stats are recomputed in-process and logged -- a transient cannot repeat, ambient state must; the next firing answers the row's central question mechanically. Note the family split: member 2 (gpic A/B) is bit-exact two-state = NOT a transient; member 3's evidence now leans transient. They are different phenomena sharing one ledger.)* *(TWELFTH observation, the very next gate -- THE DISCRIMINATOR ANSWERED: recompute_matches_first_gpu = true, recompute_matches_cpu = false, with the SAME pair of sigma values as observation 11 (0.007009746250353312 CPU / ...311 CUDA, masked arm). The transient account is DEAD for member 3: the in-suite CUDA value reproduces deterministically within the process, and across processes. Member 3 is now fully characterized as deterministic per-process state: the CUDA masked-sigma arithmetic in-suite differs from standalone for boundary-sitting inputs -- no math_mode/fastmath setter exists anywhere in src/ or test/ (grepped), the kernel is launch-shape-invariant by construction, and the mean passes bitwise, so the mechanism is still open (compiled-code or device-context state; the next instrument is a lane_sums dump in both contexts to see WHICH of the 4096 lanes move). OPERATIONAL consequence: the pinned Xoshiro(29) vector sits ON the flip boundary, so the suite now fails DETERMINISTICALLY -- and reinterpreting tonight's roulette record (5 green, 1 red on fresh draws), roughly 1 in 6 random vectors sits on the boundary under suite state. The pin's literal claim (CUDA sigma BITWISE equals CPU in any process state) is measured FALSE; the true invariant is: count/min/max/mean bitwise equal, sigma within 1 ulp. Owner decision (2026-08-19): the sigma assert becomes a <= 1 ulp bound with this derivation recorded at the assert; count/extrema/mean stay bitwise; the pinned Xoshiro(29) vector stays (now valuable precisely BECAUSE it sits on the boundary -- it exercises the 1-ulp path deterministically in-suite); the discriminator stays armed and logs every occurrence. The open mechanism question keeps this tally entry live: WHY the in-suite process computes a different last bit than a standalone process for the identical kernel and input -- next instrument is a lane_sums dump in both contexts.)* |

## Open questions from studies

- **Observable-matched slicing nodes (Phase 3c, one experiment):** minimize the
  kick error (or a luminosity-weighted error) directly over `{z_k, w_k}` at
  fixed `ns` to get the true lower bound the slicing rules should be scored
  against. The Gauss-Hermite result is direct evidence that source-fidelity
  objectives and kick accuracy come apart. Per-machine result, acceptable for a
  fixed design point; hypothesis, not established practice.
- **Hadron-side slicing stopping criterion:** Furman's `Q ~ 4/sqrt(tau)`
  applies to the electron ring; the proton ring has no comparable `tau`, so
  nothing masks its residual. Pick a long-term emittance-growth rate or
  diffusion coefficient instead.
- **`:equal_width` carries a second convergence parameter** (`slice_width`): a
  user who fixes the width will see convergence stall. Consider deriving the
  width from `ns` by default, or rejecting a fixed width above some `ns`.

## Watch

| CI segfault on Julia 1.12.6 | **open (2026-08-12)** | CPU-only CI on ubuntu (`ubuntu-latest`, Julia resolved by the floating '1.12' pin to 1.12.6) dies with signal 11 right after the "MomentObserver append mode continues one table" testset; every printed testset passes. The identical suite passes on 1.12.4 locally, GPU-active AND with `CUDA_VISIBLE_DEVICES=""`. CI is pinned to 1.12.4 as mitigation; local 1.12.6 reproduction was launched 2026-08-12 (fresh depot, CPU-only) to obtain the native crash location — prime suspects are native-library finalizers (HDF5) or a 1.12.6 runtime regression. Loosen the CI pin only after diagnosis. **Correction and repro status (2026-08-13):** the version-flip framing was wrong — 1.12.6 was released 2026-04-10 and is still the newest 1.12.x (official versions.json), so *every* CI run since the workflow landed (2026-07-21) resolved '1.12' to 1.12.6, ~330 of them green. Exactly two segfault-class failures exist (derived from the complete 18-failure CI ledger: 12 are the known 2026-08-04 F2 deterministic-abort cluster starting at `baf0255`, 2 are 2026-07-23 dev-era reds, and the two 2026-08-06 entries are a runner-provisioning failure in "Set up job" and a cancelled job — neither reached the suite): 2026-08-11T01:15Z on main (4ed84e4, test step died at 11.1 min where the green run just before took 17.0) and 2026-08-12T18:42Z on paper-cpc (12325fc — the *same commit* had passed on main at 18:32Z; died at 13.3 min vs 16.7). Same-commit pass/fail ten minutes apart and two different death points → flaky and timing-dependent, not a deterministic 1.12.6 regression; the 1.12.4 pin is a toolchain freeze justified by local-gate parity, not a fix, and its three green runs since prove nothing at a ~2/330 failure rate. The ubuntu24 runner-image update in that window (20260810, published 2026-08-11T10:37Z) postdates the first failure, so an image roll does not explain onset either. The 2026-08-12 local 1.12.6 reproduction came back **GREEN** (fresh depot, CPU-only, `--threads=4`, 170 testsets — `gate_1126_nogpu.log`), so the crash has not been seen off CI; a 6-run 1.12.6 stress loop pinned to 4 CPUs (`taskset`, `JULIA_CPU_THREADS=4`, core dumps enabled) was launched 2026-08-13 to emulate runner contention. Mechanism note: all Octopus HDF5 use is scoped `h5open() do` blocks, and the installed HDF5.jl 0.17.3 already defers finalizers via `trylock` with every API call behind `liblock` (JuliaIO/HDF5.jl#990/#1048), so the classic finalizer race *should* be closed — the mechanism is genuinely unknown until a native backtrace exists. The raw CI logs (crash banner, runner-image build) are auth-walled to the API; they need a logged-in browser: runs 31629024074 (Aug 12) and 31448717154 (Aug 11). Version-asymmetry caveat on the pin: the v1.12.4→v1.12.6 backports (85 commits) include GC/threading *fixes* 1.12.4 lacks — a missing GC write barrier (JuliaLang/julia#61120, heap-corruption class), `jl_exit_threaded_region` on interrupt (#60822), uninitialized memory in the GC stack (#60651) — plus a new heap-size-based full-GC heuristic (#60376) that changes when finalizer bursts fire, a plausible modulator for a latent race firing on 16 GB CI runners and never on a 503 GB idle node. So 1.12.6 is the strictly more-fixed binary and the pin buys local parity only; if the stress loop stays green, the decision to loosen should weigh #61120 against the two observed 1.12.6 flakes. **Stress result (2026-08-13):** 3 of 3 completed pinned-4-CPU 1.12.6 full suites green (~22 min each); the loop was stopped at 3 of 6 planned runs to free the repository for the survey/F16 campaign — at the observed CI rate (~2/330) six local runs were never a strong detector, and the ongoing detector is CI statistics after any future un-pin. The crash remains unreproduced off CI. **Recommendation (2026-08-14, owner decision):** re-float the pin to '1.12' (= 1.12.6). The evidence: ~330 green 1.12.6 runs against 2 flakes; 1.12.4 lacks 1.12.6's GC write-barrier fix (#61120, heap-corruption class) and threaded-region fix (#60822), so the pin trades a known-fixed silent-corruption class for the avoidance of a ~0.6%-per-run visible flake; a flaky red is loud and re-runnable, a write-barrier miss is neither. Against: the recorded "loosen only after diagnosis" condition, which this row's later evidence supersedes in the recommender's judgment but which the owner set the mitigation under. **Approved and acted (2026-08-14):** the pin floats again; ci.yml carries the operational note (a signal-11 red after a passing testset summary is the known flake — re-run, and tally it HERE). Flake tally since re-float: 0 in 4 runs (through `ac40f62`, 2026-08-15Z). This row stays open as the statistics ledger until either the crash reproduces somewhere debuggable or a Julia point release changes the question. |

(The CI row above is a statistics ledger, not a work item: tally any signal-11
flake after a passing testset summary here, per the note in `ci.yml`.)
